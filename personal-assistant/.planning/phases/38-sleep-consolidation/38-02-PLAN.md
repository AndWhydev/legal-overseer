---
phase: 38-sleep-consolidation
plan: 02
type: execute
---

<objective>
Surface the morning briefing on the dashboard and make it available via API.

Purpose: The morning briefing generated by sleep consolidation should be visible to the user on first login and available to the chat agent.
Output: API endpoint + dashboard card for morning briefing.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/dashboard/weekly-summary-card.tsx
@src/app/api/dashboard/weekly-summary/route.ts
@.planning/phases/38-sleep-consolidation/38-01-SUMMARY.md

**From Plan 38-01:** Morning briefing stored in organisations.settings.morning_briefing as JSON.
**Existing pattern:** Weekly summary card fetches from /api/dashboard/weekly-summary. Follow same SWR pattern.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create morning briefing API endpoint</name>
  <files>src/app/api/dashboard/morning-briefing/route.ts</files>
  <action>
Create GET endpoint that returns the morning briefing from org settings:

1. Auth: get user, get orgId (same pattern as other dashboard routes)
2. Query: SELECT settings->'morning_briefing' FROM organisations WHERE id = orgId
3. Return the briefing JSON, or empty briefing if none exists
4. Include a 'stale' flag: if generatedAt > 24h ago, mark as stale

Follow the pattern of weekly-summary/route.ts for auth and error handling.
  </action>
  <verify>curl with auth returns briefing JSON or empty structure.</verify>
  <done>GET /api/dashboard/morning-briefing returns briefing data.</done>
</task>

<task type="auto">
  <name>Task 2: Create morning briefing dashboard card</name>
  <files>src/components/dashboard/morning-briefing-card.tsx</files>
  <action>
Create a dashboard card component that displays the morning briefing:

1. Use SWR to fetch from /api/dashboard/morning-briefing (same pattern as WeeklySummaryCard)
2. Display sections:
   - Urgent items (red indicator, entity name + reason + deadline)
   - Follow-ups due (amber, entity + days since last contact)
   - Discoveries (blue, "New connection: X relates to Y")
   - Pending approvals count
3. If briefing is stale (>24h), show subtle "Last updated: X hours ago"
4. If no briefing data, show "Morning briefing generates overnight"
5. Match existing card pattern: Card/CardHeader/CardContent from shadcn, monochrome styling, no hyphens/emdashes in copy

Add the card to the dashboard layout in dashboard-redesign.tsx (in the operational cards row alongside WeeklySummaryCard).
  </action>
  <verify>Dev server compiles. Dashboard shows morning briefing card.</verify>
  <done>Morning briefing card renders on dashboard with live data from API.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] API endpoint returns briefing data
- [ ] Dashboard card renders correctly
- [ ] SWR fetching works
- [ ] Phase 38 complete
</verification>

<success_criteria>
- Morning briefing visible on dashboard
- API available for chat agent to reference
- Phase 38 complete — sleep consolidation operational
</success_criteria>

<output>
After completion, create `.planning/phases/38-sleep-consolidation/38-02-SUMMARY.md`
</output>
