---
phase: 35-entity-graph-foundation
plan: 03
type: execute
---

<objective>
Backfill entity_nodes from existing contacts and projects, integrate Google multimodal + Voyage embeddings, and wire extraction pipeline into the message ingestion flow.

Purpose: Populate the knowledge graph with existing data so Phase 36 (graph-aware retrieval) has content to work with. Connect the extraction pipeline so new data flows in automatically.
Output: Graph populated with existing contacts/projects, embeddings generated, extraction pipeline live.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-04-04-cognitive-memory-os-design.md
@src/lib/knowledge-graph/graph-queries.ts
@src/lib/knowledge-graph/entity-extractor.ts
@src/lib/rag/embedding-service.ts
@src/lib/rag/voyage-client.ts
@src/lib/channels/relay-daemon.ts
@src/lib/conversation/unified-pipeline.ts

**From Plans 35-01 and 35-02:** Tables exist, graph query helpers tested, extraction pipeline tested.
**Existing contacts table has:** id, org_id, name, slug, emails[], phones[], metadata JSONB
**Existing projects table has:** id, org_id, name, status, contact_id, metadata JSONB (phases, blockers, next_action)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Google multimodal embedding client + Voyage dual embedding</name>
  <files>src/lib/rag/google-embedding-client.ts, src/lib/knowledge-graph/embed-entity.ts</files>
  <action>
1. Create google-embedding-client.ts:
   - Use Google AI SDK (@google/generative-ai or REST API) with model 'text-embedding-004' (768d, supports task_type parameter)
   - Export embedText(text, taskType='RETRIEVAL_DOCUMENT'): Promise<number[]>
   - Export embedQuery(text): Promise<number[]> (uses task_type='RETRIEVAL_QUERY')
   - Lazy init pattern matching voyage-client.ts
   - API key from GOOGLE_API_KEY env var
   - Retry with exponential backoff (3 attempts)

2. Create embed-entity.ts:
   - Export embedEntityNode(supabase, node): Promise<void>
   - Generates text representation: "{type}: {name}. {properties summary}"
   - Calls Google embedding for primary (768d) → updates entity_nodes.embedding
   - Calls Voyage for secondary (1024d) → updates entity_nodes.text_embedding
   - Fire-and-forget safe (never throws)

Note: Use 'text-embedding-004' not multimodal model for now — text-only is sufficient for entity nodes. Multimodal can be added later when we ingest images/PDFs. This keeps the dependency simpler (no Vertex AI SDK needed, just the standard Google AI SDK).
  </action>
  <verify>TypeScript compiles. Manual test: ssh studio-server "cd /home/claude/bitbit/personal-assistant && node -e 'require(\"./src/lib/rag/google-embedding-client\").embedText(\"test\").then(v => console.log(\"dim:\", v.length))'"</verify>
  <done>Both embedding clients produce correct dimensions (768 for Google, 1024 for Voyage). embedEntityNode updates both vector columns.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill script — contacts → entity_nodes, projects → edges</name>
  <files>scripts/backfill-entity-graph.ts</files>
  <action>
Create a one-time backfill script that:

1. Fetch all contacts for each org:
   - For each contact: findOrCreateEntity(supabase, orgId, contact.name, 'person', [contact.slug, ...contact.emails, ...contact.phones])
   - Copy relevant metadata (email, phone) to entity_nodes.properties
   - Call embedEntityNode() for each created node

2. Fetch all projects for each org:
   - For each project: findOrCreateEntity(supabase, orgId, project.name, 'project', [project.name.toLowerCase()])
   - Copy metadata (status, phases, next_action) to properties
   - If project.contact_id exists: create edge (contact_entity → project_entity, 'owns')
   - If project has blockers in metadata: create edge per blocker (project → blocker_entity, 'blocked_by')

3. Log progress: "Backfilled X contacts, Y projects, Z edges for org {orgId}"

4. Make it idempotent — findOrCreateEntity handles dedup via alias matching.

Run via: npx tsx scripts/backfill-entity-graph.ts
  </action>
  <verify>Run the script. Check entity_nodes count matches contacts + projects count. Check entity_edges exist for project-contact relationships.</verify>
  <done>All existing contacts and projects have corresponding entity_nodes. Edges connect contacts to their projects. Embeddings generated for all nodes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire extraction pipeline into message ingestion</name>
  <files>src/lib/rag/embedding-service.ts, src/lib/channels/relay-daemon.ts, src/lib/conversation/unified-pipeline.ts</files>
  <action>
Add fire-and-forget call to extractAndPopulateGraph() at the same points where embeddings are currently enqueued:

1. In embedding-service.ts: After successful Pinecone upsert (where entity extraction already fires), add:
   extractAndPopulateGraph(supabase, orgId, chunk.text, {sender, channel, timestamp}).catch(() => {})

2. In relay-daemon.ts (line ~488-515 area where messages are enqueued for embedding): Add same fire-and-forget call after embedding enqueue.

3. In unified-pipeline.ts (lines ~497, 511 where user/assistant messages are enqueued): Add same call.

Pattern: Import extractAndPopulateGraph, call it with .catch(() => {}) so it never blocks the main pipeline. This matches the existing fire-and-forget pattern already used for entity extraction in embedding-service.ts.

Do NOT change the existing Pinecone pipeline — this runs alongside it.
  </action>
  <verify>Restart dev server. Send a chat message mentioning a contact name. Check entity_nodes and event_tuples tables for new entries.</verify>
  <done>New messages automatically populate the knowledge graph. Existing Pinecone pipeline unaffected. No errors in dev server logs from the extraction pipeline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Google embedding client returns 768d vectors
- [ ] Voyage embedding returns 1024d vectors
- [ ] Backfill script populates entity_nodes from contacts and projects
- [ ] Edges created between contacts and their projects
- [ ] New chat messages trigger graph extraction automatically
- [ ] No build errors, no runtime errors in dev server
- [ ] Existing Pinecone pipeline still works
</verification>

<success_criteria>
- Entity graph populated with existing contacts and projects
- Dual embeddings (768d + 1024d) on all entity nodes
- Message ingestion pipeline wired to extract entities/events
- Phase 35 complete — foundation ready for Phase 36 (graph-aware retrieval)
</success_criteria>

<output>
After completion, create `.planning/phases/35-entity-graph-foundation/35-03-SUMMARY.md`
</output>
