{
  "name": "@bitbit/core",
  "version": "0.1.0",
  "private": true,
  "description": "BitBit core engine — agentic loop, model router, orchestrator, tool system, channel pipeline",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "db:migrate": "node src/db/migrate.js",
    "db:seed": "node src/db/seed.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "@supabase/supabase-js": "^2.95.3"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
