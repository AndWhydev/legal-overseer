{
  "vulnerabilities": [
    {
      "id": "INJ-001",
      "type": "SQL Injection (Template Literal)",
      "severity": "CRITICAL",
      "file": "/repos/awu-landing/personal-assistant/src/lib/channels/imessage.ts",
      "line": 65,
      "entry_point": "ChannelAdapter.pull() - iMessage adapter",
      "user_controllable_parameter": "since (Date parameter)",
      "data_flow": "since parameter -> appleEpochNanos calculation -> Python script template literal with ${appleEpochNanos}",
      "vulnerable_code": "WHERE m.date > ${appleEpochNanos}",
      "description": "User-controllable 'since' date parameter flows into a Python script SQL query via string interpolation. An attacker could manipulate the 'since' parameter to inject SQL into the iMessage database query.",
      "impact": "SQL injection into macOS iMessage chat.db, potentially allowing unauthorized access to message history or database manipulation.",
      "exploitation_scenario": "If the 'since' parameter is controlled by user input (e.g., API parameter), an attacker could craft a malicious date value that breaks out of the numeric context and injects SQL commands.",
      "remediation": "Use parameterized queries in the Python script instead of string interpolation. Pass appleEpochNanos as a parameter to the SQL query."
    },
    {
      "id": "INJ-002",
      "type": "NoSQL Injection (Mass Assignment)",
      "severity": "HIGH",
      "file": "/repos/awu-landing/personal-assistant/src/app/api/tasks/[id]/route.ts",
      "line": 19,
      "entry_point": "PATCH /api/tasks/[id]",
      "user_controllable_parameter": "request.json() body",
      "data_flow": "request body -> .update(body) -> Supabase update query",
      "vulnerable_code": ".update(body)",
      "description": "The entire request body is passed directly to Supabase .update() without validation. This allows mass assignment where an attacker can modify any column in the tasks table, including columns that should be protected (e.g., org_id, created_at, metadata).",
      "impact": "Privilege escalation via column manipulation. Attacker could modify org_id to access other organizations' data, change task ownership, or inject malicious data into JSON columns.",
      "exploitation_scenario": "Authenticated user sends PATCH request with body containing org_id to modify protected fields.",
      "remediation": "Whitelist allowed fields before passing to update query."
    },
    {
      "id": "INJ-003",
      "type": "NoSQL Injection (Mass Assignment)",
      "severity": "HIGH",
      "file": "/repos/awu-landing/personal-assistant/src/app/api/contacts/[id]/route.ts",
      "line": 19,
      "entry_point": "PATCH /api/contacts/[id]",
      "user_controllable_parameter": "request.json() body",
      "data_flow": "request body -> .update(body) -> Supabase update query",
      "vulnerable_code": ".update(body)",
      "description": "The entire request body is passed directly to Supabase .update() without validation. Attacker can modify any column including org_id or inject malicious data into JSON columns.",
      "impact": "Privilege escalation, data poisoning. Attacker could change org_id to access other organizations' contacts.",
      "exploitation_scenario": "Authenticated user sends PATCH with modified org_id or malicious JSON payloads.",
      "remediation": "Whitelist allowed fields and validate JSON structure."
    },
    {
      "id": "INJ-004",
      "type": "NoSQL Injection (Mass Assignment)",
      "severity": "HIGH",
      "file": "/repos/awu-landing/personal-assistant/src/app/api/tasks/route.ts",
      "line": 40,
      "entry_point": "POST /api/tasks",
      "user_controllable_parameter": "request.json() body",
      "data_flow": "request body fields -> .insert() with body.* -> Supabase insert query",
      "vulnerable_code": "metadata: body.metadata || {}",
      "description": "User-controlled metadata field accepts arbitrary JSON without validation.",
      "impact": "Data poisoning via malicious JSON structures.",
      "exploitation_scenario": "Attacker sends POST with malicious metadata objects.",
      "remediation": "Validate metadata structure with schema validator."
    },
    {
      "id": "INJ-005",
      "type": "NoSQL Injection (Mass Assignment)",
      "severity": "HIGH",
      "file": "/repos/awu-landing/personal-assistant/src/app/api/contacts/route.ts",
      "line": 39,
      "entry_point": "POST /api/contacts",
      "user_controllable_parameter": "request.json() body",
      "data_flow": "request body fields -> .insert() with body.* -> Supabase insert query",
      "vulnerable_code": "profile_data: body.profile_data || {}",
      "description": "User-controlled JSON fields are inserted without validation.",
      "impact": "Data poisoning, potential prototype pollution.",
      "exploitation_scenario": "Attacker sends malicious JSON in profile_data.",
      "remediation": "Validate JSON structure before insertion."
    },
    {
      "id": "INJ-006",
      "type": "NoSQL Injection (Mass Assignment)",
      "severity": "HIGH",
      "file": "/repos/awu-landing/personal-assistant/src/app/api/activity/route.ts",
      "line": 40,
      "entry_point": "POST /api/activity",
      "user_controllable_parameter": "request.json() body",
      "data_flow": "request body fields -> .insert() into activity_feed",
      "vulnerable_code": "result: body.result || null",
      "description": "User-controlled result field inserted without sanitization.",
      "impact": "Activity log poisoning, potential XSS if displayed.",
      "exploitation_scenario": "Attacker injects XSS payload into logs.",
      "remediation": "Sanitize all user input fields."
    },
    {
      "id": "INJ-007",
      "type": "Deserialization (Unsafe YAML)",
      "severity": "HIGH",
      "file": "/repos/awu-landing/landing-page/lib/bitbit/tool-loader.ts",
      "line": 60,
      "entry_point": "loadTools() and loadHandlers()",
      "user_controllable_parameter": "YAML file content",
      "data_flow": "fs.readFileSync() -> yaml.load() -> tool configuration",
      "vulnerable_code": "yaml.load(content)",
      "description": "yaml.load() instead of yaml.safeLoad() allows code execution during parsing.",
      "impact": "Potential RCE if YAML file can be modified.",
      "exploitation_scenario": "Attacker modifies tools.yaml with malicious YAML.",
      "remediation": "Use yaml.safeLoad() to prevent code execution."
    },
    {
      "id": "INJ-008",
      "type": "Command Injection (Indirect)",
      "severity": "MEDIUM",
      "file": "/repos/awu-landing/personal-assistant/src/lib/channels/imessage.ts",
      "line": 15,
      "entry_point": "ChannelAdapter.pull()",
      "user_controllable_parameter": "since parameter",
      "data_flow": "Python script with user data -> execSync()",
      "vulnerable_code": "execSync() with embedded user data",
      "description": "User data embedded in Python script executed via execSync.",
      "impact": "Potential command injection via multi-stage attack.",
      "exploitation_scenario": "SQL injection leading to Python code execution.",
      "remediation": "Use parameterized queries in Python script."
    }
  ],
  "summary": {
    "total_vulnerabilities": 8,
    "critical": 1,
    "high": 6,
    "medium": 1
  }
}