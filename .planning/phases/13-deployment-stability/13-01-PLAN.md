---
phase: 13-deployment-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/next.config.ts
  - personal-assistant/src/app/api/cron/scheduler/route.ts
  - personal-assistant/src/app/api/cron/channel-sync/route.ts
  - personal-assistant/src/app/api/cron/triage/route.ts
  - personal-assistant/src/app/api/cron/sentry/route.ts
  - personal-assistant/src/app/api/cron/morning-briefing/route.ts
  - personal-assistant/src/app/api/cron/proactive-alerts/route.ts
  - personal-assistant/src/app/api/cron/daily-digest/route.ts
  - personal-assistant/src/app/api/cron/weekly-report/route.ts
  - personal-assistant/src/app/api/cron/monthly-report/route.ts
  - personal-assistant/src/lib/cron/cron-guard.ts
  - personal-assistant/vercel.json
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "Vercel production build completes with zero errors"
    - "All 9 cron endpoints return 200 when called with correct authorization"
    - "Cron endpoints reject unauthorized requests with 401"
    - "Each cron endpoint logs execution result for observability"
  artifacts:
    - path: "personal-assistant/src/lib/cron/cron-guard.ts"
      provides: "Shared cron authorization and execution wrapper"
      exports: ["withCronGuard"]
    - path: "personal-assistant/next.config.ts"
      provides: "Clean production build config"
      contains: "nextConfig"
    - path: "personal-assistant/vercel.json"
      provides: "All 9 cron schedules registered"
      contains: "crons"
  key_links:
    - from: "personal-assistant/src/app/api/cron/*/route.ts"
      to: "personal-assistant/src/lib/cron/cron-guard.ts"
      via: "import withCronGuard"
      pattern: "withCronGuard"
    - from: "personal-assistant/vercel.json"
      to: "personal-assistant/src/app/api/cron/*/route.ts"
      via: "cron path registration"
      pattern: "/api/cron/"
---

<objective>
Harden the Vercel production build and all 9 cron endpoints for reliable deployment.

Purpose: The build must deploy cleanly (DEPLOY-01) and all cron jobs must fire reliably with proper auth, error handling, and logging (DEPLOY-02). This is the foundation for everything else in v1.2.

Output: Clean build config, shared cron guard utility, all 9 cron routes hardened with consistent auth/error/logging patterns.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@personal-assistant/next.config.ts
@personal-assistant/vercel.json
@personal-assistant/src/app/api/cron/scheduler/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Vercel build and create shared cron guard</name>
  <files>
    personal-assistant/next.config.ts
    personal-assistant/src/lib/cron/cron-guard.ts
  </files>
  <action>
1. Review `personal-assistant/next.config.ts`. It currently has `ignoreBuildErrors: true` — this masks real issues. Audit the codebase for actual TS errors by running `cd personal-assistant && npx tsc --noEmit 2>&1 | head -100`. Fix any genuine errors found. If monorepo type mismatches from workspace packages are the only errors (agent packages aliased to false), keep ignoreBuildErrors but add a comment explaining why. If there are real app errors, fix them and remove ignoreBuildErrors.

2. Create `personal-assistant/src/lib/cron/cron-guard.ts` — a shared wrapper for all cron routes:
   - Export `withCronGuard(handler: (supabase: SupabaseClient) => Promise<CronResult>) => NextResponse`
   - Validates `Authorization: Bearer {CRON_SECRET}` header (returns 401 if missing/wrong when CRON_SECRET is set)
   - Creates service-role Supabase client (using `createClient` from `@supabase/supabase-js` directly with `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` — NOT the cookie-based server client, since cron routes have no user session)
   - Wraps handler in try/catch, logs start time, duration, success/failure
   - Returns JSON `{ success, duration_ms, result?, error? }` with appropriate status code
   - Sets `maxDuration = 300` and `dynamic = 'force-dynamic'` as exported constants
   - Type: `CronResult = { message: string; details?: Record<string, unknown> }`
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>Verify cron-guard.ts exports withCronGuard function</manual>
  </verify>
  <done>cron-guard.ts exists with withCronGuard export. Build config reviewed and production-ready.</done>
</task>

<task type="auto">
  <name>Task 2: Harden all 9 cron routes with cron guard</name>
  <files>
    personal-assistant/src/app/api/cron/scheduler/route.ts
    personal-assistant/src/app/api/cron/channel-sync/route.ts
    personal-assistant/src/app/api/cron/triage/route.ts
    personal-assistant/src/app/api/cron/sentry/route.ts
    personal-assistant/src/app/api/cron/morning-briefing/route.ts
    personal-assistant/src/app/api/cron/proactive-alerts/route.ts
    personal-assistant/src/app/api/cron/daily-digest/route.ts
    personal-assistant/src/app/api/cron/weekly-report/route.ts
    personal-assistant/src/app/api/cron/monthly-report/route.ts
  </files>
  <action>
Refactor all 9 cron route files to use the shared `withCronGuard` wrapper from `@/lib/cron/cron-guard`.

For each route file:
1. Remove inline auth checks (the guard handles this)
2. Remove inline try/catch (the guard handles this)
3. Remove inline Supabase client creation (the guard provides it)
4. Keep the business logic as the handler function passed to withCronGuard
5. Export `maxDuration` and `dynamic` from the guard's constants

Pattern for each route:
```typescript
import { withCronGuard, cronMaxDuration, cronDynamic } from '@/lib/cron/cron-guard'
import { businessLogicFn } from '@/lib/...'

export const maxDuration = cronMaxDuration
export const dynamic = cronDynamic

export async function GET(request: Request) {
  return withCronGuard(request, async (supabase) => {
    const result = await businessLogicFn(supabase)
    return { message: 'Description of what ran', details: { ...result } }
  })
}
```

Read each route to understand its current business logic before refactoring. Preserve all functional behavior — only change the boilerplate wrapper.

Verify vercel.json has all 9 cron paths registered (it already does — confirm no drift).
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>Grep all cron routes for withCronGuard import to confirm consistency</manual>
  </verify>
  <done>All 9 cron routes use withCronGuard. Each returns structured JSON with success/duration. Unauthorized requests return 401. vercel.json has all 9 paths.</done>
</task>

</tasks>

<verification>
1. `cd personal-assistant && npx tsc --noEmit` completes (with or without ignoreBuildErrors depending on monorepo situation)
2. All 9 cron routes import from `@/lib/cron/cron-guard`
3. `grep -r "withCronGuard" personal-assistant/src/app/api/cron/` shows 9 matches
4. vercel.json contains 9 cron entries
</verification>

<success_criteria>
- Vercel build configuration is production-ready
- All 9 cron endpoints have consistent auth, error handling, and logging via shared guard
- No inline auth/try-catch duplication across cron routes
</success_criteria>

<output>
After completion, create `.planning/phases/13-deployment-stability/13-01-SUMMARY.md`
</output>
