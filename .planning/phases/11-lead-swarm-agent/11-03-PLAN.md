---
phase: 11-lead-swarm-agent
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - personal-assistant/src/app/api/agent/leads/route.ts
  - personal-assistant/src/app/api/agent/leads/[leadId]/route.ts
  - personal-assistant/src/components/leads/leads-kanban.tsx
  - personal-assistant/src/components/dashboard/tabs/leads-tab.tsx
  - personal-assistant/src/components/dashboard/spa-shell.tsx
  - personal-assistant/src/components/dashboard/sidebar-nav.tsx
  - personal-assistant/src/app/dashboard/leads/page.tsx
autonomous: true
requirements: [LEAD-05]

must_haves:
  truths:
    - "Dashboard shows lead pipeline columns New, Qualified, Booked, Won/Lost"
    - "Leads can move between stages and persist after refresh"
    - "Lead board data is org-scoped and reflects qualification output from Lead Swarm"
  artifacts:
    - path: "personal-assistant/src/components/leads/leads-kanban.tsx"
      provides: "Lead pipeline kanban UI with stage columns and move actions"
    - path: "personal-assistant/src/app/api/agent/leads/route.ts"
      provides: "Lead list API for kanban data hydration"
      exports: ["GET"]
    - path: "personal-assistant/src/app/api/agent/leads/[leadId]/route.ts"
      provides: "Lead stage update API for board interactions"
      exports: ["PATCH"]
  key_links:
    - from: "personal-assistant/src/components/leads/leads-kanban.tsx"
      to: "personal-assistant/src/app/api/agent/leads/route.ts"
      via: "initial board fetch and refresh"
      pattern: "fetch\('/api/agent/leads'\)"
    - from: "personal-assistant/src/components/leads/leads-kanban.tsx"
      to: "personal-assistant/src/app/api/agent/leads/[leadId]/route.ts"
      via: "stage transition persistence"
      pattern: "PATCH"
---

<objective>
Deliver the leads pipeline kanban UI and API so Andy can track and move leads through the full lifecycle.

Purpose: Phase 11 value is not complete until qualified leads are visible and actionable in dashboard operations; this plan follows existing dashboard tab and watch-manager interaction patterns from phases 9-10.

Output: Org-scoped lead pipeline APIs, kanban component, and dashboard route/tab wiring for New/Qualified/Booked/Won-Lost stages.
</objective>

<execution_context>
@/home/claude/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/claude/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/comprehensive-roadmap.md
@.planning/phases/09-approval-flow/09-02-SUMMARY.md
@.planning/phases/10-sentry-agent/10-04-SUMMARY.md
@.planning/phases/11-lead-swarm-agent/11-01-SUMMARY.md
@personal-assistant/src/components/dashboard/spa-shell.tsx
@personal-assistant/src/components/dashboard/sidebar-nav.tsx
@personal-assistant/src/components/sentry/watch-manager.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build org-scoped leads API for kanban hydration and stage updates</name>
  <files>
    personal-assistant/src/app/api/agent/leads/route.ts
    personal-assistant/src/app/api/agent/leads/[leadId]/route.ts
  </files>
  <action>
    Implement authenticated lead APIs using the shared auth-context pattern from approvals/sentry endpoints.

    In `GET /api/agent/leads`:
    - Resolve `org_id` from authenticated profile.
    - Return lead rows sorted by recency with optional status filter support for board segment hydration.
    - Include fields needed for board cards (status, score, notes, estimated_value, timeline_days, service_interest, source metadata).

    In `PATCH /api/agent/leads/[leadId]`:
    - Allow stage updates mapped to board columns (`new`, `qualified`, `booked`, `converted`, `lost`).
    - Keep org scoping and return 404 for cross-org/missing leads.
    - Validate updates so only allowed stage values are accepted.
  </action>
  <verify>Run `cd personal-assistant && npm run lint -- src/app/api/agent/leads/route.ts src/app/api/agent/leads/[leadId]/route.ts`.</verify>
  <done>Leads board has authenticated API support to load and persist pipeline stage changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Leads Kanban UI and dashboard tab integration</name>
  <files>
    personal-assistant/src/components/leads/leads-kanban.tsx
    personal-assistant/src/components/dashboard/tabs/leads-tab.tsx
    personal-assistant/src/components/dashboard/spa-shell.tsx
    personal-assistant/src/components/dashboard/sidebar-nav.tsx
    personal-assistant/src/app/dashboard/leads/page.tsx
  </files>
  <action>
    Build the pipeline interface and wire it into existing SPA navigation conventions.

    Create `leads-kanban.tsx` with columns:
    - New
    - Qualified
    - Booked
    - Won/Lost (render converted + lost in one final lane with badge distinction)

    UI behavior:
    - Fetch `/api/agent/leads` on mount and refresh after stage mutation.
    - Support stage transitions via explicit actions (or drag/drop if reusing existing dnd patterns) that call PATCH endpoint.
    - Show score badges (hot/warm/cold), estimated value, and timeline snapshot per card.
    - Include inline success/error feedback consistent with sentry watch manager UX.

    Integrate through:
    - New `LeadsTab` component.
    - `spa-shell.tsx` tab registry/lazy import.
    - `sidebar-nav.tsx` icon map + ordering.
    - `/dashboard/leads` route for direct URL access.
  </action>
  <verify>Run `cd personal-assistant && npx tsc --noEmit -p tsconfig.json 2>&1 | rg "(components/leads/leads-kanban|tabs/leads-tab|app/dashboard/leads/page|app/api/agent/leads)" || true`.</verify>
  <done>Andy can open the Leads tab and manage pipeline stages across New, Qualified, Booked, and Won/Lost.</done>
</task>

</tasks>

<verification>
- Leads APIs enforce auth + org scoping and persist stage transitions.
- Leads tab appears in SPA navigation and direct `/dashboard/leads` route.
- Kanban shows New/Qualified/Booked/Won-Lost lanes with lead cards and score/value context.
- Type-check/lint checks for new lead API/UI files pass.
</verification>

<success_criteria>
- LEAD-05 satisfied: dashboard provides working lead pipeline kanban with required stages.
- Stage changes persist and reload from API state.
</success_criteria>

<output>
After completion, create `.planning/phases/11-lead-swarm-agent/11-03-SUMMARY.md`
</output>
