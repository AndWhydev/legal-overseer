---
phase: 14-channel-relay-oauth
plan: 04
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - personal-assistant/src/lib/channels/dedup.ts
  - personal-assistant/src/lib/channels/synthesizer.ts
  - personal-assistant/src/lib/channels/relay-daemon.ts
  - personal-assistant/src/app/api/channels/relay/route.ts
  - personal-assistant/src/lib/channels/whatsapp-monitor.ts
autonomous: true
requirements:
  - CHAN-04
  - CHAN-05

must_haves:
  truths:
    - "Message deduplication uses fast external_id check then content-hash fallback within 5-minute window"
    - "Classification pipeline handles burst of 50 messages with queue-and-process-sequentially behavior"
    - "Poll-to-classification latency is measured and logged for each relay cycle"
    - "WhatsApp session monitoring checks and logs connection stability"
  artifacts:
    - path: "personal-assistant/src/lib/channels/dedup.ts"
      provides: "Cross-channel deduplication service"
      exports: ["isDuplicate", "computeContentHash"]
    - path: "personal-assistant/src/lib/channels/whatsapp-monitor.ts"
      provides: "WhatsApp session health checker"
      exports: ["checkWhatsAppSession", "logSessionHealth"]
    - path: "personal-assistant/src/lib/channels/relay-daemon.ts"
      provides: "Relay daemon with latency instrumentation"
      contains: "latency"
  key_links:
    - from: "personal-assistant/src/lib/channels/dedup.ts"
      to: "personal-assistant/src/lib/channels/relay-daemon.ts"
      via: "isDuplicate called before message insertion"
      pattern: "isDuplicate"
    - from: "personal-assistant/src/lib/channels/relay-daemon.ts"
      to: "personal-assistant/src/lib/channels/synthesizer.ts"
      via: "messages flow to classification after dedup"
      pattern: "classifyMessage|synthesize"
---

<objective>
Implement robust message deduplication (ID + content-hash), burst handling with sequential queue processing, latency instrumentation, and WhatsApp session monitoring.

Purpose: Production reliability -- dedup prevents duplicate task creation, burst handling prevents message loss, latency measurement validates SLA compliance, WhatsApp monitoring provides session health baseline.
Output: Production-grade dedup service, instrumented relay pipeline, WhatsApp session checker.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@personal-assistant/src/lib/channels/relay-daemon.ts
@personal-assistant/src/lib/channels/synthesizer.ts
@personal-assistant/src/app/api/channels/relay/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-channel dedup service and wire into relay</name>
  <files>
    personal-assistant/src/lib/channels/dedup.ts
    personal-assistant/src/lib/channels/relay-daemon.ts
  </files>
  <action>
1. Create `dedup.ts`:
   - `computeContentHash(sender: string, subject: string | undefined, body: string): string` -- SHA-256 of `${sender.toLowerCase().trim()}:${(subject || '').toLowerCase().trim()}:${body.slice(0, 200).toLowerCase().trim()}`. Use `crypto.createHash('sha256')`.
   - `isDuplicate(supabase: SupabaseClient, orgId: string, msg: ChannelMessage): Promise<{ duplicate: boolean; matchType?: 'external_id' | 'content_hash' }>`:
     - First check: query channel_messages for matching `external_id` + `org_id` + `channel`. If found, return `{ duplicate: true, matchType: 'external_id' }`.
     - Second check (cross-channel): compute content_hash, query channel_messages for matching `content_hash` + `org_id` where `received_at` is within 5 minutes of msg.receivedAt AND `channel != msg.channel`. If found, return `{ duplicate: true, matchType: 'content_hash' }`.
     - Otherwise return `{ duplicate: false }`.

2. Update `relay-daemon.ts` `pollChannel` function:
   - Import and use `isDuplicate` before each message insertion. Skip insertion if duplicate.
   - Track dedup stats: `{ externalIdDupes: number, contentHashDupes: number }` and include in PollResult.
   - Add latency instrumentation: record `Date.now()` at poll start and after each phase (pull, dedup, insert, classification). Log a structured JSON line: `{ channel, pollStartMs, pullDurationMs, dedupDurationMs, insertDurationMs, totalDurationMs, messagesFound, messagesInserted, duplicatesSkipped }`.
   - Add burst handling per user decision: if messages.length > 20, process sequentially (already does this in the for loop), but add a log warning: `[relay] Burst detected: ${messages.length} messages from ${channelType}`. All messages are still processed -- no skipping.

3. Update the PollResult interface to include latency and dedup stats:
   ```
   export interface PollResult {
     messagesFound: number
     messagesInserted: number
     skipped: boolean
     error?: string
     latencyMs?: number
     dedupStats?: { externalId: number; contentHash: number }
   }
   ```
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify dedup.ts exports isDuplicate and computeContentHash, relay-daemon logs latency</manual>
  </verify>
  <done>Cross-channel dedup uses external_id fast check then content-hash within 5-min window. Relay daemon logs structured latency metrics per poll cycle. Burst conditions detected and logged but all messages processed.</done>
</task>

<task type="auto">
  <name>Task 2: Add WhatsApp session monitoring and relay route instrumentation</name>
  <files>
    personal-assistant/src/app/api/channels/relay/route.ts
    personal-assistant/src/lib/channels/whatsapp-monitor.ts
  </files>
  <action>
1. Create `whatsapp-monitor.ts`:
   - `checkWhatsAppSession(supabase: SupabaseClient, orgId: string): Promise<{ connected: boolean; sessionAge: number | null; lastActivity: string | null; error?: string }>`:
     - Query `whatsapp_sessions` for this org_id with status='connected'.
     - If found, compute session age in hours from created_at.
     - Check `whatsapp_outbox` for last sent message timestamp as lastActivity.
     - Return status object.
   - `logSessionHealth(supabase: SupabaseClient, orgId: string, status: { connected: boolean; sessionAge: number | null }): Promise<void>`:
     - Insert a row into a `channel_health_log` (or use existing health infrastructure from `channels/health.ts` -- check for storeHealthReports). Log session uptime, age, connection stability.
   - This provides the monitoring baseline for WhatsApp stability. Actual Baileys bridge is Phase 15.

2. Update `/api/channels/relay/route.ts`:
   - Add latency headers to response: `X-Relay-Duration-Ms`, `X-Messages-Processed`, `X-Duplicates-Skipped`.
   - Include per-channel PollResult with latency stats in the JSON response.
   - Add a WhatsApp session health check call if WhatsApp is configured for this org. Include status in response.
   - Ensure the route handles burst gracefully: if total messages across all channels > 50, log a burst alert but still process all (per user decision: "classify all messages, queue if overwhelmed and process sequentially -- no messages skipped").
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify relay route includes latency headers and WhatsApp monitor checks session health</manual>
  </verify>
  <done>WhatsApp session monitoring tracks connection age and activity. Relay route returns latency headers and per-channel stats. Burst handling processes all messages sequentially with alerting. Poll-to-classification latency measured end-to-end.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (or only pre-existing errors)
- dedup.ts provides two-tier dedup: external_id then content_hash within 5-min window
- Relay daemon logs structured latency JSON for each poll cycle
- WhatsApp monitor checks session health from whatsapp_sessions table
- Relay route includes X-Relay-Duration-Ms header
</verification>

<success_criteria>
Message deduplication handles both same-channel (external_id) and cross-channel (content_hash within 5-min window) duplicates. Burst of 50+ messages processed sequentially without loss. Poll-to-classification latency measured and logged. WhatsApp session monitoring in place for stability tracking.
</success_criteria>

<output>
After completion, create `.planning/phases/14-channel-relay-oauth/14-04-SUMMARY.md`
</output>
