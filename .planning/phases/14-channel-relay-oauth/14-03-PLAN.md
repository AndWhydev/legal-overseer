---
phase: 14-channel-relay-oauth
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - personal-assistant/src/components/channels/channel-card.tsx
  - personal-assistant/src/components/channels/channel-grid.tsx
  - personal-assistant/src/components/channels/channel-config-drawer.tsx
  - personal-assistant/src/components/channels/connect-modal.tsx
autonomous: true
requirements:
  - OAUTH-07
  - OAUTH-03
  - OAUTH-06

must_haves:
  truths:
    - "All 6 channels are visible as cards in a responsive grid (2-3 columns)"
    - "Connected cards show green status dot, last sync time, message count, and disconnect button"
    - "Unconnected cards show a Connect button that triggers OAuth flow or API key modal"
    - "Clicking a connected card opens a slide-out drawer with channel config"
    - "WhatsApp card shows QR code pairing placeholder (bridge is Phase 15)"
    - "Stripe card shows API key input modal instead of OAuth"
  artifacts:
    - path: "personal-assistant/src/components/channels/channel-card.tsx"
      provides: "Channel card with status, sync time, connect/disconnect"
      min_lines: 60
    - path: "personal-assistant/src/components/channels/channel-config-drawer.tsx"
      provides: "Slide-out drawer for connected channel configuration"
      min_lines: 40
    - path: "personal-assistant/src/components/channels/connect-modal.tsx"
      provides: "Modal for API key input (Stripe) and WhatsApp QR pairing"
      min_lines: 30
  key_links:
    - from: "personal-assistant/src/components/channels/channel-card.tsx"
      to: "/api/channels/connect"
      via: "fetch POST on Connect click"
      pattern: "api/channels/connect"
    - from: "personal-assistant/src/components/channels/channel-card.tsx"
      to: "/api/channels/disconnect"
      via: "fetch POST on Disconnect click"
      pattern: "api/channels/disconnect"
    - from: "personal-assistant/src/components/channels/channel-grid.tsx"
      to: "/api/channels/status"
      via: "fetch GET on mount for connection statuses"
      pattern: "api/channels/status"
---

<objective>
Build the channel settings UI with cards grid, connect/disconnect flows, and config drawer. Per user decisions: cards layout, in-app modal/popup for OAuth, green badge on connect, toast on error, slide-out drawer for config.

Purpose: Users need a visual way to connect, monitor, and configure all 6 channels from one place.
Output: Fully interactive channel settings page with connect/disconnect/config UI.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-channel-relay-oauth/14-CONTEXT.md
@personal-assistant/src/components/channels/channel-grid.tsx
@personal-assistant/src/app/dashboard/channels/page.tsx
@personal-assistant/src/lib/integrations/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild channel cards with connection management</name>
  <files>
    personal-assistant/src/components/channels/channel-card.tsx
    personal-assistant/src/components/channels/channel-grid.tsx
    personal-assistant/src/components/channels/connect-modal.tsx
  </files>
  <action>
1. Rewrite `channel-card.tsx` to match user decisions:
   - Card shows: channel icon (use lucide-react icons), channel name, description, status dot (green=connected, yellow=syncing/error, grey=disconnected), last sync time (relative: "2 min ago"), message count badge.
   - Connected state: show green dot, last sync, message count processed, "Disconnect" button (subtle, text-only). If status is 'error', show amber dot and "Needs attention" badge per user decision.
   - Disconnected state: show "Connect" button (primary style).
   - On Connect click:
     - For OAuth channels (gmail, outlook, asana, calendly): POST to `/api/channels/connect` with `{ channel }`. Response contains `{ redirect: true, url }`. Open url in a popup window (600x700) using `window.open()` -- per user decision, stay on settings page. Listen for popup close, then re-fetch status.
     - For API key channels (stripe): open ConnectModal with API key input.
     - For WhatsApp: open ConnectModal with QR code placeholder and instructions.
   - On Disconnect click: confirm dialog, POST to `/api/channels/disconnect` with `{ channel }`, re-fetch status.
   - Click on connected card body (not buttons) opens config drawer.
   - On success connect: animate card to connected state with green badge (use CSS transition, per user decision).
   - On error: show toast notification (use existing toast pattern from channel-grid.tsx).

2. Update `channel-grid.tsx`:
   - Fetch from `/api/channels/status` on mount (already does this).
   - Define the 6 target channels with their metadata: Gmail, Outlook, WhatsApp, Asana, Calendly, Stripe. Always show all 6 cards regardless of adapter availability.
   - Merge fetched status into the static 6-channel list.
   - Responsive grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` (already correct).
   - Handle `?connected=PROVIDER` query param (from OAuth callback redirect) to show success toast.
   - Handle `?error=MESSAGE` query param to show error toast.

3. Create `connect-modal.tsx`:
   - A modal dialog (using shadcn Dialog or Sheet) with two modes:
     - `api_key` mode: Input field for API secret key, "Connect" submit button. On submit, POST to `/api/channels/connect` with `{ channel: 'stripe', credentials: { secret_key } }`.
     - `whatsapp_qr` mode: Show placeholder area for QR code image, instructions text ("Open WhatsApp on your phone > Settings > Linked Devices > Link a Device > Scan this QR code"), connection status indicator. POST to `/api/channels/connect` with `{ channel: 'whatsapp' }` to initiate pairing session. Note: actual QR generation is a Phase 15 concern with the Baileys bridge; this plan creates the UI shell.
   - On success in both modes: close modal, show success toast, trigger re-fetch.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Check that channel-card.tsx handles all 3 connect paths (OAuth popup, API key modal, WhatsApp QR modal)</manual>
  </verify>
  <done>All 6 channel cards render with correct connect/disconnect flows. OAuth channels open popup window. Stripe opens API key modal. WhatsApp opens QR pairing modal shell. Connected cards show green dot, sync time, message count. Error cards show amber "Needs attention" badge.</done>
</task>

<task type="auto">
  <name>Task 2: Create channel config drawer</name>
  <files>
    personal-assistant/src/components/channels/channel-config-drawer.tsx
  </files>
  <action>
1. Create `channel-config-drawer.tsx` as a slide-out drawer (use shadcn Sheet component, side="right"):
   - Props: `channel: string, orgId: string, isOpen: boolean, onClose: () => void`.
   - On open, fetch config from `GET /api/channels/${channel}/config`.
   - Display fields based on channel type:
     - All channels: Sync frequency selector (1min, 2min, 5min, 15min, 30min, 1hr), relay_enabled toggle, last sync timestamp.
     - Gmail/Outlook: folder/label filter options (text input for comma-separated labels).
     - Asana: workspace selector (if multiple workspaces available).
     - Calendly: event type filter.
     - Stripe: event types filter (checkboxes: payment_intent.succeeded, invoice.paid, etc.).
     - WhatsApp: session status display, reconnect button.
   - Save button: PATCH to `/api/channels/${channel}/config` with updated config.
   - Disconnect button at bottom of drawer (red, with confirmation).
   - Per user decision: exact layout and field arrangement at Claude's discretion.

2. Wire the drawer into channel-grid.tsx:
   - Add state for selectedChannel and drawerOpen.
   - Pass onClick handler to ChannelCard that opens drawer for connected channels.
   - Render ChannelConfigDrawer conditionally when drawerOpen is true.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify drawer renders with channel-specific fields and save/disconnect actions</manual>
  </verify>
  <done>Channel config drawer slides out from right with channel-specific settings. Users can adjust sync frequency, toggle relay, filter messages, and disconnect. Drawer wired into channel grid via card click.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (or only pre-existing errors)
- All 6 channel cards visible in grid layout
- OAuth channels trigger popup window on Connect
- Stripe shows API key input modal
- WhatsApp shows QR pairing modal shell
- Connected cards open config drawer on click
- Disconnect preserves messages
</verification>

<success_criteria>
Channel settings page shows all 6 channels as interactive cards. Users can connect via OAuth popup (Gmail, Outlook, Asana, Calendly), API key modal (Stripe), or QR pairing shell (WhatsApp). Connected channels show live status and open config drawer. Error/success states shown via toasts and status badges.
</success_criteria>

<output>
After completion, create `.planning/phases/14-channel-relay-oauth/14-03-SUMMARY.md`
</output>
