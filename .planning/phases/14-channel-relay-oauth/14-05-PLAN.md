---
phase: 14-channel-relay-oauth
plan: 05
type: execute
wave: 3
depends_on:
  - 14-03
  - 14-04
files_modified:
  - personal-assistant/src/app/dashboard/channels/page.tsx
autonomous: false
requirements:
  - OAUTH-07
  - CHAN-05

user_setup:
  - service: google-oauth
    why: "Gmail OAuth for live channel connection"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client ID"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client Secret"
    dashboard_config:
      - task: "Add OAuth redirect URI for production"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client -> Authorized redirect URIs -> Add https://YOUR_DOMAIN/callback/gmail"
  - service: microsoft-oauth
    why: "Outlook OAuth for live channel connection"
    env_vars:
      - name: OUTLOOK_CLIENT_ID
        source: "Azure Portal -> App registrations -> Application (client) ID"
      - name: OUTLOOK_CLIENT_SECRET
        source: "Azure Portal -> App registrations -> Certificates & secrets -> New client secret"
    dashboard_config:
      - task: "Add OAuth redirect URI for production"
        location: "Azure Portal -> App registrations -> Authentication -> Add platform -> Web -> Redirect URI: https://YOUR_DOMAIN/callback/outlook"

must_haves:
  truths:
    - "Channel settings page renders all 6 channels with correct UI"
    - "Connect button triggers the appropriate flow for each channel type"
    - "Connected channels show live status and config drawer works"
    - "Disconnect preserves historical messages"
    - "Token auto-refresh cron is scheduled and responds correctly"
    - "Relay route returns dedup headers under burst conditions"
  artifacts:
    - path: "personal-assistant/src/app/dashboard/channels/page.tsx"
      provides: "Channel settings page"
  key_links:
    - from: "personal-assistant/src/components/channels/channel-grid.tsx"
      to: "/api/channels/status"
      via: "fetches real connection status on mount"
      pattern: "api/channels/status"
    - from: "personal-assistant/src/app/api/cron/token-refresh/route.ts"
      to: "personal-assistant/src/lib/channels/token-refresh.ts"
      via: "cron calls refreshAllTokens"
      pattern: "refreshAllTokens"
---

<objective>
Integration verification, environment provisioning checkpoint, and visual verification of the complete channel settings page. Ensures all Phase 14 components work together end-to-end including token refresh cron and dedup pipeline.

Purpose: Human verification that the UI matches user decisions and that deployed environment has the required OAuth credentials and redirect URIs configured.
Output: Verified channel settings UI, confirmed environment readiness.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-channel-relay-oauth/14-CONTEXT.md
@.planning/phases/14-channel-relay-oauth/14-03-SUMMARY.md
@.planning/phases/14-channel-relay-oauth/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final integration and build verification</name>
  <files>
    personal-assistant/src/app/dashboard/channels/page.tsx
  </files>
  <action>
1. Ensure the channels page imports and renders the updated ChannelGrid component (should already be correct from existing code).

2. Run the full build to verify no import or type errors across all plan changes:
   - `cd personal-assistant && npx next build` (or at minimum `npx tsc --noEmit`).
   - Fix any compilation errors from plans 01-04.

3. Verify the vercel.json or next.config includes the cron schedule for `/api/cron/token-refresh` (add if missing):
   - Check existing cron configuration pattern from Phase 13.
   - Add token-refresh cron: path `/api/cron/token-refresh`, schedule `0 * * * *` (hourly).

4. Create a latency documentation section. Add a comment block at the top of `relay-daemon.ts` documenting the measured latency expectations:
   - Channel Relay Latency Budget (CHAN-05):
   - Poll initiation: less than 100ms
   - IMAP/API pull: 500ms-5s (depends on message count and provider)
   - Dedup check: less than 50ms per message (DB query)
   - Insert: less than 20ms per message (upsert)
   - Classification routing: less than 100ms (queue, not inline)
   - Total per-cycle target: less than 10s for up to 50 messages
   - Burst conditions (more than 20 messages): sequential processing, no skip
   - Cross-channel dedup window: 5 minutes
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify build passes and cron schedule includes token-refresh</manual>
  </verify>
  <done>Build passes with all Phase 14 changes. Cron schedule includes hourly token refresh. Latency budget documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Environment provisioning and visual verification</name>
  <action>Human provisions OAuth credentials in Vercel environment, registers redirect URIs in Google/Microsoft dashboards, and verifies the complete channel settings UI.</action>
  <what-built>Complete channel settings page with 6 channel cards (Gmail, Outlook, WhatsApp, Asana, Calendly, Stripe), OAuth connect flows, API key modal, WhatsApp QR shell, config drawer, token refresh cron, and dedup pipeline.</what-built>
  <how-to-verify>
    **Environment Setup (required for CHAN-01/CHAN-02 live verification):**
    1. In Vercel dashboard, add env vars: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, OUTLOOK_CLIENT_ID, OUTLOOK_CLIENT_SECRET
    2. In Google Cloud Console -> APIs & Services -> Credentials -> OAuth client: add redirect URI `https://YOUR_DOMAIN/callback/gmail`
    3. In Azure Portal -> App registrations -> Authentication: add redirect URI `https://YOUR_DOMAIN/callback/outlook`
    4. Deploy with `vercel --prod` (or push to trigger deploy)

    **UI Verification:**
    5. Navigate to /dashboard/channels -- verify all 6 channel cards are visible in a responsive grid
    6. Each disconnected card should show a "Connect" button
    7. Click Connect on Gmail -- should open OAuth popup, complete flow, card animates to connected with green badge
    8. Click Connect on Stripe -- should show API key input modal
    9. Click Connect on WhatsApp -- should show QR code pairing modal shell
    10. Verify connected Gmail card shows last sync time and message count after first relay cycle

    **Token Refresh Verification:**
    11. Call `curl -H "Authorization: Bearer CRON_SECRET" https://YOUR_DOMAIN/api/cron/token-refresh` -- should return JSON with refresh results (gmail token refreshed or "not expiring")

    **Dedup Verification:**
    12. Call `curl https://YOUR_DOMAIN/api/channels/relay` -- response should include `X-Relay-Duration-Ms` and `X-Duplicates-Skipped` headers

    13. Check that /api/channels/status returns all 6 channels with their connection state
  </how-to-verify>
  <verify>Human confirms UI matches requirements and live OAuth round-trip succeeds</verify>
  <done>All 6 channel cards render correctly. Gmail/Outlook OAuth completes live round-trip in deployed environment. Token refresh cron responds. Relay route returns dedup/latency headers.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full build passes
- All 6 channels render as cards
- Connect flows work for each channel type (OAuth popup, API key modal, QR shell)
- Token refresh cron scheduled hourly and responds to manual trigger
- Relay route returns X-Relay-Duration-Ms and X-Duplicates-Skipped headers
- Live Gmail OAuth round-trip succeeds in deployed environment
- Latency budget documented
</verification>

<success_criteria>
Phase 14 complete: All 6 channels connectable from settings via appropriate flows (OAuth/API key/QR), live status display, dedup holds under burst, token refresh automated and verified, latency measured and documented, live OAuth verified in production.
</success_criteria>

<output>
After completion, create `.planning/phases/14-channel-relay-oauth/14-05-SUMMARY.md`
</output>
