---
phase: 14-channel-relay-oauth
plan: 05
type: execute
wave: 3
depends_on:
  - 14-03
  - 14-04
files_modified:
  - personal-assistant/src/app/dashboard/channels/page.tsx
autonomous: false
requirements:
  - OAUTH-07
  - CHAN-05

must_haves:
  truths:
    - "Channel settings page renders all 6 channels with correct UI"
    - "Connect button triggers the appropriate flow for each channel type"
    - "Connected channels show live status and config drawer works"
    - "Disconnect preserves historical messages"
  artifacts:
    - path: "personal-assistant/src/app/dashboard/channels/page.tsx"
      provides: "Channel settings page"
  key_links:
    - from: "personal-assistant/src/components/channels/channel-grid.tsx"
      to: "/api/channels/status"
      via: "fetches real connection status on mount"
      pattern: "api/channels/status"
---

<objective>
Visual verification of the complete channel settings page. All 6 channels should render as cards with correct connect/disconnect flows, status indicators, and config drawer.

Purpose: Human verification that the UI matches user decisions (cards grid, modal OAuth, green badges, toast errors, slide-out drawer).
Output: Verified channel settings UI.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-channel-relay-oauth/14-CONTEXT.md
@.planning/phases/14-channel-relay-oauth/14-03-SUMMARY.md
@.planning/phases/14-channel-relay-oauth/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final integration and build verification</name>
  <files>
    personal-assistant/src/app/dashboard/channels/page.tsx
  </files>
  <action>
1. Ensure the channels page imports and renders the updated ChannelGrid component (should already be correct from existing code).

2. Run the full build to verify no import or type errors across all plan changes:
   - `cd personal-assistant && npx next build` (or at minimum `npx tsc --noEmit`).
   - Fix any compilation errors from plans 01-04.

3. Verify the vercel.json or next.config includes the cron schedule for `/api/cron/token-refresh` (add if missing):
   - Check existing cron configuration pattern from Phase 13.
   - Add token-refresh cron: path `/api/cron/token-refresh`, schedule `0 * * * *` (hourly).

4. Create a latency documentation section. Add a comment block at the top of `relay-daemon.ts` documenting the measured latency expectations:
   - Channel Relay Latency Budget (CHAN-05):
   - Poll initiation: less than 100ms
   - IMAP/API pull: 500ms-5s (depends on message count and provider)
   - Dedup check: less than 50ms per message (DB query)
   - Insert: less than 20ms per message (upsert)
   - Classification routing: less than 100ms (queue, not inline)
   - Total per-cycle target: less than 10s for up to 50 messages
   - Burst conditions (more than 20 messages): sequential processing, no skip
   - Cross-channel dedup window: 5 minutes
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify build passes and cron schedule includes token-refresh</manual>
  </verify>
  <done>Build passes with all Phase 14 changes. Cron schedule includes hourly token refresh. Latency budget documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of channel settings page</name>
  <action>Human verifies the complete channel settings UI matches user decisions from CONTEXT.md.</action>
  <what-built>Complete channel settings page with 6 channel cards (Gmail, Outlook, WhatsApp, Asana, Calendly, Stripe), OAuth connect flows, API key modal, WhatsApp QR shell, config drawer, token refresh cron, and dedup pipeline.</what-built>
  <how-to-verify>
    1. Navigate to /dashboard/channels -- verify all 6 channel cards are visible in a responsive grid
    2. Each disconnected card should show a "Connect" button
    3. Click Connect on Gmail -- should open OAuth popup (will fail without real Google credentials, but popup should open)
    4. Click Connect on Stripe -- should show API key input modal
    5. Click Connect on WhatsApp -- should show QR code pairing modal shell
    6. Verify the build passes: `cd personal-assistant && npx next build`
    7. Check that /api/channels/status returns all 6 channels with their connection state
  </how-to-verify>
  <verify>Human confirms UI matches requirements</verify>
  <done>All 6 channel cards render correctly with appropriate connect flows per channel type.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full build passes
- All 6 channels render as cards
- Connect flows work for each channel type (OAuth popup, API key modal, QR shell)
- Token refresh cron scheduled hourly
- Latency budget documented
</verification>

<success_criteria>
Phase 14 complete: All 6 channels connectable from settings via appropriate flows (OAuth/API key/QR), live status display, dedup holds under burst, token refresh automated, latency measured and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/14-channel-relay-oauth/14-05-SUMMARY.md`
</output>
