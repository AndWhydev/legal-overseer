---
phase: 05-wire-integration-points
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/src/app/api/agent/chat/route.ts
  - personal-assistant/src/lib/agent/engine.ts
autonomous: true
requirements: [AGNT-11, AGNT-12, AGNT-13]

must_haves:
  truths:
    - "loadAllAgents() runs at app startup and populates the in-memory agent registry"
    - "routeAgentAction() is available to gate tool execution with confidence thresholds"
  artifacts:
    - path: "personal-assistant/src/app/api/agent/chat/route.ts"
      provides: "Registry initialization on first request"
      contains: "loadAllAgents"
    - path: "personal-assistant/src/lib/agent/engine.ts"
      provides: "Confidence routing import available for agent tool execution"
      contains: "routeAgentAction"
  key_links:
    - from: "personal-assistant/src/app/api/agent/chat/route.ts"
      to: "personal-assistant/src/lib/agent/registry-loader.ts"
      via: "import loadAllAgents"
      pattern: "loadAllAgents\\(\\)"
    - from: "personal-assistant/src/lib/agent/engine.ts"
      to: "personal-assistant/src/lib/agent/confidence-router.ts"
      via: "import routeAgentAction"
      pattern: "routeAgentAction"
---

<objective>
Wire agent registry startup and confidence routing into the chat pipeline.

Purpose: loadAllAgents() exists but is never called — no agents self-register at startup. routeAgentAction() exists but is not wired into tool execution. This plan connects both so the agent infrastructure activates in production.
Output: Registry loads on first chat request; confidence routing is imported and available in engine.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@personal-assistant/src/app/api/agent/chat/route.ts
@personal-assistant/src/lib/agent/engine.ts
@personal-assistant/src/lib/agent/registry-loader.ts
@personal-assistant/src/lib/agent/confidence-router.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire loadAllAgents into chat route with lazy init</name>
  <files>personal-assistant/src/app/api/agent/chat/route.ts</files>
  <action>
    In route.ts:
    1. Add import: `import { loadAllAgents } from '@/lib/agent/registry-loader'`
    2. Add a module-level initialization flag:
       ```
       let registryInitialized = false
       ```
    3. At the top of the POST handler (after the message check, before auth), add:
       ```
       if (!registryInitialized) {
         loadAllAgents()
         registryInitialized = true
       }
       ```

    This uses lazy initialization on first request — Next.js serverless functions don't have a traditional app startup, so module-level init with a guard flag is the standard pattern. loadAllAgents() is synchronous (dynamic imports are fire-and-forget), so this won't block the request.
  </action>
  <verify>Run `grep -n 'loadAllAgents' personal-assistant/src/app/api/agent/chat/route.ts` — shows import and call. Run `npx tsc --noEmit -p personal-assistant/tsconfig.json 2>&1 | head -20` — no new errors.</verify>
  <done>First chat request triggers loadAllAgents() which auto-discovers and registers all available agent packages</done>
</task>

<task type="auto">
  <name>Task 2: Import confidence routing into engine for tool gating</name>
  <files>personal-assistant/src/lib/agent/engine.ts</files>
  <action>
    In engine.ts:
    1. Add import: `import { routeAgentAction } from './confidence-router'`
    2. Add a comment above the tool execution loop (the `for` loop that handles tool_use blocks) noting where confidence gating will be applied:
       ```
       // TODO(v2): Apply routeAgentAction() here to gate tool execution
       // For now, all tools execute at act level (no autonomous agent runs yet)
       ```

    Full confidence gating (blocking tool calls based on score) requires the agent run framework (AGNT-17/18, v2 scope) — we can't gate individual chat tool calls without an agent orchestrator deciding confidence. The import makes the function available for when agent runs are implemented. The TODO marks the exact integration point.

    Note: Do NOT add routeAgentAction into the tool execution path yet — chat tool calls are user-initiated (implicit act-level confidence). Gating applies to autonomous agent actions, not interactive chat.
  </action>
  <verify>Run `grep -n 'routeAgentAction' personal-assistant/src/lib/agent/engine.ts` — shows import. Run `npx tsc --noEmit -p personal-assistant/tsconfig.json 2>&1 | head -20` — no new errors (import used in comment/TODO is fine, but if TS complains about unused import, add `// eslint-disable-next-line @typescript-eslint/no-unused-vars` above the import or use `import type` approach — actually, just keep the import and add `void routeAgentAction` after the import to prevent unused warnings).</verify>
  <done>routeAgentAction is imported into engine.ts with a clear TODO marking where confidence gating activates for autonomous agent runs</done>
</task>

</tasks>

<verification>
1. `grep 'loadAllAgents' personal-assistant/src/app/api/agent/chat/route.ts` shows import and call
2. `grep 'registryInitialized' personal-assistant/src/app/api/agent/chat/route.ts` shows the guard flag
3. `grep 'routeAgentAction' personal-assistant/src/lib/agent/engine.ts` shows import
4. TypeScript compilation passes with no new errors
</verification>

<success_criteria>
- loadAllAgents() called on first chat request via lazy init pattern
- routeAgentAction imported into engine.ts with clear integration point marked
- TypeScript compilation passes with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-wire-integration-points/05-02-SUMMARY.md`
</output>
