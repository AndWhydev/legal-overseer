---
phase: 05-wire-integration-points
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/src/lib/agent/engine.ts
  - personal-assistant/src/app/api/agent/chat/route.ts
  - personal-assistant/src/lib/context/cross-reference.ts
autonomous: true
requirements: [SCTX-05, SCTX-08, SCTX-09]

must_haves:
  truths:
    - "Chat messages trigger buildEntityAwarePrompt so entity context enriches every response"
    - "cross-reference.ts queries match actual invoices schema columns (total, paid_date)"
  artifacts:
    - path: "personal-assistant/src/lib/agent/engine.ts"
      provides: "Entity-aware prompt wiring"
      contains: "buildEntityAwarePrompt"
    - path: "personal-assistant/src/lib/context/cross-reference.ts"
      provides: "Corrected invoice column references"
      contains: "total"
  key_links:
    - from: "personal-assistant/src/lib/agent/engine.ts"
      to: "personal-assistant/src/lib/agent/prompt-builder.ts"
      via: "import buildEntityAwarePrompt"
      pattern: "buildEntityAwarePrompt\\(config\\.orgId,\\s*message\\)"
    - from: "personal-assistant/src/lib/context/cross-reference.ts"
      to: "invoices table"
      via: "Supabase select"
      pattern: "\\.select\\('id, status, total, paid_date, due_date'\\)"
---

<objective>
Wire entity-aware prompts into the chat engine and fix cross-reference schema mismatch.

Purpose: The entity-aware prompt builder exists but is never called — engine.ts still uses buildSystemPrompt. The cross-reference engine queries non-existent columns (total_amount, paid_at) instead of actual schema columns (total, paid_date).
Output: Chat engine uses entity-enriched prompts; cross-reference queries match real DB schema.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@personal-assistant/src/lib/agent/engine.ts
@personal-assistant/src/lib/agent/prompt-builder.ts
@personal-assistant/src/lib/context/cross-reference.ts
@personal-assistant/supabase/migrations/011_invoices.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire buildEntityAwarePrompt into chat engine</name>
  <files>personal-assistant/src/lib/agent/engine.ts</files>
  <action>
    In engine.ts:
    1. Change import from `import { buildSystemPrompt } from './prompt-builder'` to `import { buildEntityAwarePrompt } from './prompt-builder'`
    2. Change line 46 from `const systemPrompt = await buildSystemPrompt(config.orgId)` to `const systemPrompt = await buildEntityAwarePrompt(config.orgId, message)`

    This wires the entity-aware prompt builder so that every chat message triggers entity resolution and context enrichment. The buildEntityAwarePrompt function already falls back to the base prompt when no entities are detected, so this is a safe swap.
  </action>
  <verify>Run `npx tsc --noEmit -p personal-assistant/tsconfig.json 2>&1 | head -20` — no new errors related to engine.ts or prompt-builder.ts</verify>
  <done>engine.ts imports and calls buildEntityAwarePrompt(orgId, message) instead of buildSystemPrompt(orgId)</done>
</task>

<task type="auto">
  <name>Task 2: Fix cross-reference invoice column names to match schema</name>
  <files>personal-assistant/src/lib/context/cross-reference.ts</files>
  <action>
    In cross-reference.ts getFinancialSignals function:
    1. Line 83: Change `.select('id, status, total_amount, paid_at, due_date')` to `.select('id, status, total, paid_date, due_date')`
    2. Line 96: Change `(inv.total_amount as number)` to `(inv.total as number)`
    3. Lines 101-103: Change all `inv.paid_at` references to `inv.paid_date`

    The actual invoices table schema (011_invoices.sql) uses columns `total` (not total_amount) and `paid_date` (not paid_at). This mismatch would cause Supabase to return null for these columns at runtime.
  </action>
  <verify>Run `grep -n 'total_amount\|paid_at' personal-assistant/src/lib/context/cross-reference.ts` — should return no matches. Run `npx tsc --noEmit -p personal-assistant/tsconfig.json 2>&1 | head -20` — no new errors.</verify>
  <done>cross-reference.ts uses `total` and `paid_date` matching the actual invoices table schema</done>
</task>

</tasks>

<verification>
1. `grep 'buildEntityAwarePrompt' personal-assistant/src/lib/agent/engine.ts` shows import and usage
2. `grep 'buildSystemPrompt' personal-assistant/src/lib/agent/engine.ts` returns no matches
3. `grep -c 'total_amount\|paid_at' personal-assistant/src/lib/context/cross-reference.ts` returns 0
4. `grep 'total, paid_date' personal-assistant/src/lib/context/cross-reference.ts` returns the select line
</verification>

<success_criteria>
- engine.ts calls buildEntityAwarePrompt(orgId, message) for every chat message
- cross-reference.ts queries invoices with correct column names (total, paid_date)
- TypeScript compilation passes with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-wire-integration-points/05-01-SUMMARY.md`
</output>
