---
phase: 32-marketing-site-checkout
plan: 03
type: execute
wave: 2
depends_on: [32-01, 32-02]
files_modified:
  - personal-assistant/src/app/sitemap.ts
  - personal-assistant/src/app/robots.ts
  - personal-assistant/src/app/layout.tsx
  - personal-assistant/src/app/(public)/pricing/page.tsx
  - personal-assistant/src/app/(public)/case-study/page.tsx
  - personal-assistant/src/app/(public)/industries/agencies/page.tsx
  - personal-assistant/src/app/(public)/industries/trades/page.tsx
  - personal-assistant/src/app/(public)/industries/professional-services/page.tsx
autonomous: true
requirements: [MKTG-05]

must_haves:
  truths:
    - "Every marketing page has proper meta title, description, and Open Graph tags"
    - "sitemap.xml includes all new pages (landing, pricing, 3 industry pages, case study)"
    - "robots.txt allows crawling of all public marketing pages"
    - "Structured data (JSON-LD) exists on landing page (Organization, Product) and pricing page (FAQ)"
    - "Open Graph images are configured for social sharing"
  artifacts:
    - path: "personal-assistant/src/app/sitemap.ts"
      provides: "Complete sitemap with all marketing pages"
      contains: "industries/agencies"
    - path: "personal-assistant/src/app/robots.ts"
      provides: "robots.txt allowing all marketing page crawling"
      contains: "/industries"
    - path: "personal-assistant/src/app/layout.tsx"
      provides: "Root layout with enhanced global metadata"
      contains: "openGraph"
  key_links:
    - from: "personal-assistant/src/app/sitemap.ts"
      to: "all marketing page routes"
      via: "URL entries array"
      pattern: "industries|case-study|pricing"
    - from: "personal-assistant/src/app/layout.tsx"
      to: "Open Graph metadata"
      via: "metadata export"
      pattern: "openGraph"
---

<objective>
Add SEO foundation: structured data, comprehensive meta tags, Open Graph, complete sitemap, and robots.txt for all marketing pages.

Purpose: Search engines and social platforms must correctly index and preview all marketing pages. Target keywords: "AI business assistant", "automated invoicing", "lead management AI".
Output: Updated sitemap, robots, layout metadata, and JSON-LD structured data on key pages.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-marketing-site-checkout/32-01-SUMMARY.md
@.planning/phases/32-marketing-site-checkout/32-02-SUMMARY.md

<interfaces>
<!-- Existing sitemap and robots files to update -->

From src/app/sitemap.ts (current):
```typescript
// Returns 7 entries: /, /pricing, /privacy, /terms, /docs, /login, /onboard
// Uses VERCEL_URL env var with fallback to https://bitbit.chat
```

From src/app/robots.ts (current):
```typescript
// Allows: /, /pricing, /privacy, /terms, /docs, /onboard
// Disallows: /dashboard, /api, /admin, /*.json$, /*.json?*
// AdsBot-Google allowed /
// Sitemap: ${baseUrl}/sitemap.xml
```

From src/app/layout.tsx (current metadata):
```typescript
export const metadata: Metadata = {
  title: { default: "BitBit - AI Operations Platform", template: "%s | BitBit" },
  description: "Agentic AI operations platform for digital agencies...",
  keywords: ["AI operations", "digital agency", "automation", ...],
  authors: [{ name: "BitBit" }],
  icons: { icon: [...], apple: [...] },
  // NO openGraph, NO twitter, NO structured data
}
```

From Next.js metadata API:
```typescript
// Page-level metadata exports override layout metadata
// openGraph: { title, description, url, siteName, images, locale, type }
// twitter: { card, title, description, images }
// other: { 'application/ld+json': structured data } — or use generateMetadata
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sitemap, robots, and global metadata with Open Graph</name>
  <files>
    personal-assistant/src/app/sitemap.ts,
    personal-assistant/src/app/robots.ts,
    personal-assistant/src/app/layout.tsx
  </files>
  <action>
    **Sitemap** (`sitemap.ts`):
    - Rewrite to include all marketing pages with proper priorities and changeFrequency:
      ```
      / (landing)           — priority: 1.0, changeFrequency: 'weekly'
      /pricing              — priority: 0.9, changeFrequency: 'monthly'
      /case-study           — priority: 0.8, changeFrequency: 'monthly'
      /industries/agencies  — priority: 0.8, changeFrequency: 'monthly'
      /industries/trades    — priority: 0.8, changeFrequency: 'monthly'
      /industries/professional-services — priority: 0.8, changeFrequency: 'monthly'
      /privacy              — priority: 0.3, changeFrequency: 'yearly'
      /terms                — priority: 0.3, changeFrequency: 'yearly'
      /onboard              — priority: 0.7, changeFrequency: 'never'
      /login                — priority: 0.3, changeFrequency: 'never'
      ```
    - Remove /docs entry (doesn't exist yet)
    - Keep using VERCEL_URL with fallback to 'https://bitbit.chat'

    **Robots** (`robots.ts`):
    - Add all new marketing routes to the allow list:
      ```
      allow: ['/', '/pricing', '/privacy', '/terms', '/onboard', '/case-study', '/industries/agencies', '/industries/trades', '/industries/professional-services']
      ```
    - Keep existing disallow rules
    - Keep AdsBot-Google rule

    **Root layout metadata** (`layout.tsx`):
    - Enhance the existing metadata export (DO NOT change fonts, imports, or component structure — only the metadata const)
    - Add Open Graph:
      ```typescript
      openGraph: {
        title: 'BitBit - AI Operations That Actually Work',
        description: 'Autonomous AI agents that handle invoicing, lead capture, client comms, and tenders. Built for agencies, trades, and professional services.',
        url: 'https://bitbit.chat',
        siteName: 'BitBit',
        locale: 'en_AU',
        type: 'website',
        images: [{
          url: 'https://bitbit.chat/og-image.png',
          width: 1200,
          height: 630,
          alt: 'BitBit - AI Operations Platform',
        }],
      },
      ```
    - Add Twitter card:
      ```typescript
      twitter: {
        card: 'summary_large_image',
        title: 'BitBit - AI Operations That Actually Work',
        description: 'Autonomous AI agents for agencies, trades, and professional services.',
        images: ['https://bitbit.chat/og-image.png'],
      },
      ```
    - Update keywords array to include target SEO terms: "AI business assistant", "automated invoicing", "lead management AI", "AI operations platform", "agency automation", "trades business automation"
    - Add `metadataBase: new URL('https://bitbit.chat')` — this tells Next.js the canonical base URL
    - Add `alternates: { canonical: '/' }` at root level
    - Keep ALL existing metadata fields (title template, description, authors, creator, publisher, icons)
    - Note: og-image.png does not exist yet — create a placeholder reference. The user can generate the actual image later. For now the metadata should reference it so when it exists, sharing will work.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - sitemap.ts returns entries for all 10 marketing pages with proper priorities
    - robots.ts allows crawling of all marketing pages
    - Root layout has Open Graph, Twitter card, updated keywords, and metadataBase
    - Build passes cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Add JSON-LD structured data to key marketing pages</name>
  <files>
    personal-assistant/src/app/page.tsx,
    personal-assistant/src/app/(public)/pricing/page.tsx,
    personal-assistant/src/app/(public)/case-study/page.tsx,
    personal-assistant/src/app/(public)/industries/agencies/page.tsx,
    personal-assistant/src/app/(public)/industries/trades/page.tsx,
    personal-assistant/src/app/(public)/industries/professional-services/page.tsx
  </files>
  <action>
    **Landing page** (`page.tsx`) — add JSON-LD structured data in the page's server component:
    - Organization schema:
      ```json
      {
        "@context": "https://schema.org",
        "@type": "Organization",
        "name": "BitBit",
        "url": "https://bitbit.chat",
        "logo": "https://bitbit.chat/bitbit-app-icon-192.png",
        "description": "AI operations platform for digital agencies, trades, and professional services",
        "sameAs": []
      }
      ```
    - SoftwareApplication schema:
      ```json
      {
        "@context": "https://schema.org",
        "@type": "SoftwareApplication",
        "name": "BitBit",
        "applicationCategory": "BusinessApplication",
        "operatingSystem": "Web",
        "url": "https://bitbit.chat",
        "description": "Autonomous AI agents that handle invoicing, lead capture, client communications, and business operations",
        "offers": {
          "@type": "AggregateOffer",
          "lowPrice": "0",
          "highPrice": "599",
          "priceCurrency": "AUD",
          "offerCount": "4"
        }
      }
      ```
    - Render as `<script type="application/ld+json">` in the page component (Next.js supports this in server components)

    **Pricing page** — add FAQ structured data:
    - Read the FAQ questions/answers from the pricing page (created in plan 32-02)
    - Add FAQPage schema:
      ```json
      {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": [
          { "@type": "Question", "name": "What happens after my free trial?", "acceptedAnswer": { "@type": "Answer", "text": "..." } },
          // ... all FAQ items
        ]
      }
      ```
    - Render as `<script type="application/ld+json">` — this must go in the server component wrapper (page.tsx), not the client component

    **Case study page** — add Article structured data:
      ```json
      {
        "@context": "https://schema.org",
        "@type": "Article",
        "headline": "How All Webbed Up Automated Agency Operations with BitBit",
        "description": "Case study: Brisbane marketing agency saves 10+ hours per week with AI agents",
        "author": { "@type": "Organization", "name": "BitBit" },
        "publisher": { "@type": "Organization", "name": "BitBit" },
        "datePublished": "2026-03-26",
        "url": "https://bitbit.chat/case-study"
      }
      ```

    **Industry pages** — add each page's metadata with industry-specific descriptions:
    - Agencies: Update metadata to include keywords "marketing agency automation", "agency AI operations"
    - Trades: Keywords "trades business automation", "tradie invoicing AI", "job site automation"
    - Professional Services: Keywords "professional services AI", "client management automation"
    - Each page: add a simple WebPage schema with name, description, and url

    **Implementation pattern** for JSON-LD in Next.js server components:
    ```tsx
    // In the server component (page.tsx)
    const jsonLd = { "@context": "https://schema.org", ... }
    return (
      <>
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
        />
        <CaseStudyContent />
        <Footer />
      </>
    )
    ```

    For pages that are currently 'use client' (like pricing), the JSON-LD must go in the server component wrapper. If the page was split in 32-02 (page.tsx server + pricing-page-client.tsx client), add JSON-LD to the server page.tsx. If it wasn't split, create the split now.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Landing page has Organization + SoftwareApplication JSON-LD
    - Pricing page has FAQPage JSON-LD
    - Case study page has Article JSON-LD
    - All industry pages have WebPage JSON-LD and industry-specific keywords
    - All structured data is valid JSON embedded as script tags
    - Build passes cleanly
  </done>
</task>

</tasks>

<verification>
- `cd /home/claude/bitbit/personal-assistant && npx next build` completes without errors
- sitemap includes all marketing routes (verify with `grep -c 'url:' src/app/sitemap.ts` shows 10)
- robots.ts allows /industries and /case-study routes
- Landing page source contains `application/ld+json` script tags
- Pricing page contains FAQPage structured data
- Layout metadata includes openGraph and twitter fields
- No broken imports or missing dependencies
</verification>

<success_criteria>
1. Complete sitemap.xml covering all 10 marketing pages with proper priorities
2. robots.txt allows all marketing pages, blocks dashboard/API
3. Open Graph and Twitter card metadata on root layout
4. JSON-LD structured data on landing (Organization + Product), pricing (FAQ), case study (Article)
5. All industry pages have SEO-optimized metadata with industry keywords
6. Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/32-marketing-site-checkout/32-03-SUMMARY.md`
</output>
