---
phase: 45-wire-living-brain
plan: 04
type: execute
wave: 2
depends_on: [45-01, 45-02]
files_modified:
  - personal-assistant/src/lib/context-assembly/context-assembler.ts
  - personal-assistant/src/lib/brain/global-workspace.ts
  - personal-assistant/src/lib/brain/query-gate.ts
  - personal-assistant/src/lib/agent/engine/taor-loop.ts
autonomous: true
requirements: [WIRE-06, WIRE-07]

must_haves:
  truths:
    - "Global Workspace dynamically allocates context budget across competing memory modules"
    - "Query Gate System 1 path reads cached dossiers in <50ms"
    - "System 2 path does full retrieval with all memory modules"
  artifacts:
    - path: "personal-assistant/src/lib/context-assembly/context-assembler.ts"
      provides: "Global Workspace integration for dynamic budget allocation"
      contains: "allocateContextBudget"
    - path: "personal-assistant/src/lib/agent/engine/taor-loop.ts"
      provides: "System 1/2 routing with dossier cache fast path"
      contains: "system1"
  key_links:
    - from: "context-assembler.ts"
      to: "global-workspace.ts"
      via: "allocateContextBudget()"
      pattern: "import"
    - from: "taor-loop.ts"
      to: "query-gate.ts"
      via: "classifyQueryComplexity()"
      pattern: "import"
---

<objective>
Enable Global Workspace competitive context allocation and wire Query Gate System 1 fast path to use cached dossiers.

Purpose: Global Workspace ensures the most relevant memory modules get the most context tokens (fiduciary constraints always win, then entity dossiers, then patterns, etc.). Query Gate routes simple queries through a <50ms dossier-only path (System 1) while complex queries get full retrieval (System 2).
Output: allocateContextBudget() called during assembly, System 1 reads dossiers only.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Global Workspace into context assembler</name>
  <files>personal-assistant/src/lib/context-assembly/context-assembler.ts, personal-assistant/src/lib/brain/global-workspace.ts</files>
  <action>
1. Read global-workspace.ts — understand allocateContextBudget():
   - Takes: query text, total budget (tokens), available modules
   - Returns: Map of module → allocated tokens
   - Each module has priority (critical/high/medium/low) and min/max bounds
   - Fiduciary module always gets minimum budget (safety)

2. Read context-assembler.ts — find where memory modules are currently assembled with static budgets.

3. When useGlobalWorkspace is true in config:
   a. Before assembling memory sections, call allocateContextBudget() with the user's query
   b. Use the returned allocations to set token limits per section
   c. Replace any static token budgets with the dynamic allocations
   d. Log allocations for debugging: { module: tokens } map

4. When useGlobalWorkspace is false, use existing static budgets (backward compat).
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && grep -n "allocateContextBudget" src/lib/context-assembly/context-assembler.ts | head -5</automated>
  </verify>
  <done>allocateContextBudget() called when useGlobalWorkspace=true, dynamic token limits applied per module.</done>
</task>

<task type="auto">
  <name>Task 2: Wire System 1 fast path with dossier cache</name>
  <files>personal-assistant/src/lib/agent/engine/taor-loop.ts, personal-assistant/src/lib/brain/query-gate.ts</files>
  <action>
1. Read taor-loop.ts — find where queryComplexity (System 1/2) is already classified.
   The classifyQueryComplexity() call already exists and returns 'system1' or 'system2'.
   Currently, System 1 just uses reduced assembler config (maxEntities: 3, no compressed history).

2. For System 1 path, modify to:
   a. Build brain state prefix from cached dossiers only (skip proactive recall, skip RAG)
   b. Use the prompt cache L1 hit — buildBrainStatePrefix returns cached content for the org
   c. Set assembler overrides to skip expensive modules: { skipProactiveRecall: true, skipRAG: true, maxEntities: 3 }
   d. Target: <50ms response time for System 1 queries (greetings, confirmations, simple lookups)

3. For System 2 path, use full assembly (existing behavior + new brain modules from plans 02/03).

4. Add timing measurement: log time_to_first_token for System 1 vs System 2 paths.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && grep -n "system1\|system2\|skipProactiveRecall" src/lib/agent/engine/taor-loop.ts | head -10</automated>
  </verify>
  <done>System 1 queries skip proactive recall + RAG, use dossier cache only. System 2 uses full pipeline. Timing logged.</done>
</task>

</tasks>

<verification>
- allocateContextBudget import and call in context-assembler.ts
- System 1 path skips expensive modules
- Both System 1 and System 2 paths work without errors
</verification>

<success_criteria>
- Global Workspace dynamically allocates context budget
- System 1 queries routed through fast dossier path
- System 2 queries use full retrieval
</success_criteria>

<output>
After completion, create `.planning/phases/45-wire-living-brain/45-04-SUMMARY.md`
</output>
