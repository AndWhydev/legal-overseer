---
phase: 45-wire-living-brain
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/src/lib/context-assembly/context-assembler.ts
  - personal-assistant/src/lib/context/baseplate-snapshot.ts
  - personal-assistant/src/lib/brain/prompt-cache.ts
autonomous: true
requirements: [WIRE-01, WIRE-05]

must_haves:
  truths:
    - "ContextAssembler reads entity_dossiers as primary context source, falling back to entity_profiles"
    - "Domain profiles injected into system prompt via prompt cache L1 prefix"
    - "buildBrainStatePrefix is called in the conversation pipeline when usePromptCache is true"
  artifacts:
    - path: "personal-assistant/src/lib/context/baseplate-snapshot.ts"
      provides: "Entity dossier lookup with entity_profiles fallback"
      contains: "entity_dossiers"
    - path: "personal-assistant/src/lib/context-assembly/context-assembler.ts"
      provides: "Prompt cache integration via buildBrainStatePrefix"
      contains: "buildBrainStatePrefix"
  key_links:
    - from: "context-assembler.ts"
      to: "prompt-cache.ts"
      via: "buildBrainStatePrefix()"
      pattern: "import"
---

<objective>
Wire entity dossiers into context assembly (replacing old entity_profiles) and enable prompt cache with domain profiles.

Purpose: Make every conversation response brain-powered. Entity dossiers compiled by the Living Brain replace the old baseplate snapshot as the primary source of entity context.
Output: getBaseplateSnapshot reads entity_dossiers first (with fallback), buildBrainStatePrefix called during conversation assembly.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire entity_dossiers into baseplate-snapshot with fallback</name>
  <files>personal-assistant/src/lib/context/baseplate-snapshot.ts</files>
  <action>
Modify getBaseplateSnapshot() to try entity_dossiers first:

1. Read the current implementation — it queries `entity_profiles` table.
2. Add a new function `getDossierSnapshot()` that:
   - Queries `entity_dossiers` for the given entity_id + org_id
   - If found, returns the dossier_markdown as the entity context (much richer than old profiles)
   - If not found (dossier not yet compiled), falls back to the existing entity_profiles query
3. Replace the body of getBaseplateSnapshot to call getDossierSnapshot first.
4. Log which source was used (dossier vs profile) for monitoring.

The fallback is critical — entity_dossiers will be empty until the brain consolidation pipeline processes enough data. Old entity_profiles must continue to work until then.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx vitest run src/lib/context/__tests__/ 2>&1 | tail -5</automated>
  </verify>
  <done>getBaseplateSnapshot tries entity_dossiers first, falls back to entity_profiles. Logging indicates source.</done>
</task>

<task type="auto">
  <name>Task 2: Call buildBrainStatePrefix in context assembler</name>
  <files>personal-assistant/src/lib/context-assembly/context-assembler.ts, personal-assistant/src/lib/brain/prompt-cache.ts</files>
  <action>
1. Read context-assembler.ts — find where the system prompt is assembled (the assemble() method).
2. Read prompt-cache.ts — understand buildBrainStatePrefix() signature and what it returns.
3. In context-assembler.ts assemble(), when `this.config.usePromptCache` is true:
   a. Call buildBrainStatePrefix(supabase, orgId, { userProfile, budget: 50000 })
   b. Prepend the returned prefix to the system prompt
   c. This prefix contains: fiduciary constraints, entity dossiers (top 30 by recency), domain profiles
   d. The prefix is designed to be cacheable by Anthropic's prompt caching (static between turns)
4. If usePromptCache is false or buildBrainStatePrefix throws, fall through to existing behavior.
5. The usePromptCache flag was already wired into ContextAssembler constructor in the taor-loop.ts edit.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && grep -n "buildBrainStatePrefix" src/lib/context-assembly/context-assembler.ts | head -5</automated>
  </verify>
  <done>buildBrainStatePrefix called during assemble() when usePromptCache=true, prefix prepended to system prompt.</done>
</task>

</tasks>

<verification>
- grep confirms buildBrainStatePrefix import and call in context-assembler.ts
- grep confirms entity_dossiers query in baseplate-snapshot.ts
- Existing tests still pass (no breaking changes due to fallback)
</verification>

<success_criteria>
- Entity dossiers are the primary context source (with profile fallback)
- Domain profiles injected via prompt cache prefix
- No regression in existing conversation flow
</success_criteria>

<output>
After completion, create `.planning/phases/45-wire-living-brain/45-02-SUMMARY.md`
</output>
