---
phase: 45-wire-living-brain
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/src/lib/memory-palace/proactive-recall.ts
  - personal-assistant/src/lib/knowledge-graph/graph-queries.ts
autonomous: true
requirements: [WIRE-02, WIRE-03]

must_haves:
  truths:
    - "Spreading activation fires from mentioned entities during proactive recall"
    - "Neural decay confidence scores affect recall ranking (low-confidence memories rank lower)"
    - "Max 3 additional entities surfaced via spreading activation per recall"
  artifacts:
    - path: "personal-assistant/src/lib/memory-palace/proactive-recall.ts"
      provides: "Spreading activation integration + decay-weighted ranking"
      contains: "spreadingActivation"
  key_links:
    - from: "proactive-recall.ts"
      to: "graph-queries.ts"
      via: "spreadingActivation()"
      pattern: "import"
---

<objective>
Wire spreading activation and neural decay into proactive recall.

Purpose: When a user mentions "Steve", proactive recall fires spreading activation from Steve's entity node to discover related entities (Bob who works with Steve, the project they share). Memory recall ranking now factors in neural decay confidence — stale memories rank lower.
Output: proactive-recall.ts calls spreadingActivation() and weights results by confidence.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate spreading activation + decay weighting into proactive recall</name>
  <files>personal-assistant/src/lib/memory-palace/proactive-recall.ts, personal-assistant/src/lib/knowledge-graph/graph-queries.ts</files>
  <action>
1. Read proactive-recall.ts — understand the current recall flow:
   - It currently does vector similarity search on memory_palace_entries
   - It may have a graphAwareRecall path that uses entity_nodes
   - Find the main entry point (proactiveRecall or similar)

2. Read graph-queries.ts — understand spreadingActivation():
   - Signature: spreadingActivation(supabase, orgId, seedEntityId, opts?)
   - Returns: Array of { entity_id, activation_level, name }
   - Uses Postgres RPC with decay factor and time decay

3. Modify proactive-recall.ts:
   a. After identifying mentioned entities from the user message, call spreadingActivation() for each
   b. Cap at max 3 additional entities per recall (prevents context bloat)
   c. For each activated entity, fetch their entity_dossier (if exists) and include in recall results
   d. Weight all recall results by their confidence field — multiply relevance score by confidence
   e. Sort final results by weighted score (relevance × confidence)
   f. Memories with confidence < 0.3 (heavily decayed) are excluded entirely

4. Guard with try/catch — if spreading activation RPC fails, fall back to existing behavior.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && grep -n "spreadingActivation" src/lib/memory-palace/proactive-recall.ts | head -5</automated>
  </verify>
  <done>proactive-recall.ts imports and calls spreadingActivation(), caps at 3 extra entities, weights by confidence, excludes < 0.3 confidence.</done>
</task>

</tasks>

<verification>
- spreadingActivation import present in proactive-recall.ts
- Confidence weighting logic visible in recall scoring
- Existing recall tests still pass
</verification>

<success_criteria>
- Spreading activation fires for mentioned entities
- Related entities surfaced (max 3)
- Low-confidence memories rank lower in recall
- Graceful fallback if RPC fails
</success_criteria>

<output>
After completion, create `.planning/phases/45-wire-living-brain/45-03-SUMMARY.md`
</output>
