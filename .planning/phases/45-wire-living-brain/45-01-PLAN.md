---
phase: 45-wire-living-brain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/package.json
  - personal-assistant/supabase/migrations/20260416000001_cognitive_tables_rls.sql
  - personal-assistant/src/lib/brain/cognitive/types.ts
autonomous: true
requirements: [INFRA-01, INFRA-04, INFRA-05]

must_haves:
  truths:
    - "graphology and simple-statistics are installed and importable"
    - "All 6 new cognitive tables exist with RLS enabled"
    - "entity_dossiers has confidence_by_domain column"
  artifacts:
    - path: "personal-assistant/package.json"
      provides: "graphology + simple-statistics deps"
      contains: "graphology"
    - path: "personal-assistant/supabase/migrations/20260416000001_cognitive_tables_rls.sql"
      provides: "belief_states, anomaly_events, clarification_queue, goal_nodes, temporal_constraints, metacognitive_scores tables + RLS"
      min_lines: 80
    - path: "personal-assistant/src/lib/brain/cognitive/types.ts"
      provides: "Shared Zod schemas and TS types for all cognitive modules"
      min_lines: 50
  key_links:
    - from: "migration SQL"
      to: "Supabase Postgres"
      via: "supabase db push"
      pattern: "CREATE TABLE"
---

<objective>
Install dependencies, create database tables with RLS, and define shared cognitive types.

Purpose: Foundation layer that all other plans depend on -- deps, tables, and type contracts.
Output: graphology + simple-statistics installed, 6 new tables with RLS, shared types file.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + create cognitive types</name>
  <files>personal-assistant/package.json, personal-assistant/src/lib/brain/cognitive/types.ts</files>
  <action>
1. cd personal-assistant && npm install graphology simple-statistics && npm install -D @types/graphology

2. Create `src/lib/brain/cognitive/types.ts` with Zod schemas and TS types for all cognitive modules:
   - BeliefState: { id, orgId, entityId, topic, beliefText, groundTruthText?, divergence: 'aligned'|'outdated'|'contradicted'|'unknown', confidence, evidenceIds, lastEvidenceAt }
   - AnomalyEvent: { id, orgId, entityId, metricName, currentValue, baselineMean, baselineStddev, zScore, severity: 'warning'|'critical', explanation?, surfacedAt?, resolvedAt? }
   - ClarifyingQuestion: { id, orgId, entityId?, question, knowledgeGap, informationGain, urgency: 'low'|'medium'|'high', status: 'pending'|'asked'|'answered'|'expired', answer? }
   - GoalNode: { id, orgId, parentId?, title, description?, status: 'not_started'|'in_progress'|'blocked'|'completed'|'abandoned', dependencies, deadline?, estimatedHours?, criticalPath, progress }
   - TemporalConstraint: { id, orgId, goalId?, entityId?, constraintType: 'deadline'|'duration'|'dependency'|'commitment', description, earliestStart?, latestFinish?, slack? }
   - MetacognitiveScore: { id, orgId, entityId, domain, confidence, evidenceCount, knownGaps, staleAreas, lastSignalAt }
   - DomainConfidence: Record<string, number> (e.g. { financial: 0.9, relational: 0.7 })
   - SurpriseEvent: { entityId, factText, surpriseScore, domain }

   Export all types + Zod schemas. Use z.object() with z.infer<> pattern.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && node -e "require('graphology'); require('simple-statistics'); console.log('deps OK')" && npx tsc --noEmit src/lib/brain/cognitive/types.ts 2>&1 | head -20</automated>
  </verify>
  <done>graphology + simple-statistics importable, types.ts compiles with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Create cognitive tables migration with RLS</name>
  <files>personal-assistant/supabase/migrations/20260416000001_cognitive_tables_rls.sql</files>
  <action>
Create migration file with all 6 tables from ARCHITECTURE.md research, plus column additions:

Tables (exact schemas from research/ARCHITECTURE.md lines 299-414):
1. belief_states (Theory of Mind)
2. anomaly_events (Anomaly Detection)
3. clarification_queue (Active Learning)
4. goal_nodes (Goal Decomposition)
5. temporal_constraints (Deadline Propagation)
6. metacognitive_scores (Metacognition)

Column additions:
- ALTER TABLE entity_dossiers ADD COLUMN IF NOT EXISTS confidence_by_domain jsonb DEFAULT '{}';

All indexes from the research doc. All 6 tables get:
- ALTER TABLE X ENABLE ROW LEVEL SECURITY;
- CREATE POLICY "org_isolation_X" ON X FOR ALL USING (org_id = (current_setting('app.current_org_id', true))::uuid);

This uses the same RLS pattern as existing tables in the codebase. Check an existing migration for the exact policy pattern if needed (e.g. entity_dossiers table).

DO NOT run `supabase db push` -- that's a manual step the user does.
  </action>
  <verify>
    <automated>cat /home/claude/bitbit/personal-assistant/supabase/migrations/20260416000001_cognitive_tables_rls.sql | grep -c 'CREATE TABLE' && cat /home/claude/bitbit/personal-assistant/supabase/migrations/20260416000001_cognitive_tables_rls.sql | grep -c 'ENABLE ROW LEVEL SECURITY'</automated>
  </verify>
  <done>Migration file has 6 CREATE TABLE statements, 6 ENABLE ROW LEVEL SECURITY, all indexes, confidence_by_domain column addition. NOT applied yet (user runs supabase db push).</done>
</task>

</tasks>

<verification>
- `npm ls graphology simple-statistics` shows both installed
- Migration SQL is syntactically valid (6 tables, 6 RLS, indexes)
- types.ts compiles
</verification>

<success_criteria>
- graphology and simple-statistics are in package.json dependencies
- Migration file exists with all 6 tables + RLS + indexes
- Shared types file exports all cognitive type interfaces and Zod schemas
</success_criteria>

<output>
After completion, create `.planning/phases/45-wire-living-brain/45-01-SUMMARY.md`
</output>
