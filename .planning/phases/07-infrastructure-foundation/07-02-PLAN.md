---
phase: 07-infrastructure-foundation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - personal-assistant/src/lib/agent/run-logger.ts
  - personal-assistant/src/lib/agent/run-logger.test.ts
  - personal-assistant/src/lib/agent/confidence-router.test.ts
  - personal-assistant/src/lib/agent/shared-tools.test.ts
autonomous: true
requirements: [INFR-02, INFR-03]

must_haves:
  truths:
    - "Every agent execution logs token count (in + out), cost estimate, actions taken, and confidence score to the agent_runs table"
    - "run-logger accepts a Supabase client parameter (follows DI pattern from plan 01)"
    - "Confidence routing produces correct act/ask/escalate decisions for boundary values (0.85, 0.55, edge cases)"
    - "Shared CRUD tools execute successfully against Supabase (insert, query, update operations verified)"
    - "Cost estimate is calculated from token counts and model tier"
  artifacts:
    - path: "personal-assistant/src/lib/agent/run-logger.ts"
      provides: "Agent run logging with cost tracking"
      exports: ["logAgentRun", "estimateRunCost"]
    - path: "personal-assistant/src/lib/agent/run-logger.test.ts"
      provides: "Tests for run logging"
      min_lines: 40
    - path: "personal-assistant/src/lib/agent/confidence-router.test.ts"
      provides: "Production-grade tests for confidence routing"
      min_lines: 80
  key_links:
    - from: "personal-assistant/src/lib/agent/run-logger.ts"
      to: "agent_runs table"
      via: "Supabase insert"
      pattern: "from.*agent_runs.*insert"
    - from: "personal-assistant/src/lib/agent/run-logger.ts"
      to: "AgentRun type"
      via: "import from @bitbit/core or @/lib/bitbit-core"
      pattern: "AgentRun"
    - from: "personal-assistant/src/lib/agent/confidence-router.test.ts"
      to: "routeByConfidence"
      via: "test assertions"
      pattern: "routeByConfidence|routeAgentAction"
---

<objective>
Create agent run logging module and verify v1.0 agent infrastructure (confidence routing + shared CRUD tools) works correctly with production-grade tests.

Purpose: INFR-02 requires every agent execution to log tokens, cost, actions, and confidence to the database. INFR-03 requires verifying that confidence routing (AGNT-12) and shared CRUD tools (AGNT-13) produce correct results. This plan creates the run logger and adds comprehensive tests to prove the v1.0 infrastructure is production-ready.

Output: run-logger.ts module, expanded confidence-router tests, expanded shared-tools tests (post-DI).
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-infrastructure-foundation/07-01-SUMMARY.md
@personal-assistant/src/lib/agent/confidence-router.ts
@personal-assistant/supabase/migrations/009_agent_runs.sql
@packages/core/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent run logger with cost estimation</name>
  <files>
    personal-assistant/src/lib/agent/run-logger.ts
    personal-assistant/src/lib/agent/run-logger.test.ts
  </files>
  <action>
    Create run-logger.ts:
    1. Import `SupabaseClient` type from `@supabase/supabase-js` (follows DI pattern from plan 01)
    2. Import `AgentRun`, `AgentAction`, `ModelTier`, `ConfidenceDecision` from `@/lib/bitbit-core`
    3. Define cost constants per model tier:
       - haiku: $0.25/M input, $1.25/M output
       - sonnet: $3/M input, $15/M output
       - opus: $15/M input, $75/M output
    4. Export `estimateRunCost(tokensIn: number, tokensOut: number, model: ModelTier): number` — returns USD cost
    5. Export `logAgentRun(supabase: SupabaseClient, run: Omit<AgentRun, 'id' | 'created_at'>): Promise<{ id: string } | null>` — inserts into agent_runs table, returns the generated id
    6. Handle errors gracefully: log warning, return null (never throw — logging should not break agent execution)
    7. Export `getRecentRuns(supabase: SupabaseClient, orgId: string, limit?: number): Promise<AgentRun[]>` — fetches recent runs for an org, ordered by created_at DESC

    Create run-logger.test.ts:
    1. Test `estimateRunCost()` with known token counts for each model tier
    2. Test `logAgentRun()` with mocked Supabase — verify correct table and payload
    3. Test `logAgentRun()` error handling — verify it returns null on failure, does not throw
    4. Test `getRecentRuns()` with mocked Supabase — verify query parameters
  </action>
  <verify>
    Run: `cd personal-assistant && npx tsc --noEmit` — zero errors
    Run: `cd personal-assistant && npx jest run-logger` — all tests pass
  </verify>
  <done>
    run-logger.ts exports logAgentRun (DI pattern, inserts to agent_runs), estimateRunCost (calculates USD from tokens + model), getRecentRuns. All functions accept SupabaseClient parameter. Tests verify correct behavior and error handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify confidence routing and shared CRUD tools with production-grade tests</name>
  <files>
    personal-assistant/src/lib/agent/confidence-router.test.ts
    personal-assistant/src/lib/agent/shared-tools.test.ts
  </files>
  <action>
    Expand confidence-router.test.ts (INFR-03 / AGNT-12 verification):
    1. If the file already has 24 tests, review them for completeness. Add any missing:
       - Exact boundary values: confidence=0.85 → act, confidence=0.8499 → ask, confidence=0.55 → ask, confidence=0.5499 → escalate
       - Custom thresholds: agent-level overrides org-level overrides defaults
       - Invalid thresholds: act <= ask falls back to defaults
       - Edge cases: confidence=0.0, confidence=1.0, confidence=-0.1, confidence=1.5
       - `routeAgentAction()` convenience function with full config objects
    2. Add a test group "Production scenarios" that simulates real agent decisions:
       - Invoice send (confidence 0.92) → act
       - Lead reply draft (confidence 0.70) → ask
       - Unknown request (confidence 0.30) → escalate

    Expand shared-tools.test.ts (INFR-03 / AGNT-13 verification):
    1. Update tests to use DI pattern (pass mocked Supabase as first arg, per plan 01 refactor)
    2. Verify all CRUD operations work with the DI pattern:
       - createTask: inserts with correct org_id scoping
       - searchContacts: queries with entity resolver
       - createInvoice: calculates GST, inserts correctly
       - updateInvoice: updates status
       - searchInvoices: filters by status, contact, date range
    3. Add error handling tests: what happens when Supabase returns an error for each operation
  </action>
  <verify>
    Run: `cd personal-assistant && npx jest confidence-router` — all tests pass (24+ existing + new)
    Run: `cd personal-assistant && npx jest shared-tools` — all tests pass (11+ existing + new)
  </verify>
  <done>
    Confidence routing verified with boundary values, custom thresholds, edge cases, and production scenarios. Shared CRUD tools verified with DI pattern, org-scoping, error handling. Both modules are production-ready.
  </done>
</task>

</tasks>

<verification>
1. `cd personal-assistant && npx tsc --noEmit` passes with zero errors
2. `cd personal-assistant && npx jest run-logger` — all tests pass
3. `cd personal-assistant && npx jest confidence-router` — all tests pass including boundary values
4. `cd personal-assistant && npx jest shared-tools` — all tests pass with DI pattern
5. run-logger.ts uses SupabaseClient parameter (DI pattern)
6. Cost estimation produces correct values for known inputs
</verification>

<success_criteria>
- run-logger.ts exists with logAgentRun, estimateRunCost, getRecentRuns
- Agent run logging inserts correct payload to agent_runs table (tokens, cost, actions, confidence, model)
- Confidence routing passes all boundary tests (0.85, 0.55, edge cases)
- Shared CRUD tools pass all tests with DI pattern
- All modules follow DI pattern (SupabaseClient as parameter)
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-infrastructure-foundation/07-02-SUMMARY.md`
</output>
