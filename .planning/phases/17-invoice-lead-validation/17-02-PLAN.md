---
phase: 17-invoice-lead-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/src/lib/agent/invoice-pdf.ts
  - personal-assistant/src/lib/agent/invoice-pdf.test.ts
  - personal-assistant/src/lib/agent/invoice-sender.ts
  - personal-assistant/src/lib/agent/invoice-sender.test.ts
autonomous: true
requirements: [INVC-08, INVC-09, INVC-10]

must_haves:
  truths:
    - "Generated PDF HTML includes ABN field and GST registration number from org settings"
    - "Generated PDF HTML includes professional branded header with logo placeholder and company color"
    - "Invoice email uses Resend with proper from name, reply-to, and subject line to avoid spam filters"
    - "Full invoice lifecycle (draft -> sent -> viewed -> paid) is tested with status transitions and timestamps"
    - "Invoice 'viewed' status can be set via a tracking endpoint or direct update"
  artifacts:
    - path: "personal-assistant/src/lib/agent/invoice-pdf.ts"
      provides: "Branded PDF with ABN/GST fields"
      contains: "abn"
    - path: "personal-assistant/src/lib/agent/invoice-pdf.test.ts"
      provides: "Tests verifying ABN, GST number, branding in PDF output"
      contains: "ABN"
    - path: "personal-assistant/src/lib/agent/invoice-sender.ts"
      provides: "Email delivery with spam-avoidance headers and lifecycle tracking"
      contains: "viewed"
    - path: "personal-assistant/src/lib/agent/invoice-sender.test.ts"
      provides: "Full lifecycle transition tests"
      contains: "lifecycle"
  key_links:
    - from: "personal-assistant/src/lib/agent/invoice-pdf.ts"
      to: "personal-assistant/src/lib/agent/invoice-sender.ts"
      via: "generateInvoicePdf import"
      pattern: "generateInvoicePdf"
    - from: "personal-assistant/src/lib/agent/invoice-sender.ts"
      to: "personal-assistant/src/lib/email/send-invoice.ts"
      via: "sendInvoiceEmail import"
      pattern: "sendInvoiceEmail"
---

<objective>
Enhance invoice PDF with ABN/GST details and professional branding, improve email delivery for inbox placement, and validate the full invoice lifecycle from draft through paid.

Purpose: INVC-08 requires branded PDFs with correct ABN/GST. INVC-09 requires emails reaching inbox (not spam). INVC-10 requires full lifecycle validation (draft -> approved -> sent -> viewed -> paid).

Output: Enhanced PDF generator, improved email sending, lifecycle test coverage.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@personal-assistant/src/lib/agent/invoice-pdf.ts
@personal-assistant/src/lib/agent/invoice-pdf.test.ts
@personal-assistant/src/lib/agent/invoice-sender.ts
@personal-assistant/src/lib/agent/invoice-sender.test.ts
@personal-assistant/src/lib/email/send-invoice.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ABN/GST to PDF and enhance branding</name>
  <files>personal-assistant/src/lib/agent/invoice-pdf.ts, personal-assistant/src/lib/agent/invoice-pdf.test.ts</files>
  <action>
**1. Extend `InvoicePdfSettings` interface** in invoice-pdf.ts:
- Add `abn?: string` (Australian Business Number, format: "XX XXX XXX XXX")
- Add `gst_registered?: boolean` (defaults to true for Australian businesses)
- Add `address_lines?: string[]` (company address for professional header)
- Add `payment_instructions?: string` (replaces generic bank details text)

**2. Update `generateInvoicePdf` HTML template:**

a. **Header section** — add company address lines below company name if provided:
```html
${(orgSettings?.address_lines ?? []).map(line => `<p style="margin:0;font-size:13px;color:#475569;">${escapeHtml(line)}</p>`).join('')}
```

b. **Footer section** — add ABN and GST registration:
- If `abn` is provided: `<p>ABN: ${escapeHtml(abn)}</p>`
- If `gst_registered` is true (or undefined, defaulting to true): `<p>GST Registered</p>`
- Keep existing bank details line

c. **GST line in totals** — already shows "GST (10%)" which is correct. Add a note if NOT GST registered: change to "Tax (0%)" and set tax display to $0.00 when `gst_registered === false`.

d. **Professional polish:**
- Add a subtle border-left on the header using the accent color: `border-left: 4px solid ${accent}`
- Add "Tax Invoice" label (required by Australian tax law for GST-registered businesses) above the invoice number in the header

**3. Update tests** in invoice-pdf.test.ts:
- Test that ABN appears in output when provided in settings
- Test that "Tax Invoice" label appears when gst_registered is true or undefined
- Test that GST shows 0% when gst_registered is false
- Test that address lines render in header
- Test that payment_instructions replaces default bank details text
  </action>
  <verify>
    <automated>cd /home/claude/bitbit && npx vitest run personal-assistant/src/lib/agent/invoice-pdf.test.ts 2>&1 | tail -20</automated>
    <manual>PDF HTML output includes ABN, GST registration, Tax Invoice label, and address lines</manual>
  </verify>
  <done>Invoice PDF includes ABN, GST registration status, "Tax Invoice" label, company address, and payment instructions. All PDF tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Email delivery hardening and full lifecycle validation tests</name>
  <files>personal-assistant/src/lib/agent/invoice-sender.ts, personal-assistant/src/lib/agent/invoice-sender.test.ts</files>
  <action>
**1. Email delivery improvements** in invoice-sender.ts:

a. In `processApprovedInvoiceSends`, update the `sendInvoiceEmail` call to include:
- `from` with a professional display name: `"${orgName} Invoices" <invoices@bitbit.chat>` where orgName comes from organization.name
- `subject` with professional format: `Invoice ${invoice.invoice_number} — ${projectRef || 'Services'} — Due ${dueDate}`

b. These fields are already supported by `SendInvoiceEmailOptions` interface (has `from?` and `subject?` optional params) so no changes needed to send-invoice.ts.

**2. Add invoice lifecycle tracking** in invoice-sender.ts:

a. Add a new exported function `markInvoiceViewed`:
```typescript
export async function markInvoiceViewed(
  supabase: SupabaseClient,
  orgId: string,
  invoiceId: string,
): Promise<{ updated: boolean; error?: string }>
```
- Fetches invoice, checks status is 'sent' (valid transition to 'viewed')
- Updates status to 'viewed' and sets `viewed_at` timestamp (add to update payload)
- Returns `{ updated: true }` on success

b. Add a new exported function `markInvoicePaid`:
```typescript
export async function markInvoicePaid(
  supabase: SupabaseClient,
  orgId: string,
  invoiceId: string,
  paymentDetails?: { method?: string; reference?: string },
): Promise<{ updated: boolean; error?: string }>
```
- Fetches invoice, checks valid transition to 'paid' (from sent, viewed, or overdue)
- Updates status to 'paid', sets `paid_at` timestamp
- Returns `{ updated: true }` on success

**3. Add lifecycle tests** in invoice-sender.test.ts:

a. New describe block 'invoice lifecycle transitions':
- Test draft -> sent (already covered by processApprovedInvoiceSends test)
- Test sent -> viewed via markInvoiceViewed
- Test viewed -> paid via markInvoicePaid
- Test overdue -> paid via markInvoicePaid (valid path)
- Test draft -> viewed returns error (invalid transition)
- Test draft -> paid returns error (invalid transition)
- Test paid -> sent returns error (terminal state)
- Test cancelled -> anything returns error (terminal state)

b. Test email from/subject formatting:
- Verify processApprovedInvoiceSends passes formatted from/subject to sendInvoiceEmail mock
- Mock sendInvoiceEmail to capture arguments and verify format
  </action>
  <verify>
    <automated>cd /home/claude/bitbit && npx vitest run personal-assistant/src/lib/agent/invoice-sender.test.ts 2>&1 | tail -20</automated>
    <manual>All lifecycle transition tests pass, email formatting verified</manual>
  </verify>
  <done>Email sends with professional from/subject. markInvoiceViewed and markInvoicePaid functions handle lifecycle transitions. Full lifecycle from draft through paid is tested. Invalid transitions are rejected.</done>
</task>

</tasks>

<verification>
- `npx vitest run personal-assistant/src/lib/agent/invoice-pdf.test.ts` — all tests pass including ABN/GST
- `npx vitest run personal-assistant/src/lib/agent/invoice-sender.test.ts` — all tests pass including lifecycle
- PDF output contains "Tax Invoice", ABN number, GST registration status
- Email from field includes org name, subject includes invoice number and due date
- Full lifecycle (draft -> sent -> viewed -> paid) verified via tests
</verification>

<success_criteria>
- ABN and GST details appear in generated invoice PDF HTML
- "Tax Invoice" label present for GST-registered businesses
- Email from/subject formatted professionally
- markInvoiceViewed and markInvoicePaid exported and tested
- All valid and invalid lifecycle transitions have test coverage
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-invoice-lead-validation/17-02-SUMMARY.md`
</output>
