---
phase: 01-platform-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/supabase/migrations/001_core_schema.sql
  - personal-assistant/supabase/migrations/002_rls_policies.sql
  - personal-assistant/supabase/migrations/003_seed_defaults.sql
  - personal-assistant/supabase/migrations/004_channels.sql
  - personal-assistant/supabase/seed_awu.sql
autonomous: false
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - PLAT-09
user_setup:
  - service: supabase
    why: "Database backend and auth"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key"
    dashboard_config:
      - task: "Create new Supabase project named 'bitbit-awu' in ap-southeast-2 (Sydney)"
        location: "Supabase Dashboard -> New Project"
      - task: "Enable email auth provider"
        location: "Supabase Dashboard -> Authentication -> Providers -> Email"

must_haves:
  truths:
    - "Supabase project 'bitbit-awu' exists in ap-southeast-2 with auth enabled"
    - "All 12 core tables exist (organizations, profiles, kanban_columns, goals, tasks, contacts, memory_entries, activity_feed, channels, channel_accounts, channel_messages, channel_sync_state)"
    - "RLS is active on all tables with org_id scoping"
    - "AWU organization exists with slug 'awu' and plan 'pro'"
    - "Andy's auth user exists with profile linked to AWU org"
    - "6 AWU client contacts are seeded (White House RE/Sezer, Event Hero/Harun, BEPOP/Dima+Rawya, SexPay, Ozy Homes/Ghazi, Salken/Marquis)"
    - "Sample kanban tasks seeded across columns for demo richness"
  artifacts:
    - path: "personal-assistant/supabase/seed_awu.sql"
      provides: "AWU org, Andy user, 6 contacts, kanban tasks seed data"
      min_lines: 100
  key_links:
    - from: "profiles"
      to: "organizations"
      via: "org_id foreign key"
      pattern: "org_id.*REFERENCES organizations"
    - from: "contacts"
      to: "organizations"
      via: "org_id foreign key"
      pattern: "org_id.*REFERENCES organizations"
---

<objective>
Set up the Supabase project, run all 4 existing migrations, and seed AWU-specific data (org, Andy's user, 6 client contacts, sample kanban tasks).

Purpose: This is the foundation — without a live database with AWU data, nothing else works. Andy needs to see his real clients and tasks when he logs in.
Output: Live Supabase project with schema, RLS, AWU org, Andy's auth account, 6 contacts, and ~12 kanban tasks.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@personal-assistant/supabase/migrations/001_core_schema.sql
@personal-assistant/supabase/migrations/002_rls_policies.sql
@personal-assistant/supabase/migrations/003_seed_defaults.sql
@personal-assistant/supabase/migrations/004_channels.sql
@personal-assistant/supabase/seed_data.sql
@deployments/awu/config.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Supabase project and provide credentials</name>
  <action>
    Andy must create the Supabase project manually (Claude cannot create Supabase projects via CLI without an access token):

    1. Go to https://supabase.com/dashboard → New Project
    2. Name: `bitbit-awu`
    3. Region: `ap-southeast-2` (Sydney)
    4. Generate a strong database password (save it)
    5. Wait for project to provision (~2 minutes)
    6. Go to Project Settings → API and copy:
       - Project URL (NEXT_PUBLIC_SUPABASE_URL)
       - anon/public key (NEXT_PUBLIC_SUPABASE_ANON_KEY)
       - service_role key (needed for seed scripts)
    7. Go to Authentication → Providers → ensure Email is enabled
    8. Provide all 3 values to continue
  </action>
  <verify>User provides NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, and service_role key</verify>
  <done>Supabase project exists in ap-southeast-2 with auth enabled and credentials available</done>
</task>

<task type="auto">
  <name>Task 2: Run migrations and seed AWU data</name>
  <files>personal-assistant/supabase/seed_awu.sql</files>
  <action>
    **Step 1: Run the 4 existing migrations in order via Supabase SQL Editor or CLI.**

    Use the Supabase Management API or `supabase db push` to apply:
    - 001_core_schema.sql (organizations, profiles, kanban_columns, goals, tasks, contacts, memory_entries, activity_feed)
    - 002_rls_policies.sql (RLS on all core tables, org_id scoping)
    - 003_seed_defaults.sql (default kanban columns trigger, updated_at triggers)
    - 004_channels.sql (channels, channel_accounts, channel_messages, channel_sync_state)

    If CLI not available, concatenate and run via `curl` to the Supabase REST SQL endpoint using service_role key.

    **Step 2: Create seed_awu.sql with AWU-specific data.**

    Write `personal-assistant/supabase/seed_awu.sql` that:
    1. Creates AWU organization: name='All Webbed Up', slug='awu', plan='pro', settings from deployments/awu/config.ts (timezone, confidence thresholds, notification channels, branding)
    2. Creates Andy's auth user via `auth.users` insert (or Supabase Auth Admin API): email=`andy@allwebbedup.com.au`, password=temporary (Andy changes on first login)
    3. Creates Andy's profile linked to AWU org: role='owner', display_name='Andy Taleb'
    4. Seeds 6 AWU client contacts from deployments/awu/config.ts clients array:
       - Sezer Yunus (White House RE) — type=client, status=active
       - Harun (Event Hero) — type=client, status=scoping
       - Dima/Rawya (BEPOP) — type=client, status=proposal_sent
       - SexPay contact — type=client, status=quote_pending
       - Ghazi (Ozy Homes) — type=client, status=active
       - Marquis Abela (Salken/Club Team Manager) — type=client, status=active
    5. Seeds ~10-12 sample kanban tasks across columns (Backlog, To Do, In Progress) with AWU-relevant content:
       - "Invoice Sezer for White House RE changes" (To Do, high)
       - "Event Hero scope doc — get .docx from Harun" (To Do, high)
       - "BEPOP proposal follow-up" (To Do, medium)
       - "Ozy Homes ad materials" (In Progress, medium)
       - "Club Team Manager — demo prep for Marquis" (In Progress, high)
       - "Review AWU website analytics" (Backlog, low)
       - Plus 4-6 more realistic AWU agency tasks
    6. Seeds 2-3 goals (e.g., "Grow AWU to 10 active clients", "Launch BitBit for AWU ops")

    **Step 3: Run seed_awu.sql against the live Supabase project.**

    Use curl with service_role key:
    ```
    curl -X POST "$SUPABASE_URL/rest/v1/rpc/exec_sql" \
      -H "apikey: $SERVICE_ROLE_KEY" \
      -H "Authorization: Bearer $SERVICE_ROLE_KEY" \
      -d @personal-assistant/supabase/seed_awu.sql
    ```

    Or use the Supabase SQL Editor in the dashboard.

    **IMPORTANT:** The existing seed_data.sql is for Tor's personal org — do NOT run it. Create a fresh AWU-specific seed file.
  </action>
  <verify>
    Query Supabase to verify counts:
    ```sql
    SELECT 'organizations' as t, count(*) FROM organizations WHERE slug = 'awu'
    UNION ALL SELECT 'profiles', count(*) FROM profiles
    UNION ALL SELECT 'contacts', count(*) FROM contacts
    UNION ALL SELECT 'tasks', count(*) FROM tasks
    UNION ALL SELECT 'kanban_columns', count(*) FROM kanban_columns;
    ```
    Expected: 1 org, 1 profile, 6 contacts, 10+ tasks, 5 kanban columns.
    Also verify RLS is active: `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public';`
  </verify>
  <done>All 12 tables exist with RLS active. AWU org seeded with Andy's account, 6 contacts, and 10+ kanban tasks.</done>
</task>

</tasks>

<verification>
1. Supabase dashboard shows project 'bitbit-awu' in ap-southeast-2
2. SQL query returns 12+ tables in public schema
3. All tables have RLS enabled
4. AWU org exists with correct settings
5. Andy can log in via Supabase Auth (email: andy@allwebbedup.com.au)
6. 6 contacts visible in contacts table
7. Kanban tasks distributed across columns
</verification>

<success_criteria>
- Supabase project live in ap-southeast-2 with all 4 migrations applied
- RLS active on all tables with org_id scoping
- AWU organization, Andy's auth user, 6 client contacts, and 10+ sample tasks seeded
- Andy can authenticate with email/password
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-deploy/01-01-SUMMARY.md`
</output>
