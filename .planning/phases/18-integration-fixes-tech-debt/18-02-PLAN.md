---
phase: 18-integration-fixes-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deployments/fly/src/worker.ts
  - deployments/fly/src/agent-executor.ts
autonomous: true
requirements: [DEPLOY-05, DEPLOY-06]

must_haves:
  truths:
    - "Fly.io worker POST /api/agent/run executes actual agent logic based on agent_type"
    - "Worker dispatches to agent tick functions using Supabase REST API for data access"
    - "Task status is updated to 'completed' or 'failed' after execution"
    - "Worker handles unknown agent types gracefully with error status"
  artifacts:
    - path: "deployments/fly/src/worker.ts"
      provides: "HTTP server that dispatches to agent executor"
      contains: "executeAgentTask"
    - path: "deployments/fly/src/agent-executor.ts"
      provides: "Agent execution logic using Supabase REST API"
      exports: ["executeAgentTask"]
  key_links:
    - from: "deployments/fly/src/worker.ts"
      to: "deployments/fly/src/agent-executor.ts"
      via: "import { executeAgentTask }"
      pattern: "executeAgentTask"
    - from: "deployments/fly/src/agent-executor.ts"
      to: "Supabase REST API"
      via: "fetch to SUPABASE_URL/rest/v1/"
      pattern: "SUPABASE_URL.*rest/v1"
---

<objective>
Replace the TODO stub in the Fly.io worker with actual agent execution logic.

Purpose: The Cloudflare edge cron dispatches tasks to Fly.io, but the worker accepts tasks and does nothing (TODO at line 129). This breaks the DEPLOY-05/DEPLOY-06 requirements -- the worker fleet is "deployed" but not "operational."
Output: Fly.io worker that executes agent tasks and reports completion/failure status.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@deployments/fly/src/worker.ts
@personal-assistant/src/lib/agent/scheduler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement agent executor module for Fly.io worker</name>
  <files>
    deployments/fly/src/agent-executor.ts
  </files>
  <action>
    Create `deployments/fly/src/agent-executor.ts` -- a standalone agent executor that works without Next.js or the monorepo `@supabase/supabase-js` package. It must use raw Supabase REST API calls (the worker already has this pattern in updateTaskStatus).

    The executor should:
    1. Export `executeAgentTask(taskId: string, agentType: string, payload: Record<string, unknown>): Promise<{ success: boolean; error?: string; result?: unknown }>`
    2. Read SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY from env (already available in worker.ts)
    3. Read ANTHROPIC_API_KEY from env for LLM classification
    4. Create a helper `supabaseRest(method, path, body?)` that wraps fetch with apikey/Authorization headers
    5. Implement a dispatch map for known agent types:
       - `channel-triage`: Query unprocessed channel_messages, call Anthropic API to classify each (significance 1-10, time_sensitivity, recommended_actions), update message with classification
       - `lead-swarm`: Query new leads (status='new'), call Anthropic to qualify (hot/warm/cold + confidence), update lead status
       - `invoice-flow`: Query pending invoice tasks from agent_task_queue payload, extract contact/project/amount, update task with result
       - `sentry`: Query recent activity for anomalies, log alerts
       - For all other agent types: Log a warning and return `{ success: true, result: 'no-op for agent type: ${agentType}' }` -- do not fail
    6. Each agent handler should be wrapped in try/catch, returning `{ success: false, error }` on failure
    7. Keep handlers minimal (10-30 lines each) -- the Fly.io worker handles overflow/batch tasks, not full agent lifecycle. The primary use case is channel-triage classification.
    8. Include a 10-second AbortController timeout on Anthropic API calls (consistent with Phase 13 decision)
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/deployments/fly && npx tsc --noEmit src/agent-executor.ts 2>&1 | head -5</automated>
    <manual>File exists with executeAgentTask export, dispatch map for known agents, supabaseRest helper</manual>
  </verify>
  <done>Agent executor module exists with working dispatch for channel-triage, lead-swarm, invoice-flow, sentry, and graceful no-op for unknown types.</done>
</task>

<task type="auto">
  <name>Task 2: Wire agent executor into Fly.io worker and update task status</name>
  <files>
    deployments/fly/src/worker.ts
  </files>
  <action>
    Update `handleAgentRun()` in worker.ts to:
    1. Import `executeAgentTask` from `./agent-executor.js`
    2. Remove the TODO comment at line 129
    3. After `updateTaskStatus(task_id, 'processing')`, call `executeAgentTask(task_id, agent_type, payload as Record<string, unknown>)`
    4. On success: call `updateTaskStatus(task_id, 'completed')` and return 200 with `{ completed: true, task_id, result }`
    5. On failure: call `updateTaskStatus(task_id, 'failed')` and return 200 with `{ completed: false, task_id, error }` (200 not 500 -- the dispatch itself succeeded, the task failed)
    6. Change the response from 202 (accepted, not processed) to the appropriate status based on execution result
    7. Wrap the entire execution in a try/catch -- if executeAgentTask throws unexpectedly, still update status to 'failed' and respond 500
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/deployments/fly && npx tsc --noEmit src/worker.ts 2>&1 | head -5; grep -c "TODO" src/worker.ts; grep -c "executeAgentTask" src/worker.ts</automated>
    <manual>No TODO stub remains. Worker imports and calls executeAgentTask, updates status to completed/failed.</manual>
  </verify>
  <done>Fly.io worker executes actual agent logic. Tasks dispatched from Cloudflare are processed and status updated to completed/failed. No TODO stubs remain.</done>
</task>

</tasks>

<verification>
1. `grep "TODO" deployments/fly/src/worker.ts` returns nothing
2. `grep "executeAgentTask" deployments/fly/src/worker.ts` shows import and call
3. `grep "completed\|failed" deployments/fly/src/worker.ts` shows status updates for both outcomes
4. `deployments/fly/src/agent-executor.ts` exists with executeAgentTask export
5. TypeScript compiles without errors: `cd deployments/fly && npx tsc --noEmit`
</verification>

<success_criteria>
- Fly.io worker executes agent tasks (not a TODO stub)
- Known agent types (channel-triage, lead-swarm, invoice-flow, sentry) have working handlers
- Unknown agent types get graceful no-op (not an error)
- Task status updated to completed/failed after execution
- No TODO comments remain in worker.ts
</success_criteria>

<output>
After completion, create `.planning/phases/18-integration-fixes-tech-debt/18-02-SUMMARY.md`
</output>
