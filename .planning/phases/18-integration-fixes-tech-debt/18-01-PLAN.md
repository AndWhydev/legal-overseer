---
phase: 18-integration-fixes-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/src/app/api/cron/channel-sync/route.ts
  - personal-assistant/src/lib/channels/relay-daemon.ts
  - personal-assistant/src/components/channels/connect-modal.tsx
  - personal-assistant/src/components/channels/channel-grid.tsx
autonomous: true
requirements: [CHAN-04, CHAN-05, OAUTH-03, CHAN-03]

must_haves:
  truths:
    - "channel-sync cron routes messages through relay daemon pollChannel() with dedup, latency instrumentation, burst detection, and classification retry"
    - "classifyWithRetry retry/backoff logic is reachable (no early return before catch)"
    - "connect-modal.tsx WhatsApp QR panel calls bridge API and polls for QR code"
    - "connect-modal.tsx API key form checks correct response field from connect API"
    - "channel-grid.tsx disconnect handler checks correct response field from disconnect API"
    - "RELAY_SECRET env var is documented in setup requirements"
  artifacts:
    - path: "personal-assistant/src/app/api/cron/channel-sync/route.ts"
      provides: "Cron route that delegates to relay-daemon pollChannel()"
      contains: "pollChannel"
    - path: "personal-assistant/src/lib/channels/relay-daemon.ts"
      provides: "classifyWithRetry with reachable retry logic"
      contains: "classifyWithRetry"
    - path: "personal-assistant/src/components/channels/connect-modal.tsx"
      provides: "WhatsApp QR panel that calls bridge API and polls for QR"
      contains: "/api/channels/whatsapp/bridge"
    - path: "personal-assistant/src/components/channels/channel-grid.tsx"
      provides: "Correct response field checks for disconnect"
      contains: "data.disconnected"
  key_links:
    - from: "personal-assistant/src/app/api/cron/channel-sync/route.ts"
      to: "personal-assistant/src/lib/channels/relay-daemon.ts"
      via: "import { pollChannel }"
      pattern: "pollChannel"
    - from: "personal-assistant/src/components/channels/connect-modal.tsx"
      to: "/api/channels/whatsapp/bridge"
      via: "fetch POST then GET polling"
      pattern: "api/channels/whatsapp/bridge"
---

<objective>
Fix broken integrations in channel-sync cron, WhatsApp QR modal, and UI response field mismatches.

Purpose: The channel-sync cron bypasses the relay daemon (skipping dedup, latency, retry). The WhatsApp QR modal never starts the bridge. UI components check wrong response fields. These are the 3 broken integrations + 4 tech debt items from the v1.2 audit.
Output: All channel pipeline integrations working correctly, no response field mismatches.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md
@personal-assistant/src/app/api/cron/channel-sync/route.ts
@personal-assistant/src/lib/channels/relay-daemon.ts
@personal-assistant/src/components/channels/connect-modal.tsx
@personal-assistant/src/components/channels/channel-grid.tsx
@personal-assistant/src/app/api/channels/whatsapp/bridge/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire channel-sync cron to use relay daemon + fix classifyWithRetry</name>
  <files>
    personal-assistant/src/app/api/cron/channel-sync/route.ts
    personal-assistant/src/lib/channels/relay-daemon.ts
  </files>
  <action>
    **channel-sync/route.ts:** Replace the current implementation that uses `getAllAdapters()` + `routeMessages()` directly. Instead:
    1. Remove imports of `getAllAdapters` and `routeMessages`
    2. Import `pollChannel` from `@/lib/channels/relay-daemon`
    3. For each org, query `channel_connections` where `status = 'connected'` and `relay_enabled = true`
    4. For each connection, call `pollChannel(supabase, orgId, channelType)` -- this provides dedup, latency instrumentation, burst detection, and classification retry
    5. Aggregate PollResult stats and log total messages found/inserted
    6. Keep the activity_feed insert but use PollResult data
    7. Remove the manual `last_sync` update (pollChannel already does this)

    **relay-daemon.ts classifyWithRetry fix:** The current code has a `return` on line 65 after the `update` call inside the try block. This makes the retry/catch logic unreachable for normal DB errors. The issue is that the `update` call does NOT throw on Supabase errors -- it returns `{ error }`. Fix:
    1. After the `.update()` call, check `const { error: updateError } = await supabase...`
    2. If `updateError`, throw new Error(updateError.message) so the catch block fires
    3. Only return (success) when updateError is null/undefined
    4. Apply the same pattern in the final failure branch (the unclassified update)
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit src/app/api/cron/channel-sync/route.ts 2>&1 | head -5; grep -c "pollChannel" src/app/api/cron/channel-sync/route.ts; grep -c "getAllAdapters\|routeMessages" src/app/api/cron/channel-sync/route.ts</automated>
    <manual>Verify channel-sync route imports pollChannel and no longer imports getAllAdapters/routeMessages</manual>
  </verify>
  <done>channel-sync cron delegates to pollChannel() for all message processing. classifyWithRetry correctly throws on DB errors so retry/backoff logic is reachable.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WhatsApp QR modal bridge integration + UI response field mismatches</name>
  <files>
    personal-assistant/src/components/channels/connect-modal.tsx
    personal-assistant/src/components/channels/channel-grid.tsx
  </files>
  <action>
    **connect-modal.tsx WhatsAppQRPanel:** Replace the current placeholder implementation:
    1. In `handleInitiate()`, after the existing POST to `/api/channels/connect` succeeds (creates pairing session), also POST to `/api/channels/whatsapp/bridge` to start the Baileys bridge
    2. If bridge start succeeds, set `sessionStarted = true` and begin polling
    3. Add a `qrCode` state variable (string | null) and a `bridgeStatus` state ('idle' | 'starting' | 'pairing' | 'connected' | 'error')
    4. Add a polling effect: when `sessionStarted` is true, poll GET `/api/channels/whatsapp/bridge` every 3 seconds
    5. When the GET response contains `qrCode` (non-null string), display it as a base64 image: `<img src={qrCode} />` (Baileys returns data URI)
    6. When bridge status becomes 'connected', call `onSuccess()` and stop polling
    7. Clear polling interval on unmount via useEffect cleanup
    8. Remove the placeholder text "QR code generation available in Phase 15"
    9. Show loading spinner while bridge is starting, QR image when available, success state when connected

    **connect-modal.tsx ApiKeyForm:** The form checks `res.ok` for success which is correct (line 84). No change needed here -- the audit mentioned `data.success` but the code actually uses `res.ok`. Verify this is correct and leave as-is.

    **channel-grid.tsx handleDisconnect:** The current code checks `res.ok` (line 206) which is correct. The audit mentioned `data.success` but the actual code uses `res.ok`. Verify and leave as-is.

    **channel-grid.tsx handleSyncAll and handleSyncChannel:** These check `data.success` (lines 238, 266). Check what the `/api/channels/sync` endpoint actually returns. If it returns `{ success: true, results: [...] }` then no change needed. If it returns a different shape, fix the field check.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && grep -c "api/channels/whatsapp/bridge" src/components/channels/connect-modal.tsx; grep -c "Phase 15" src/components/channels/connect-modal.tsx</automated>
    <manual>connect-modal.tsx calls bridge API, no "Phase 15" placeholder text remains. channel-grid.tsx response checks verified correct.</manual>
  </verify>
  <done>WhatsApp QR modal calls bridge API to start Baileys, polls for QR code, and displays real QR. UI response field checks are verified correct against actual API responses.</done>
</task>

</tasks>

<verification>
1. `grep "pollChannel" personal-assistant/src/app/api/cron/channel-sync/route.ts` shows import and usage
2. `grep -c "getAllAdapters\|routeMessages" personal-assistant/src/app/api/cron/channel-sync/route.ts` returns 0
3. `grep "api/channels/whatsapp/bridge" personal-assistant/src/components/channels/connect-modal.tsx` shows bridge API call
4. `grep "Phase 15" personal-assistant/src/components/channels/connect-modal.tsx` returns nothing (placeholder removed)
5. classifyWithRetry in relay-daemon.ts checks for updateError and throws on failure
</verification>

<success_criteria>
- channel-sync cron uses pollChannel() for all message processing (dedup, latency, burst, retry included)
- classifyWithRetry retry logic is reachable (throws on DB error, not silently returning)
- WhatsApp QR modal calls bridge API, polls for QR code, and displays it
- No "Phase 15" placeholder text in connect-modal.tsx
- Response field checks in UI verified correct against actual API shapes
</success_criteria>

<output>
After completion, create `.planning/phases/18-integration-fixes-tech-debt/18-01-SUMMARY.md`
</output>
