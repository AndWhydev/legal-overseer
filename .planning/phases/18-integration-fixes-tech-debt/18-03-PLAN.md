---
phase: 18-integration-fixes-tech-debt
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - personal-assistant/tsconfig.json
  - personal-assistant/next.config.ts
  - personal-assistant/src/components/invoices/invoice-list.tsx
  - personal-assistant/src/components/leads/leads-kanban.tsx
  - personal-assistant/src/lib/channels/rate-limiter.ts
  - personal-assistant/src/lib/industry/packs/tradie.ts
  - personal-assistant/src/lib/search/global-search.ts
  - personal-assistant/src/components/leads/lead-detail-card.tsx
autonomous: true
requirements: [DEPLOY-05, DEPLOY-06]

must_haves:
  truths:
    - "ignoreBuildErrors is removed from next.config.ts"
    - "npx tsc --noEmit in personal-assistant/ reports zero errors"
    - "next build completes successfully without ignoreBuildErrors"
  artifacts:
    - path: "personal-assistant/next.config.ts"
      provides: "Next.js config without ignoreBuildErrors"
    - path: "personal-assistant/tsconfig.json"
      provides: "TypeScript config that resolves monorepo SupabaseClient duplication"
  key_links:
    - from: "personal-assistant/tsconfig.json"
      to: "personal-assistant/node_modules/@supabase/supabase-js"
      via: "paths alias forcing single SupabaseClient resolution"
      pattern: "@supabase/supabase-js"
---

<objective>
Remove ignoreBuildErrors from next.config.ts by fixing all 109 TypeScript errors, then verify the build passes.

Purpose: ignoreBuildErrors suppresses ALL TS errors, hiding real bugs. Removing it requires fixing the root cause (dual SupabaseClient from monorepo) plus ~13 real errors.
Output: Clean TypeScript build with zero errors, ignoreBuildErrors removed.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@personal-assistant/tsconfig.json
@personal-assistant/next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dual SupabaseClient resolution and all TypeScript errors</name>
  <files>
    personal-assistant/tsconfig.json
    personal-assistant/src/components/invoices/invoice-list.tsx
    personal-assistant/src/components/leads/leads-kanban.tsx
    personal-assistant/src/lib/channels/rate-limiter.ts
    personal-assistant/src/lib/industry/packs/tradie.ts
    personal-assistant/src/lib/search/global-search.ts
    personal-assistant/src/components/leads/lead-detail-card.tsx
  </files>
  <action>
    **Root cause fix (96 errors):** The monorepo has `@supabase/supabase-js@2.97.0` at root and `@2.95.3` in personal-assistant. TypeScript sees these as incompatible types. Fix by adding a paths alias in `personal-assistant/tsconfig.json` to force resolution to the local copy:

    ```json
    "paths": {
      "@/*": ["./src/*"],
      "@supabase/supabase-js": ["./node_modules/@supabase/supabase-js"]
    }
    ```

    This tells tsc to always resolve `@supabase/supabase-js` to the personal-assistant copy, eliminating the dual-type issue.

    **Remaining real errors (13 total, fix each):**

    1. **TS2459 (4 errors):** `InvoiceRow` not exported from invoice-list.tsx, `LeadCardData` not exported from leads-kanban.tsx. Add `export` keyword to these type/interface declarations.

    2. **TS7006 (1 error):** `parameter 'service' implicitly has 'any' type` in lead-detail-card.tsx line 76. Add explicit type annotation: `(service: string)` or `(service: { name: string; ... })` based on the data shape.

    3. **TS2554 (3 errors):** rate-limiter.ts "Expected 0-1 arguments, but got 2". Check the function signatures and fix the call sites to match.

    4. **TS2353 (1 error):** tradie.ts `kanbanDefaults` not in IndustryPack type. Either add `kanbanDefaults` to the IndustryPack interface, or remove it from the tradie pack object literal.

    5. **TS2307 (1 error):** Cannot find module `@whiskeysockets/baileys`. This is expected (optional dependency). Add a `// @ts-expect-error -- optional dependency, dynamically imported` comment or add a type declaration file `baileys.d.ts` with `declare module '@whiskeysockets/baileys'`.

    6. **TS2345 in global-search.ts (2 errors):** ParserError type from Supabase query. The `.select()` call has a column spec that Supabase's type system cannot parse. Fix by adding a type assertion on the data: `(data as Record<string, unknown>[]).map(...)` or fixing the select string to use valid column names.

    7. **TS2339 (2 errors):** Check what properties are missing and add them or fix the access patterns.

    8. **TS2367 (1 error):** Check the comparison and fix type narrowing.

    After all fixes, run `npx tsc --noEmit` and confirm zero errors.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
    <manual>Should output 0. All 109 TS errors resolved.</manual>
  </verify>
  <done>TypeScript reports zero errors. Dual SupabaseClient resolved via tsconfig paths. All 13 real errors fixed individually.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ignoreBuildErrors and verify Next.js build passes</name>
  <files>
    personal-assistant/next.config.ts
  </files>
  <action>
    1. Remove the `typescript: { ignoreBuildErrors: true }` block from `personal-assistant/next.config.ts`
    2. Remove the comment explaining why it was there (monorepo SupabaseClient mismatch -- now fixed)
    3. Run `cd personal-assistant && npx next build` to verify the build passes without ignoreBuildErrors
    4. If any new TS errors surface during build (next build may find errors tsc --noEmit misses due to .next/types), fix them
    5. The build does not need to fully succeed (it may fail on missing env vars or Supabase connection) -- only the TypeScript compilation phase must pass. Look for "Type checking" in build output.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && grep -c "ignoreBuildErrors" next.config.ts && echo "FAIL: still present" || echo "PASS: removed"; npx next build 2>&1 | grep -E "Type error|Compiled|error TS|Successfully" | head -10</automated>
    <manual>ignoreBuildErrors removed. next build type-checking phase passes (may fail later on env/runtime issues which are expected).</manual>
  </verify>
  <done>ignoreBuildErrors removed from next.config.ts. Next.js build TypeScript phase passes cleanly.</done>
</task>

</tasks>

<verification>
1. `grep "ignoreBuildErrors" personal-assistant/next.config.ts` returns nothing
2. `cd personal-assistant && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0
3. `cd personal-assistant && npx next build 2>&1 | grep "Type error"` returns nothing (no type errors during build)
</verification>

<success_criteria>
- ignoreBuildErrors is removed from next.config.ts
- TypeScript compilation (tsc --noEmit) passes with zero errors
- Next.js build type-checking phase passes
- Root cause of 96 SupabaseClient errors fixed via tsconfig paths (not individual suppression)
- All 13 real TS errors fixed properly (not suppressed)
</success_criteria>

<output>
After completion, create `.planning/phases/18-integration-fixes-tech-debt/18-03-SUMMARY.md`
</output>
