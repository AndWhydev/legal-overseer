---
phase: 36-mobile-experience
plan: 04
type: execute
wave: 3
depends_on: ["36-02", "36-03"]
files_modified:
  - mobile/src/lib/offline-queue.ts
  - mobile/src/hooks/useOffline.ts
  - mobile/src/hooks/useApprovals.ts
  - mobile/src/components/OfflineBanner.tsx
  - mobile/src/components/ApprovalCard.tsx
  - mobile/src/components/QuickAction.tsx
  - mobile/app/(tabs)/approvals.tsx
  - mobile/app/approval/[id].tsx
  - mobile/src/hooks/useChat.ts
autonomous: false
requirements:
  - MOB-04
  - MOB-05

must_haves:
  truths:
    - "Messages sent while offline are queued and sent when connectivity returns"
    - "Offline banner appears when device loses connectivity"
    - "Queued messages show pending indicator in chat"
    - "Approval list loads from /api/agent/approvals"
    - "Swiping right on an approval card approves it"
    - "Swiping left on an approval card rejects it"
    - "Quick action taps trigger correct API mutations with haptic feedback"
    - "Offline mutations (approvals, messages) survive app restart and replay on reconnect"
  artifacts:
    - path: "mobile/src/lib/offline-queue.ts"
      provides: "Offline mutation configuration for TanStack Query"
      exports: ["offlineMutationConfig", "usePendingMutations"]
    - path: "mobile/src/hooks/useOffline.ts"
      provides: "Network status hook"
      exports: ["useOffline"]
    - path: "mobile/src/hooks/useApprovals.ts"
      provides: "Approvals query + mutation hooks"
      exports: ["useApprovals", "useResolveApproval"]
    - path: "mobile/src/components/ApprovalCard.tsx"
      provides: "Swipeable approval card with approve/reject gestures"
    - path: "mobile/src/components/OfflineBanner.tsx"
      provides: "Offline status indicator bar"
  key_links:
    - from: "mobile/src/lib/offline-queue.ts"
      to: "mobile/src/providers/QueryProvider.tsx"
      via: "Mutation persistence config in QueryClient"
      pattern: "networkMode.*offlineFirst"
    - from: "mobile/src/hooks/useApprovals.ts"
      to: "personal-assistant/src/app/api/agent/approvals/route.ts"
      via: "GET for list, POST for resolve"
      pattern: "api/agent/approvals"
    - from: "mobile/src/components/ApprovalCard.tsx"
      to: "mobile/src/hooks/useApprovals.ts"
      via: "useResolveApproval mutation on swipe"
      pattern: "useResolveApproval"
    - from: "mobile/src/hooks/useOffline.ts"
      to: "@react-native-community/netinfo"
      via: "NetInfo.addEventListener"
      pattern: "NetInfo\\.addEventListener"
---

<objective>
Wire the offline mutation queue so messages and approvals survive connectivity loss, and build the approvals screen with swipe-to-approve/reject quick actions.

Purpose: Offline support is essential for mobile -- users lose signal in elevators, tunnels, and poor coverage areas. Queued mutations prevent lost work. Quick actions (swipe gestures) let users process approvals in seconds instead of navigating through detail screens.

Output: Working offline queue with visual indicators, approvals screen with swipeable cards, and haptic-feedback quick actions.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-mobile-experience/36-RESEARCH.md
@.planning/phases/36-mobile-experience/36-01-SUMMARY.md
@.planning/phases/36-mobile-experience/36-02-SUMMARY.md
@.planning/phases/36-mobile-experience/36-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From mobile/src/providers/QueryProvider.tsx:
```typescript
// Already has AsyncStorage persister and NetInfo wiring from Plan 01
// This plan adds mutation-specific offline config
```

From mobile/src/lib/api.ts:
```typescript
export const apiClient: {
  get<T>(path: string): Promise<T>,
  post<T>(path: string, body: unknown): Promise<T>,
}
```

<!-- From Plan 02 -->
From mobile/src/hooks/useChat.ts:
```typescript
export function useChat(threadId?: string): {
  messages: Message[];
  isStreaming: boolean;
  sendMessage: (text: string) => void;
  // Will be extended with offline queue integration
}
```

<!-- Backend approvals API -->
From personal-assistant/src/app/api/agent/approvals/route.ts:
```typescript
// GET /api/agent/approvals?status=pending
// Response: ApprovalRecord[]
// POST /api/agent/approvals
// Body: { id: string, decision: 'approved' | 'rejected' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline mutation queue + network status hook + banner</name>
  <files>
    mobile/src/lib/offline-queue.ts
    mobile/src/hooks/useOffline.ts
    mobile/src/components/OfflineBanner.tsx
    mobile/src/hooks/useChat.ts
    mobile/src/providers/QueryProvider.tsx
  </files>
  <action>
    Wire offline mutation persistence so chat messages and approvals queue when offline and replay on reconnect.

    1. Create `mobile/src/lib/offline-queue.ts`:
       - Configure TanStack Query mutation defaults for offline persistence:
         ```typescript
         export const offlineMutationDefaults = {
           retry: 3,
           retryDelay: (attempt: number) => Math.min(1000 * 2 ** attempt, 30000),
           networkMode: 'offlineFirst' as const,
         }
         ```
       - Export configurePersistentMutations(queryClient):
         Register default mutation functions for known mutation keys so TanStack can resume them after app restart:
         - mutationKey: ['sendMessage'] -> apiClient.post('/api/agent/chat', variables)
         - mutationKey: ['resolveApproval'] -> apiClient.post('/api/agent/approvals', variables)
       - Export usePendingMutations() hook that returns count of pending offline mutations

    2. Update `mobile/src/providers/QueryProvider.tsx`:
       - Call configurePersistentMutations(queryClient) after client creation
       - Set mutation defaults to offlineMutationDefaults
       - Ensure persister includes mutations (not just queries)

    3. Create `mobile/src/hooks/useOffline.ts`:
       - Subscribe to NetInfo state
       - Return { isOffline: boolean, isReconnecting: boolean, pendingCount: number }
       - isReconnecting = was offline + just came online + pending mutations > 0
       - pendingCount from usePendingMutations

    4. Create `mobile/src/components/OfflineBanner.tsx`:
       - Uses useOffline hook
       - When offline: red banner at top "You're offline. Messages will be sent when you reconnect."
       - When reconnecting: yellow banner "Syncing {pendingCount} pending messages..."
       - When online with no pending: hidden
       - Animated slide-in/slide-out

    5. Update `mobile/src/hooks/useChat.ts`:
       - Change sendMessage to use useMutation with mutationKey: ['sendMessage']
       - Add onMutate: optimistic update adds user message to list immediately with pending indicator
       - Add onSettled: invalidate conversation queries
       - networkMode: 'offlineFirst' -- mutation queues automatically when offline
       - Add isPending/pendingCount to returned state for UI indicators

    6. Wire OfflineBanner into root layout (app/_layout.tsx):
       - Render OfflineBanner above Slot, below any status bar
       - Only show when authenticated (inside auth guard)
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/mobile && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Offline banner appears when device loses connectivity. Chat messages queue with pending indicator when offline. Pending mutations survive app restart via AsyncStorage persister. Mutations replay automatically on reconnect.</done>
</task>

<task type="auto">
  <name>Task 2: Approvals screen with swipe gestures + quick actions</name>
  <files>
    mobile/src/hooks/useApprovals.ts
    mobile/src/components/ApprovalCard.tsx
    mobile/src/components/QuickAction.tsx
    mobile/app/(tabs)/approvals.tsx
    mobile/app/approval/[id].tsx
  </files>
  <action>
    Build the approvals screen with swipeable cards for quick approve/reject actions.

    1. Create `mobile/src/hooks/useApprovals.ts`:
       - useApprovals(): TanStack useQuery fetching GET /api/agent/approvals?status=pending
         Returns { approvals, isLoading, refetch }
       - useResolveApproval(): TanStack useMutation calling POST /api/agent/approvals
         Body: { id, decision: 'approved' | 'rejected' }
         mutationKey: ['resolveApproval'] (for offline persistence)
         networkMode: 'offlineFirst'
         onMutate: optimistic remove from list
         onError: roll back optimistic update
         onSettled: invalidate approvals query

    2. Create `mobile/src/components/ApprovalCard.tsx`:
       - Swipeable card using react-native-gesture-handler Swipeable component
       - Swipe right (left-to-right): green "Approve" action background
       - Swipe left (right-to-left): red "Reject" action background
       - On swipe complete: Haptics.impactAsync(Medium), call onApprove or onReject
       - Card content: title, body preview (2 lines), priority badge (urgent=red, normal=blue, low=gray), timestamp
       - Tap card (not swipe): navigate to approval/[id] detail

    3. Create `mobile/src/components/QuickAction.tsx`:
       - Reusable quick action button with icon, label, and onPress
       - Haptic feedback on press (ImpactFeedbackStyle.Light)
       - Pressable with scale animation on press-in (Animated API)
       - Used in approval detail and potentially chat for common operations
       - Variants: 'approve' (green), 'reject' (red), 'reply' (blue), 'snooze' (gray)

    4. Update `mobile/app/(tabs)/approvals.tsx`:
       - FlatList of ApprovalCard items from useApprovals
       - Pull-to-refresh
       - Empty state: "All caught up! No pending approvals."
       - Section header showing count: "3 pending approvals"
       - Badge on tab bar shows approval count (wire into tab _layout.tsx)

    5. Create `mobile/app/approval/[id].tsx`:
       - Full approval detail screen
       - Shows: title, full body text, action type, action payload summary, priority, created timestamp
       - QuickAction buttons at bottom: Approve (green) and Reject (red)
       - Back button to return to approvals list
       - After action: navigate back with success haptic

    6. Update tab _layout.tsx to show badge count on Approvals tab:
       - Use useApprovals to get count
       - tabBarBadge={count > 0 ? count : undefined}
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/mobile && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Approvals tab shows pending approval cards. Swiping right approves with green action reveal and haptic. Swiping left rejects with red action reveal and haptic. Tapping opens detail view with full context. Approve/reject buttons in detail work. Approval badge shows on tab. Offline approvals queue for later.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end mobile app verification on device</name>
  <files>mobile/app.json</files>
  <action>
    Human verifies the complete mobile app works end-to-end on a device or simulator. This is a verification checkpoint, not an implementation task. All code was built in Tasks 1-2 (this plan) and Plans 01-03.
  </action>
  <verify>Manual device testing per how-to-verify steps below</verify>
  <done>All 10 verification steps pass on device/simulator. User approves the mobile app experience.</done>
  <what-built>
    Complete mobile app with:
    - Login + Supabase auth + tab navigation (Plan 01)
    - Chat with SSE streaming + voice input (Plan 02)
    - Push notifications end-to-end (Plan 03)
    - Offline queue + swipeable approvals (Plan 04)
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd mobile && npx expo start`
    2. Open on iOS Simulator (press 'i') or scan QR on physical device
    3. Verify login flow: enter existing Supabase credentials, should land on Chat tab
    4. Verify chat: send a message, verify streaming response appears token-by-token
    5. Verify voice: tap mic button, speak, verify transcription appears in input
    6. Verify approvals tab: shows pending approvals (if any), try swiping to approve/reject
    7. Verify activity tab: shows notification history
    8. Verify offline: toggle airplane mode, send a message, see "offline" banner and pending indicator
    9. Reconnect: turn off airplane mode, verify pending message sends
    10. Verify settings: sign out button works, returns to login screen
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found during device testing</resume-signal>
</task>

</tasks>

<verification>
- `cd mobile && npx tsc --noEmit` passes
- Offline banner shows/hides based on connectivity
- Chat messages queue offline and send on reconnect
- Approval swipe gestures produce haptic feedback
- Approval badge count updates on resolve
- Approval mutations work offline (optimistic remove, queue replay)
</verification>

<success_criteria>
- Offline mutations (chat messages, approvals) survive app restart via AsyncStorage
- NetInfo wired to TanStack onlineManager for automatic offline detection
- OfflineBanner shows contextual status (offline, syncing, online)
- Swipe-to-approve/reject uses react-native-gesture-handler (native thread, no JS jank)
- Haptic feedback on all quick actions via expo-haptics
- Approval badge count on tab bar reflects pending count
- Full end-to-end mobile app verified working on device/simulator
</success_criteria>

<output>
After completion, create `.planning/phases/36-mobile-experience/36-04-SUMMARY.md`
</output>
