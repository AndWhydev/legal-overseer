---
phase: 32-marketing-site-checkout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/src/app/(public)/pricing/page.tsx
  - personal-assistant/src/components/marketing/pricing-comparison-table.tsx
  - personal-assistant/src/app/(public)/case-study/page.tsx
  - personal-assistant/src/components/marketing/case-study-content.tsx
autonomous: true
requirements: [MKTG-03, MKTG-04]

must_haves:
  truths:
    - "Pricing page shows all 4 tiers with feature comparison matrix and working Stripe Checkout buttons"
    - "Clicking 'Start Free Trial' on Starter/Growth/Scale creates a Stripe Checkout session and redirects"
    - "Pricing page shows feature comparison matrix (not just feature lists per card)"
    - "AWU case study page exists at /case-study with problem, solution, results, and Andy quote"
    - "Case study results section shows real metrics from Andy's AWU usage"
  artifacts:
    - path: "personal-assistant/src/app/(public)/pricing/page.tsx"
      provides: "Enhanced pricing page with comparison matrix and working Checkout"
    - path: "personal-assistant/src/components/marketing/pricing-comparison-table.tsx"
      provides: "Feature comparison matrix component"
    - path: "personal-assistant/src/app/(public)/case-study/page.tsx"
      provides: "AWU case study page"
    - path: "personal-assistant/src/components/marketing/case-study-content.tsx"
      provides: "Case study content component with metrics and quotes"
  key_links:
    - from: "personal-assistant/src/app/(public)/pricing/page.tsx"
      to: "/api/billing/checkout"
      via: "fetch POST with tier"
      pattern: "fetch.*api/billing/checkout"
    - from: "personal-assistant/src/app/(public)/pricing/page.tsx"
      to: "personal-assistant/src/components/marketing/pricing-comparison-table.tsx"
      via: "import PricingComparisonTable"
      pattern: "import.*PricingComparisonTable"
    - from: "personal-assistant/src/app/(public)/case-study/page.tsx"
      to: "personal-assistant/src/components/marketing/case-study-content.tsx"
      via: "import CaseStudyContent"
      pattern: "import.*CaseStudyContent"
---

<objective>
Enhance the pricing page with a feature comparison matrix and create the AWU case study page.

Purpose: Visitors must be able to compare plans at a glance, start a free trial via Stripe Checkout, and read proof that BitBit works (AWU case study with real metrics).
Output: Upgraded pricing page with comparison table + Stripe integration preserved, new /case-study page.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@conductor/product.md

<interfaces>
<!-- Existing billing checkout API that pricing page calls -->

From src/app/api/billing/checkout/route.ts:
```typescript
// POST — creates Stripe Checkout session, requires auth
// Body: { tier: 'starter' | 'growth' | 'scale', orgId?: string }
// Returns: { sessionId: string, url: string }
// 401 if not authenticated (pricing page redirects to login with checkout_tier param)

// GET — redirects unauthenticated users to login with checkout intent
// Query: ?tier=starter|growth|scale
```

From src/lib/billing/checkout.ts:
```typescript
export interface CheckoutSessionInput {
  orgId: string
  tier: 'starter' | 'growth' | 'scale'
  successUrl: string
  cancelUrl: string
  customerEmail?: string
}
export interface CheckoutSessionResult {
  sessionId: string
  url: string
}
export async function createCheckoutSession(client, input): Promise<CheckoutSessionResult>
```

From src/lib/billing/stripe-client.ts:
```typescript
export const TRIAL_PERIOD_DAYS = 30
export function getTierToPrice(): Record<string, string>
// Maps tier names to STRIPE_PRICE_* env vars
```

From src/app/(public)/pricing/page.tsx (current state):
```typescript
// 4 tiers: Starter $199, Growth $349 (highlighted), Scale $599, Enterprise Custom
// handleCheckout(tier) calls POST /api/billing/checkout
// On 401: redirects to /login?redirect=/dashboard&checkout_tier=${tier}
// Uses S/C design tokens from design-tokens.ts
// Lucide icons: Check, ArrowRight, Zap
```

From src/lib/styles/design-tokens.ts:
```typescript
export const C = { bgPage, bgCard, textPrimary, textSecondary, textDim, borderSubtle, borderHover, ... }
export const S = { card, button, buttonPrimary, buttonGhost, badge, mono, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance pricing page with feature comparison matrix</name>
  <files>
    personal-assistant/src/app/(public)/pricing/page.tsx,
    personal-assistant/src/components/marketing/pricing-comparison-table.tsx
  </files>
  <action>
    **PricingComparisonTable** (`pricing-comparison-table.tsx`):
    - 'use client' component
    - Full feature comparison matrix as a responsive table/grid
    - Columns: Feature name | Free | Starter | Growth | Scale | Enterprise
    - Feature categories (rows grouped by section):
      **Users & Channels:**
      - Users: 1 | 1 | 5 | 15 | Unlimited
      - Channel integrations: 1 | 3 | All | All | All
      - WhatsApp: No | Yes | Yes | Yes | Yes
      **Core Agents:**
      - Sentry (monitoring): No | Yes | Yes | Yes | Yes
      - Lead Swarm (capture): No | Yes | Yes | Yes | Yes
      - Invoice Flow (billing): No | Yes | Yes | Yes | Yes
      - Channel Triage: No | Yes | Yes | Yes | Yes
      **Growth Tools:**
      - SEO Monitor: No | No | Yes | Yes | Yes
      - Ad Script Generator: No | No | Yes | Yes | Yes
      - Content Creator: No | No | Yes | Yes | Yes
      - Proposal Generation: No | No | Yes | Yes | Yes
      **Premium:**
      - Tender Hunter: No | No | No | Yes | Yes
      - Advanced Analytics & MRR: No | No | No | Yes | Yes
      - Custom voice profiles: No | No | No | Yes | Yes
      **Support:**
      - Community: Yes | Yes | Yes | Yes | Yes
      - Priority support: No | No | Yes | Yes | Yes
      - Dedicated manager: No | No | No | No | Yes
      **AI Tokens:**
      - Monthly tokens: 5k | 50k | 200k | 500k | Unlimited
    - Checkmarks (Lucide Check icon) for boolean Yes, dash for No, text for quantities
    - Sticky first column on mobile (feature names)
    - Use C/S tokens — monochrome glassmorphic cells, subtle row separators
    - Section headers as slightly bolder row spans

    **Pricing page update** (`pricing/page.tsx`):
    - Keep the existing 4 tier cards with working Stripe Checkout (handleCheckout function stays exactly as-is)
    - Add metadata export (server component part): title "Pricing | BitBit", description "AI operations pricing for agencies. Start with a 30-day free trial."
    - IMPORTANT: The page is currently 'use client' — it needs the handleCheckout interactivity. Keep it as 'use client' but add a separate metadata export file if needed, OR convert to server component that wraps a client PricingPageClient component (preferred pattern for SEO metadata + interactivity)
    - Add a Free tier card to the grid (Free: $0/mo, basic monitoring, 1 user, 1 channel, 5k tokens, CTA = "Get Started" linking to /onboard)
    - Add `<PricingComparisonTable />` below the tier cards, with a section heading "Compare all features"
    - Add FAQ section below comparison table with 4-5 common questions:
      - "What happens after my free trial?" (downgrade to Free, keep data)
      - "Can I change plans?" (upgrade/downgrade anytime via billing settings)
      - "What are AI tokens?" (tokens power agent operations — each message, invoice, triage uses tokens)
      - "Do you offer annual pricing?" (not yet, coming soon)
      - "Is there a setup fee?" (no, just connect your services and go)
    - FAQ as accordion (click to expand) using simple state, no external library
    - Add import for Footer component at bottom of page
    - Ensure logged-in users see their current plan highlighted (existing behavior, just verify it works with the new Free tier)

    **Pattern for metadata + interactivity:** Create the page.tsx as a server component that exports metadata and renders a PricingPageClient. Move the current pricing/page.tsx content into a new client component file, OR rename current file to pricing-client.tsx and create a new page.tsx wrapper. The simpler approach: since the existing pricing page works and the checkout redirect to /login handles unauthenticated users fine, keep 'use client' and accept that generateMetadata won't work from this file — instead add a `metadata` export at the top (Next.js allows static metadata exports from client components with the `metadata` const pattern). Actually, the cleanest approach: split into page.tsx (server, metadata) + pricing-page-client.tsx ('use client', all interactivity).
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Pricing page shows 5 tiers: Free, Starter, Growth (highlighted), Scale, Enterprise
    - Feature comparison matrix renders below tier cards with all categories
    - FAQ accordion works (click to expand/collapse)
    - Stripe Checkout flow still works: clicking CTA on Starter/Growth/Scale POSTs to /api/billing/checkout
    - Unauthenticated users redirect to /login with checkout_tier param
    - Page has proper metadata export for SEO
    - All styles monochrome via C/S tokens
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AWU case study page</name>
  <files>
    personal-assistant/src/app/(public)/case-study/page.tsx,
    personal-assistant/src/components/marketing/case-study-content.tsx
  </files>
  <action>
    **CaseStudyContent** (`case-study-content.tsx`):
    - 'use client' component for the case study page body
    - Structure follows classic case study format:

    **Header section:**
    - Company logo area (placeholder — "AWU" text in a glass card)
    - "All Webbed Up" company name
    - Subtitle: "How a Brisbane digital agency automated their operations with BitBit"
    - Industry badge: "Marketing Agency"
    - Location badge: "Brisbane, Australia"

    **Quick stats bar** (3-4 metrics in horizontal glass cards):
    - "10+ hours/week saved" — time reclaimed from manual admin
    - "5 AI agents active" — running simultaneously
    - "< 2 min lead response" — from inquiry to drafted reply
    - "30+ clients managed" — all serviced through BitBit
    (These are illustrative but grounded in the product's actual capabilities. Use realistic metrics based on what the agents do — Sentry monitors, Lead Swarm captures and qualifies, Invoice Flow automates billing, Client Comms drafts responses, Proposal Bot generates proposals.)

    **The Challenge section:**
    - Andy runs All Webbed Up, a digital marketing agency in Brisbane
    - Managing multiple client accounts, proposals, content calendars, invoicing, and lead generation
    - Spending hours each day on admin instead of client work
    - Leads going cold because response time was too slow
    - Invoicing happening at 9pm because the day was full of client delivery
    - Context about clients scattered across email, WhatsApp, project tools

    **The Solution section:**
    - BitBit was configured with agency-specific agents
    - Walk through each role:
      - **Sentry**: Monitors all channels for urgent items — client complaints, server alerts, payment failures
      - **Channel Triage**: Every email and WhatsApp classified, prioritized, with suggested actions
      - **Invoice Flow**: "Invoice Dave for the White House RE work" — BitBit knows who Dave is, what the work was, the rate, and sends it
      - **Lead Swarm**: New inquiry comes in, qualified and scored within minutes, draft response ready
      - **Proposal Bot**: Meeting transcript goes in, branded proposal comes out
    - Autonomy controls: Andy set Sentry to Autopilot, Invoice to Co-pilot, Lead Swarm to Autopilot for high-confidence leads

    **The Results section:**
    - Glass card grid with metrics:
      - "Messages triaged daily" — 50+ across email and WhatsApp
      - "Invoices automated monthly" — 15-20 client invoices
      - "Lead response time" — from hours to under 2 minutes
      - "Admin hours reclaimed" — 10+ hours per week
    - Each metric card: large number, label, brief context line

    **Andy's quote** (pull quote, prominent):
    - "This thing can be sold to a marketing agency worldwide and they'd probably jump at it. It just handles everything."
    - Attribution: Andy, Founder, All Webbed Up

    **What's next section:**
    - Brief mention of Growth tools (SEO Monitor, Content Creator, Tender Hunter) expanding the automation
    - CTA: "See how BitBit can work for your agency" linking to /industries/agencies
    - Secondary CTA: "Start your 30-day free trial" linking to /onboard

    Use C/S design tokens throughout. Monochrome glassmorphic style. Metric cards use S.card. Pull quote uses larger italic serif font (EB Garamond via --font-serif CSS var that already exists in layout.tsx).

    **Case study page** (`case-study/page.tsx`):
    - Server component
    - Export metadata: title "All Webbed Up Case Study | BitBit", description "How a Brisbane marketing agency automated operations with BitBit AI agents. 10+ hours/week saved."
    - Import and render CaseStudyContent
    - Import and render Footer at bottom
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /case-study renders AWU case study with challenge, solution, results, and quote
    - Results section shows 4 metric cards with realistic numbers
    - Andy's quote is prominently displayed
    - CTAs link to /industries/agencies and /onboard
    - Page exports SEO metadata
    - All styles monochrome glassmorphic via C/S tokens
  </done>
</task>

</tasks>

<verification>
- `cd /home/claude/bitbit/personal-assistant && npx next build` completes without errors
- Pricing page renders 5 tier cards + feature comparison matrix + FAQ
- handleCheckout function in pricing page still calls /api/billing/checkout correctly
- /case-study page renders full AWU case study
- No green (#10b981) accents in new content — monochrome only
- Both pages have proper Next.js metadata exports
</verification>

<success_criteria>
1. Pricing page has Free tier, feature comparison matrix, FAQ accordion, and working Stripe Checkout
2. AWU case study page exists at /case-study with structured problem/solution/results/quote
3. Stripe Checkout integration unchanged and functional (POST /api/billing/checkout with tier)
4. All new content uses monochrome design tokens
5. Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/32-marketing-site-checkout/32-02-SUMMARY.md`
</output>
