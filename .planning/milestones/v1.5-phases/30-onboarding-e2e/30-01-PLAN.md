---
phase: 30-onboarding-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/e2e/onboarding.spec.ts
  - personal-assistant/src/app/(auth)/onboard/page.tsx
  - personal-assistant/src/lib/onboarding/state.ts
  - personal-assistant/src/lib/onboarding/analytics.ts
  - personal-assistant/src/app/api/onboarding/route.test.ts
  - personal-assistant/src/lib/onboarding/first-run-discovery.test.ts
  - personal-assistant/src/lib/onboarding/welcome-conversation.test.ts
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-04, ONBD-05]

must_haves:
  truths:
    - "New user hitting /onboard follows workspace -> connections -> sync -> agents -> value stages without dead-ends"
    - "Each onboarding stage emits a trackable analytics event"
    - "OAuth callback returns user to /onboard with connected parameter preserved"
    - "Connection progress persists across browser refresh during onboarding"
    - "E2E test passes against current page copy and validates the full happy path"
    - "Onboarding failures at any step show actionable retry guidance"
    - "First-run discovery pipeline has unit test coverage for core extraction logic"
    - "Welcome conversation generator has unit test coverage for template output"
  artifacts:
    - path: "personal-assistant/e2e/onboarding.spec.ts"
      provides: "E2E happy path test covering all FR requirements"
      min_lines: 80
    - path: "personal-assistant/src/app/(auth)/onboard/page.tsx"
      provides: "Onboarding wizard page with all 5 stages"
    - path: "personal-assistant/src/lib/onboarding/state.ts"
      provides: "Onboarding state management (completion checks, routing)"
    - path: "personal-assistant/src/lib/onboarding/first-run-discovery.test.ts"
      provides: "Unit tests for discovery pipeline (ONBD-02 retroactive verification)"
      min_lines: 40
    - path: "personal-assistant/src/lib/onboarding/welcome-conversation.test.ts"
      provides: "Unit tests for welcome message generation (ONBD-05 retroactive verification)"
      min_lines: 40
  key_links:
    - from: "personal-assistant/e2e/onboarding.spec.ts"
      to: "/onboard page"
      via: "Playwright navigation and assertions"
      pattern: "page\\.goto.*onboard"
    - from: "personal-assistant/src/app/(auth)/onboard/page.tsx"
      to: "/api/onboarding"
      via: "fetch POST for workspace creation"
      pattern: "fetch.*api/onboarding"
    - from: "personal-assistant/src/app/(auth)/onboard/page.tsx"
      to: "/api/onboarding/first-value"
      via: "fetch GET for first-value artifact"
      pattern: "fetch.*first-value"
---

<objective>
Verify all 12 FR requirements from T010 onboarding spec pass end-to-end, harden the connection wizard flow with persistent progress, and create retroactive unit tests for Plan 03's discovery and welcome conversation code.

Purpose: New users must complete signup -> channel connection -> first interaction in under 5 minutes. The E2E test must validate this happy path against the current implementation, and any gaps must be closed. Unit tests for ONBD-02 and ONBD-05 close the Wave 0 gap left by Plan 03's execution.
Output: Updated E2E test that passes, hardened onboarding wizard with persistent progress, unit tests for discovery and welcome conversation.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.5-REQUIREMENTS.md
@conductor/tracks/T010/spec.md

@personal-assistant/src/app/(auth)/onboard/page.tsx
@personal-assistant/src/lib/onboarding/state.ts
@personal-assistant/src/lib/onboarding/analytics.ts
@personal-assistant/src/components/onboarding/stage-progress.tsx
@personal-assistant/e2e/onboarding.spec.ts
@personal-assistant/src/app/api/onboarding/route.ts
@personal-assistant/src/app/api/onboarding/first-value/route.ts
@personal-assistant/src/lib/onboarding/first-run-discovery.ts
@personal-assistant/src/lib/onboarding/welcome-conversation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 unit tests for discovery and welcome conversation (retroactive ONBD-02, ONBD-05)</name>
  <files>
    personal-assistant/src/lib/onboarding/first-run-discovery.test.ts,
    personal-assistant/src/lib/onboarding/welcome-conversation.test.ts
  </files>
  <action>
Plan 03 implemented `first-run-discovery.ts` and `welcome-conversation.ts` but shipped without unit tests. These are retroactive Wave 0 test scaffolds for ONBD-02 and ONBD-05.

**Create `first-run-discovery.test.ts`:**

The `runFirstRunDiscovery()` function requires a Supabase client and calls `crawlAllChannels()`, so the unit tests should mock the crawl dependency and test the pure extraction logic. Import `runFirstRunDiscovery` and mock `./intelligence-crawl` module.

Test cases:
1. **Extracts user identity from sent email headers** -- mock crawl returning sent messages with a From address. Verify `userIdentity.email` matches the most common sent-from address, and `userIdentity.name` is extracted from the From header.
2. **Builds contact frequency map** -- mock crawl with messages from multiple senders. Verify `topContacts` is sorted by `messageCount` descending, limited to 10, and includes correct `lastContact` dates.
3. **Groups active threads by subject** -- mock crawl with messages sharing subjects (including Re:/Fwd: prefixes). Verify `activeThreads` groups them correctly by stripped subject, and `needsReply` is true when the last message is received (not sent).
4. **Computes insights** -- mock crawl with a mix of replied/unreplied threads. Verify `insights.emailsNeedingReply` and `insights.overdueFollowUps` counts.
5. **Handles empty crawl** -- mock crawl returning zero messages. Verify function returns empty arrays and zeroed stats without crashing.
6. **Reports progress callbacks** -- mock crawl and verify the `onProgress` callback is called with `phase: 'scanning'` then `phase: 'analyzing'`.

Mock the Supabase client with `vi.fn()` stubs for the `.from().upsert()` chain (used for contact upsert) and `.from().update()` (used for preferences).

**Create `welcome-conversation.test.ts`:**

`generateWelcomeMessage()` and `generateFallbackWelcomeMessage()` are pure functions -- no mocking needed.

Test cases:
1. **Rich message with contacts, threads, and insights** -- provide full `WelcomeMessageInput` with 3 contacts, 2 threads (one needsReply), and insights `{ emailsNeedingReply: 2, overdueFollowUps: 1, ... }`. Verify output contains the top contact's name (bold), the thread subject needing reply, and the follow-up thread subject.
2. **Fallback when no useful data** -- provide input with empty `topContacts`, `activeThreads`, and zeroed insights. Verify output matches the "connected to {channels} and ready to start learning" fallback.
3. **Mentions specific contact names** -- provide a contact named "Dave Johnson" and verify the output contains "**Dave Johnson**".
4. **Handles single email needing reply** -- verify singular grammar: "1 email needs a reply" not "1 emails need a reply".
5. **Handles multiple emails needing reply** -- verify plural grammar: "3 emails need a reply".
6. **generateFallbackWelcomeMessage returns generic message** -- verify it mentions "Connect your email from Settings".
7. **Message stays under 150 words** -- provide a rich input and verify `output.split(/\s+/).length <= 150`.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx vitest run src/lib/onboarding/first-run-discovery.test.ts src/lib/onboarding/welcome-conversation.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Unit tests for first-run-discovery.ts pass (identity extraction, contact ranking, thread grouping, insights computation, empty crawl, progress callbacks). Unit tests for welcome-conversation.ts pass (rich message, fallback, contact names, singular/plural grammar, word count, fallback message).</done>
</task>

<task type="auto">
  <name>Task 1: Audit T010 FRs and fix onboarding wizard gaps</name>
  <files>
    personal-assistant/src/app/(auth)/onboard/page.tsx,
    personal-assistant/src/lib/onboarding/state.ts,
    personal-assistant/src/lib/onboarding/analytics.ts,
    personal-assistant/src/app/api/onboarding/route.ts
  </files>
  <action>
Systematically audit all 12 T010 functional requirements against current implementation. For each FR, verify it passes or fix the gap:

**FR-1 (Beta access model):** Verify /login and /onboard copy says "beta" not "sign up free". If login page says generic signup text, update to reflect invite-only beta access.

**FR-2 (Single routing model):** Verify `hasCompletedFirstRunOnboarding()` in state.ts gates correctly. First-time users must go to /onboard, returning users to /dashboard. Check middleware routing if it exists.

**FR-3 (Minimal workspace confirmation):** Already implemented -- workspace stage collects business name, owner name, industry. No billing shown. Confirm no regression.

**FR-4 (User-to-workspace assignment):** Verify /api/onboarding POST correctly creates org and attaches user profile. Test the `confirmExistingWorkspace` path.

**FR-5 (Canonical connections surface):** Verify onboarding uses `ConnectionsGrid` with `variant="onboarding"` and does NOT route to legacy settings/channels pages. Check all copy says "Connections" not "Channels".

**FR-6 (First connection success handling):** Verify OAuth callback returns to `/onboard?connected={channel}` and the page detects the `connected` query param. Ensure the connection status indicator updates.

**FR-7 (Learning progress state):** Verify the sync stage shows progress lines and the sync request fires. The current implementation fires `/api/channels/sync` -- confirm this actually triggers background processing.

**FR-8 (Personalized first-value artifact):** Verify the value stage calls `/api/onboarding/first-value` and displays real data (email subject, calendar event). If first-value returns null, show a meaningful fallback, not empty.

**FR-9 (Guided next steps):** Verify the value stage shows the 3 next-step cards ("Ask BitBit", "Add more sources", "Just use it"). These already exist -- confirm they render.

**FR-10 (Recoverable failure handling):** Add or verify error handling at each stage:
  - Workspace save fails -> show error message with retry
  - OAuth callback fails -> show error with "Try again" button
  - Sync fails -> show warning but allow continuing
  - First-value fetch fails -> show generic but encouraging fallback

Currently the page has `errorMsg` state but it's only used in workspace and value stages. Ensure each stage has error recovery. For the connections stage, add a subtle "Having trouble?" link that points to mailto:hi@bitbit.chat.

**FR-11 (Support visibility):** The page already has a fixed "Need help?" mailto link at bottom-right. Confirm it's visible on all stages.

**FR-12 (Instrumented funnel):** Verify `trackOnboardingEvent()` is called at each stage transition. The current analytics.ts tracks: onboarding_started, workspace_completed, connections_entered, connection_succeeded, connections_skipped, sync_started, agents_viewed, agents_completed, value_viewed, onboarding_completed. Verify these are actually called at the right points. Add `onboarding_error` event type to analytics.ts and emit it on any error.

**Connection wizard progress persistence (ONBD-04):** The current implementation stores onboarding completion in profile preferences but does NOT persist mid-wizard progress. Add progress persistence:
  1. When user completes workspace stage, the profile already gets `workspace_setup_completed: true` via API
  2. When user connects a channel, the `connected` query param handles return -- this works
  3. Add `onboarding_stage` to the preferences PATCH call so if user refreshes mid-wizard, the page resumes at the correct stage. On the bootstrap useEffect, check `preferences.onboarding_stage` and resume from there instead of always starting at workspace/connections.

Persist stage to server on each transition:
```typescript
// After each setStage(newStage), also persist:
fetch('/api/profile/preferences', {
  method: 'PATCH',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ onboarding_stage: newStage }),
}).catch(() => {}) // best effort
```

On load in bootstrap, read `preferences.onboarding_stage` and resume from that stage if it's not 'value' and onboarding isn't completed.
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx vitest run src/lib/onboarding/state.test.ts --reporter=verbose 2>&1 | tail -20 && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>All 12 FR requirements from T010 pass. Onboarding wizard persists progress across refresh. Error states at every step show actionable guidance. Analytics events fire at every transition including errors. State routing unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update E2E test to validate current onboarding flow</name>
  <files>
    personal-assistant/e2e/onboarding.spec.ts,
    personal-assistant/src/app/api/onboarding/route.test.ts
  </files>
  <action>
The current E2E test (e2e/onboarding.spec.ts) has assertions against outdated page copy (e.g., "meet your bitbit", "start with bitbit", "let bitbit learn", "bitbit is taking the first pass"). The current page uses different headings:
- Workspace stage: "Set up your workspace"
- Connections stage: "Connect a source"
- Sync stage: "Scanning your history"
- Agents stage: "Recommended agents"
- Value stage: "You're all set"

Rewrite e2e/onboarding.spec.ts to:

1. **Match current page copy** -- update all heading assertions to match the actual text in onboard/page.tsx

2. **Test the full happy path with mocked APIs:**
   - Mock /api/onboarding POST to return { orgId, ownerId, rlsConfigured: true }
   - Mock /api/channels/status to return no channels initially, then gmail connected
   - Mock /api/channels/sync POST to return { ok: true }
   - Mock /api/onboarding/first-value GET to return a realistic first-value object
   - Mock /api/profile/preferences PATCH to return 200

3. **Stage-by-stage assertions:**
   - Workspace: fill business name + owner name, submit, verify transition to connections
   - Connections: simulate OAuth return via /onboard?connected=gmail, verify connection indicator
   - Sync: verify progress lines appear and transition to agents
   - Agents: verify recommendations render, click continue
   - Value: verify "You're all set" and "What BitBit knows" sections render, verify first-value data displayed
   - Final: click "Open chat", verify redirect to /dashboard

4. **Test skippable connections:** Add a second test case where user clicks "Skip for now" on connections stage and verifies the flow still completes.

5. **Test progress persistence (ONBD-04):** Add a test named "progress persistence" that:
   - Completes workspace stage (mock preferences PATCH to return 200)
   - Navigates away from /onboard (e.g., to /login or reload)
   - Returns to /onboard with preferences mock returning `{ onboarding_stage: 'connections' }`
   - Verifies the wizard resumes at the connections stage (not workspace)
   - This is the persistence round-trip: write stage -> reload -> read stage -> resume

6. **Test error recovery (FR-10):** Add a test that mocks /api/onboarding POST returning 500 and verifies the error message is displayed with retry guidance.

Also update the API route test (route.test.ts) to verify:
- Workspace creation returns correct shape
- confirmExistingWorkspace works for returning users
- Invalid input returns 400
  </action>
  <verify>
    <automated>cd /home/claude/bitbit/personal-assistant && npx playwright test e2e/onboarding.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>E2E test passes against current onboarding implementation. Test covers: full happy path with all 5 stages, skippable connections, progress persistence across refresh (test named "progress persistence" verifies stage write -> reload -> stage resume at connections), and error recovery. API route tests verify workspace creation and error handling.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no new errors
- `npx vitest run src/lib/onboarding/` passes all unit tests (state, discovery, welcome-conversation)
- E2E onboarding test passes with mocked API routes
- Manual: navigate to /onboard as a new user, complete all stages, verify redirect to /dashboard
</verification>

<success_criteria>
- All 12 T010 FRs verified working in current implementation
- E2E test covers full happy path, skip flow, persistence, and error recovery
- Connection wizard persists progress across browser refresh
- Every onboarding stage has actionable error handling
- Analytics events fire at every stage transition
- Unit tests cover first-run-discovery extraction logic and welcome-conversation template generation
</success_criteria>

<output>
After completion, create `.planning/phases/30-onboarding-e2e/30-01-SUMMARY.md`
</output>
