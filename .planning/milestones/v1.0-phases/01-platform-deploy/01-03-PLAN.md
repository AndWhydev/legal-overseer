---
phase: 01-platform-deploy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/index.ts
  - packages/core/package.json
autonomous: true
requirements:
  - AGNT-14

must_haves:
  truths:
    - "@bitbit/core imports resolve without errors"
    - "npm install at monorepo root succeeds"
    - "TypeScript compilation of packages/core succeeds with no errors"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "Clean public API — only exports modules that exist on disk"
      min_lines: 5
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/types.ts"
      via: "export type statement"
      pattern: "export.*from.*types"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/agent-registry.ts"
      via: "export statement"
      pattern: "export.*from.*agent-registry"
---

<objective>
Audit @bitbit/core exports, remove references to non-existent modules, and verify the monorepo resolves cleanly.

Purpose: @bitbit/core/src/index.ts exports from 8 modules (engine, model-router, orchestrator, tools, confidence, policies, channels) that don't exist yet — only types.ts and agent-registry.ts exist. This breaks `npm install` and blocks all downstream development.
Output: Clean @bitbit/core with only valid exports. Monorepo builds without errors.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/index.ts
@packages/core/src/types.ts
@packages/core/src/agent-registry.ts
@packages/core/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix @bitbit/core exports</name>
  <files>packages/core/src/index.ts</files>
  <action>
    **Step 1: Identify what exists vs what's exported.**

    Current index.ts exports from 8 modules:
    - `./engine` — DOES NOT EXIST
    - `./model-router` — DOES NOT EXIST
    - `./orchestrator` — DOES NOT EXIST
    - `./tools` — DOES NOT EXIST
    - `./confidence` — DOES NOT EXIST
    - `./policies` — DOES NOT EXIST
    - `./agent-registry` — EXISTS
    - `./channels` — DOES NOT EXIST
    - `./types` — EXISTS

    **Step 2: Rewrite index.ts to only export what exists.**

    Keep:
    - `export { registerAgent, getAgent, listAgents, type AgentDefinition } from './agent-registry'`
    - All type exports from `./types`

    Remove all other export lines (engine, model-router, orchestrator, tools, confidence, policies, channels).

    Add a comment block at the top documenting the planned exports for future phases:
    ```typescript
    /**
     * @bitbit/core — Shared types and registries
     *
     * Currently exports: types + agent-registry
     * Future (Phase 4+): engine, model-router, orchestrator, tools, confidence, policies, channels
     */
    ```

    **Step 3: Verify agent-registry.ts exports match** what index.ts re-exports. Read agent-registry.ts and confirm `registerAgent`, `getAgent`, `listAgents`, and `AgentDefinition` all exist.

    **Step 4: Verify types.ts exports match.** Confirm all 10 type exports (Organization, Contact, Lead, Invoice, Proposal, AgentConfig, AgentRun, Watch, VoiceProfile, Template, OfferPackage) exist in types.ts. Remove any that don't exist in types.ts.
  </action>
  <verify>
    ```bash
    cd packages/core && npx tsc --noEmit
    ```
    Should exit 0 with no errors.
  </verify>
  <done>packages/core/src/index.ts only exports from modules that exist on disk. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Verify monorepo resolution</name>
  <files>packages/core/package.json</files>
  <action>
    **Step 1: Run npm install at monorepo root.**
    ```bash
    cd /home/claude/bitbit && npm install
    ```

    If it fails due to @bitbit/core resolution, check:
    - Root package.json has `workspaces` including `packages/*`
    - packages/core/package.json has correct `name: "@bitbit/core"`
    - No circular dependencies

    **Step 2: Verify imports from other workspaces.**

    Check if any workspace (personal-assistant, deployments/awu) imports from @bitbit/core:
    ```bash
    grep -r "@bitbit/core" personal-assistant/ deployments/ --include="*.ts" --include="*.tsx" -l
    ```

    For each file found, verify the specific imports they use still exist in the cleaned-up index.ts. If they import removed modules (engine, model-router, etc.), those imports need to be commented out or removed.

    **Step 3: Fix any broken imports** in consuming packages by either:
    - Removing unused imports of not-yet-built modules
    - Adding `// @ts-expect-error — module not yet implemented` if code needs the import shape but module doesn't exist

    Prefer removing unused imports. Only use ts-expect-error if the import is genuinely needed for runtime code.
  </action>
  <verify>
    ```bash
    cd /home/claude/bitbit && npm install && cd packages/core && npx tsc --noEmit && echo "SUCCESS"
    ```
    Both commands succeed without errors.
  </verify>
  <done>npm install succeeds at monorepo root. @bitbit/core compiles. No broken imports in consuming packages.</done>
</task>

</tasks>

<verification>
1. `npm install` at repo root exits 0
2. `npx tsc --noEmit` in packages/core exits 0
3. No TypeScript errors referencing non-existent @bitbit/core modules
4. `grep -r "from '@bitbit/core'" --include="*.ts"` shows only valid imports
</verification>

<success_criteria>
- @bitbit/core exports only existing modules (types + agent-registry)
- Monorepo npm install succeeds
- TypeScript compilation passes for packages/core
- No broken imports in any workspace
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-deploy/01-03-SUMMARY.md`
</output>
