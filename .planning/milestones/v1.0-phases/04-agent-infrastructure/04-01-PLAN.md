---
phase: 04-agent-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/agent-registry.ts
  - packages/core/src/index.ts
  - packages/core/src/types.ts
  - personal-assistant/src/lib/agent/registry-loader.ts
autonomous: true
requirements: [AGNT-11]

must_haves:
  truths:
    - "An agent module can call registerAgent() and appear in the registry without manual configuration"
    - "Registry persists agent definitions with DB-backed config merged over code defaults"
    - "listAgents() returns all registered agents sorted by priority"
    - "getAgentConfig() returns merged config (code defaults + DB overrides) for a given agent type and org"
  artifacts:
    - path: "packages/core/src/agent-registry.ts"
      provides: "Enhanced registry with DB config merge and validation"
      exports: ["registerAgent", "getAgent", "listAgents", "getAgentConfig", "getRegisteredTypes"]
    - path: "personal-assistant/src/lib/agent/registry-loader.ts"
      provides: "Auto-discovery that imports all agent packages to trigger self-registration"
      exports: ["loadAllAgents", "getAgentWithConfig"]
  key_links:
    - from: "packages/agents/*/index.ts"
      to: "packages/core/src/agent-registry.ts"
      via: "registerAgent() call on import"
      pattern: "registerAgent\\("
    - from: "personal-assistant/src/lib/agent/registry-loader.ts"
      to: "packages/agents/*/index.ts"
      via: "dynamic imports triggering self-registration"
      pattern: "import.*@bitbit/agent"
---

<objective>
Build the agent registry with self-registration pattern so any agent module can register itself on import and have its config merged with DB-level overrides.

Purpose: Agents need a central discovery mechanism. The existing `agent-registry.ts` in `@bitbit/core` has the skeleton but lacks DB config merging, validation, and auto-discovery. This plan enhances it to production readiness.

Output: Enhanced registry in `@bitbit/core`, registry loader in `personal-assistant` that auto-discovers all agent packages.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/agent-registry.ts
@packages/core/src/types.ts
@packages/core/src/index.ts
@packages/agents/lead-swarm/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance agent registry with validation, DB config merge, and type queries</name>
  <files>
    packages/core/src/agent-registry.ts
    packages/core/src/types.ts
    packages/core/src/index.ts
  </files>
  <action>
Enhance `packages/core/src/agent-registry.ts`:

1. Add `validateDefinition(def: AgentDefinition): string[]` — returns array of validation errors (empty = valid). Check: type non-empty, name non-empty, version matches semver pattern, handler non-empty, confidence thresholds (act > ask, both 0-1).

2. Update `registerAgent()` to call validateDefinition and log warnings for invalid definitions (still register, but warn). Add a `registered_at: Date` timestamp to each entry.

3. Add `getRegisteredTypes(): AgentType[]` — returns all registered agent types.

4. Add `getAgentConfig(type: AgentType, orgId: string, dbConfigs: AgentConfig[]): AgentConfig & { definition: AgentDefinition }` — merges code-level defaults from the AgentDefinition with the DB-level AgentConfig for the given org. DB values override code defaults. If no DB config exists, synthesize one from definition defaults. The `dbConfigs` parameter is passed in (not fetched here) to keep the registry pure/sync.

5. In `packages/core/src/types.ts`, add `AgentRegistryEntry` type:
```ts
export interface AgentRegistryEntry {
  definition: AgentDefinition
  config: AgentConfig
  registered_at: Date
}
```

6. Update `packages/core/src/index.ts` to export the new functions: `getRegisteredTypes`, `getAgentConfig`, `validateDefinition`, and the `AgentRegistryEntry` type.

Keep existing `registerAgent`, `getAgent`, `listAgents` signatures backward-compatible.
  </action>
  <verify>
Run `npx tsc --noEmit -p packages/core/tsconfig.json` — no type errors. Verify exports with `node -e "const m = require('./packages/core/dist/index.js'); console.log(Object.keys(m))"` after building.
  </verify>
  <done>
Registry validates definitions on register, merges DB configs over code defaults, and exports getRegisteredTypes + getAgentConfig. All existing agent index.ts files still compile against the updated registry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create registry loader with auto-discovery and org-scoped config fetch</name>
  <files>
    personal-assistant/src/lib/agent/registry-loader.ts
  </files>
  <action>
Create `personal-assistant/src/lib/agent/registry-loader.ts`:

1. `loadAllAgents(): void` — imports all agent packages to trigger self-registration. Use static imports (not dynamic) since the agent list is known at build time:
```ts
import '@bitbit/agent-lead-swarm'
import '@bitbit/agent-invoice-flow'
import '@bitbit/agent-channel-triage'
import '@bitbit/agent-client-comms'
import '@bitbit/agent-proposal-bot'
import '@bitbit/agent-ad-script-gen'
import '@bitbit/agent-client-onboarding'
import '@bitbit/agent-ai-search-optimizer'
import '@bitbit/agent-tender-hunter'
import '@bitbit/agent-sentry'
```
Note: These imports will fail silently if packages aren't resolvable yet — wrap each in try/catch. After imports, log `[registry] Loaded N agents: [types]`.

2. `getAgentWithConfig(type: AgentType, orgId: string): Promise<AgentRegistryEntry | null>` — fetches the DB `agent_configs` row for (type, orgId) via Supabase, then calls `getAgentConfig()` from core to merge. Returns null if agent type not registered.

3. `listAgentsWithConfig(orgId: string): Promise<AgentRegistryEntry[]>` — for all registered agents, fetches their org configs and returns merged entries.

Use `createClient` from `@/lib/supabase/server` for DB access. Follow the fire-and-forget error pattern from Phase 3 for non-critical failures (log but don't throw).
  </action>
  <verify>
Run `npx tsc --noEmit -p personal-assistant/tsconfig.json` — no type errors. The file should compile cleanly against the enhanced registry exports.
  </verify>
  <done>
Registry loader auto-discovers all agent packages, merges DB configs, and provides org-scoped agent config queries. An agent module calling registerAgent() appears in listAgents() without any manual wiring.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/core/tsconfig.json` passes
2. `npx tsc --noEmit -p personal-assistant/tsconfig.json` passes
3. `getRegisteredTypes()` returns array of AgentType strings after loadAllAgents()
4. `getAgentConfig()` correctly merges DB overrides over code defaults
</verification>

<success_criteria>
- Agent self-registration pattern works: import triggers registerAgent()
- Registry validates definitions and warns on issues
- DB config merges over code defaults with correct precedence
- All registered agents discoverable via listAgents() and getRegisteredTypes()
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-infrastructure/04-01-SUMMARY.md`
</output>
