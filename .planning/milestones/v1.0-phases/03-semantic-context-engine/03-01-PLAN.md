---
phase: 03-semantic-context-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/src/lib/context/relationship-linker.ts
  - personal-assistant/src/lib/context/timeline-writer.ts
  - personal-assistant/src/lib/context/types.ts
  - personal-assistant/src/lib/channels/synthesizer.ts
autonomous: true
requirements:
  - SCTX-05
  - SCTX-06

must_haves:
  truths:
    - "Creating a task via the agent tool also inserts an entity_relationship record linking the task to its org"
    - "Updating a task status writes a task_updated event to entity_timeline"
    - "Every channel message processed by the synthesizer writes a message_received event to entity_timeline"
    - "Creating or updating a contact writes a contact_created or contact_updated event to entity_timeline"
    - "Creating an invoice auto-creates entity_relationships linking invoice to client contact"
  artifacts:
    - path: "personal-assistant/src/lib/context/relationship-linker.ts"
      provides: "Auto-creates entity_relationship records on CRUD operations"
      exports: ["linkTaskToContact", "linkInvoiceToContact", "linkRelationship"]
    - path: "personal-assistant/src/lib/context/timeline-writer.ts"
      provides: "Writes entity_timeline entries for all tracked events"
      exports: ["writeTimelineEvent", "writeTaskEvent", "writeContactEvent", "writeInvoiceEvent", "writeMessageEvent"]
    - path: "personal-assistant/src/lib/context/types.ts"
      provides: "Shared types for semantic context module"
      contains: "EntityType"
  key_links:
    - from: "personal-assistant/src/lib/agent/tools.ts"
      to: "personal-assistant/src/lib/context/relationship-linker.ts"
      via: "import and call in create_task, update_task, search_contacts handlers"
      pattern: "linkTaskToContact|writeTimelineEvent"
    - from: "personal-assistant/src/lib/context/relationship-linker.ts"
      to: "entity_relationships table"
      via: "supabase.from('entity_relationships').upsert()"
      pattern: "entity_relationships"
    - from: "personal-assistant/src/lib/context/timeline-writer.ts"
      to: "entity_timeline table"
      via: "supabase.from('entity_timeline').insert()"
      pattern: "entity_timeline"
    - from: "personal-assistant/src/lib/channels/synthesizer.ts"
      to: "personal-assistant/src/lib/context/timeline-writer.ts"
      via: "import writeMessageEvent, call in message processing loop"
      pattern: "writeMessageEvent"
---

<objective>
Build the relationship auto-linker and timeline writer — the two write-path modules that populate the semantic context tables whenever CRUD operations occur.

Purpose: Every action in BitBit (task creation, contact update, invoice generation, channel message) must automatically create relationship edges and timeline events so the context engine has data to query.

Output: Two modules (relationship-linker.ts, timeline-writer.ts) integrated into existing agent tool handlers.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@personal-assistant/src/lib/agent/tools.ts
@personal-assistant/src/lib/context/loader.ts
@personal-assistant/supabase/migrations/005_entity_relationships.sql
@personal-assistant/supabase/migrations/006_entity_timeline.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared types and relationship auto-linker module</name>
  <files>
    personal-assistant/src/lib/context/types.ts
    personal-assistant/src/lib/context/relationship-linker.ts
  </files>
  <action>
    Create `personal-assistant/src/lib/context/types.ts` with:
    - `EntityType` = 'contact' | 'task' | 'invoice' | 'project' | 'channel_message' | 'goal' (matches the CHECK constraint in 005_entity_relationships.sql)
    - `RelationshipType` = 'works_on' | 'bills_for' | 'assigned_to' | 'mentioned_in' | 'related_to' | 'manages' | 'created_by' | 'client_of' | 'vendor_for' | 'part_of'
    - `TimelineEventType` matching the CHECK in 006_entity_timeline.sql
    - Interface `EntityRef` = { type: EntityType, id: string }

    Create `personal-assistant/src/lib/context/relationship-linker.ts` with:
    - Uses `createClient` from `@/lib/supabase/server` (same pattern as entity-resolver.ts)
    - `linkRelationship(orgId, entityA: EntityRef, entityB: EntityRef, relType: RelationshipType, metadata?: Record<string, unknown>)` — upserts into entity_relationships using the UNIQUE constraint (ON CONFLICT updates strength +0.1 capped at 5.0 and last_evidence_at = now())
    - `linkTaskToContact(orgId, taskId, contactId)` — creates 'assigned_to' relationship
    - `linkInvoiceToContact(orgId, invoiceId, contactId)` — creates 'bills_for' relationship
    - `linkTaskToGoal(orgId, taskId, goalId)` — creates 'part_of' relationship
    - All functions are fire-and-forget (log errors but don't throw) to avoid disrupting the main CRUD flow
    - Export all functions
  </action>
  <verify>
    Run: `cd /home/claude/bitbit && npx tsc --noEmit --project personal-assistant/tsconfig.json 2>&1 | head -30` to confirm no type errors. If tsconfig doesn't exist at that path, check the actual tsconfig location and adapt.
  </verify>
  <done>types.ts exports EntityType, RelationshipType, TimelineEventType; relationship-linker.ts exports linkRelationship, linkTaskToContact, linkInvoiceToContact, linkTaskToGoal; all compile cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create timeline writer and integrate both modules into agent tools</name>
  <files>
    personal-assistant/src/lib/context/timeline-writer.ts
    personal-assistant/src/lib/agent/tools.ts
    personal-assistant/src/lib/channels/synthesizer.ts
  </files>
  <action>
    Create `personal-assistant/src/lib/context/timeline-writer.ts` with:
    - Uses `createClient` from `@/lib/supabase/server`
    - `writeTimelineEvent(orgId, entityType: EntityType, entityId: string, eventType: TimelineEventType, eventData: Record<string, unknown>, channelSource?: string, relatedEntity?: EntityRef)` — inserts into entity_timeline
    - Convenience wrappers:
      - `writeTaskEvent(orgId, taskId, eventType: 'task_created'|'task_updated'|'task_completed', data)`
      - `writeContactEvent(orgId, contactId, eventType: 'contact_created'|'contact_updated', data)`
      - `writeInvoiceEvent(orgId, invoiceId, eventType: 'invoice_created'|'invoice_sent'|'invoice_paid'|'invoice_overdue', data)`
      - `writeMessageEvent(orgId, messageId, direction: 'inbound'|'outbound', channelSource: string, data, contactId?: string)`
    - All fire-and-forget (catch + console.error, never throw)

    Integrate into `personal-assistant/src/lib/agent/tools.ts`:
    - Import `writeTaskEvent` and `linkTaskToContact` at the top
    - In `create_task` handler: after successful insert, call `writeTaskEvent(orgId, data.id, 'task_created', { title: input.title, column: input.column, priority: input.priority })`. If input includes a contact slug or contact_id, also call `linkTaskToContact`.
    - In `update_task` handler: after successful update, call `writeTaskEvent(orgId, input.task_id, 'task_updated', updates)`. If status is 'completed', also write 'task_completed'.
    - In `log_activity` handler: no change needed (activity_feed is separate from entity_timeline)
    - Do NOT modify search-only handlers (search_tasks, search_contacts, get_contact, search_memory) — reads don't generate events

    Wire `writeMessageEvent` into channel message ingest path in `personal-assistant/src/lib/channels/synthesizer.ts`:
    - Import `writeMessageEvent` from `@/lib/context/timeline-writer`
    - Inside the `for (const msg of actionable)` loop (after the task insert block around line 198), call `writeMessageEvent(options.orgId, msg.externalId || crypto.randomUUID(), msg.direction === 'outbound' ? 'outbound' : 'inbound', msg.channel, { sender: msg.sender, subject: msg.subject, bodyPreview: msg.body.slice(0, 200) }, undefined)` for every processed message (not just task-created ones)
    - Move the writeMessageEvent call OUTSIDE the `if (supabase && toDoColumnId && actionable.length > 0)` block — place it in a separate loop over `unique` (all deduplicated messages, not just actionable) so ALL channel messages get timeline events, not just actionable ones
    - This is fire-and-forget — catch errors, log, don't throw
  </action>
  <verify>
    Run: `cd /home/claude/bitbit && npx tsc --noEmit --project personal-assistant/tsconfig.json 2>&1 | head -30` to confirm no type errors. Verify that tools.ts imports are present: `grep -n 'writeTaskEvent\|linkTaskToContact' personal-assistant/src/lib/agent/tools.ts`. Verify synthesizer wiring: `grep -n 'writeMessageEvent' personal-assistant/src/lib/channels/synthesizer.ts`
  </verify>
  <done>timeline-writer.ts exports all 5 functions; tools.ts create_task handler writes timeline event after insert; tools.ts update_task handler writes timeline event after update; synthesizer.ts calls writeMessageEvent for every processed channel message; all compile cleanly</done>
</task>

</tasks>

<verification>
1. Both new modules compile: `npx tsc --noEmit --project personal-assistant/tsconfig.json`
2. relationship-linker.ts uses upsert with ON CONFLICT for idempotent re-runs
3. timeline-writer.ts inserts append-only (no updates/deletes matching the RLS policy)
4. tools.ts create_task and update_task call the new modules after successful DB operations
5. No existing functionality broken — all fire-and-forget, errors logged not thrown
</verification>

<success_criteria>
- entity_relationships records will be created when tasks are assigned to contacts
- entity_timeline records will be created on every task create/update
- Existing tool behavior unchanged (search, read operations untouched)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-context-engine/03-01-SUMMARY.md`
</output>
