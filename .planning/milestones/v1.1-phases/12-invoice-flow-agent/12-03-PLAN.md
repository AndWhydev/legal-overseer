---
phase: 12-invoice-flow-agent
plan: 03
type: execute
wave: 1
depends_on: [12-01, 12-02]
files_modified:
  - personal-assistant/src/app/api/agent/invoices/route.ts
  - personal-assistant/src/app/api/agent/invoices/[invoiceId]/route.ts
  - personal-assistant/src/components/invoices/InvoiceList.tsx
  - personal-assistant/src/app/(dashboard)/invoices/page.tsx
autonomous: true
requirements: [INVC-01, INVC-02, INVC-03, INVC-04, INVC-05]

must_haves:
  truths:
    - "Dashboard shows invoice list with status, amount, client, and action buttons"
    - "API endpoints expose invoice CRUD for dashboard consumption"
    - "Invoice creation from chat routes through Invoice Flow agent"
  artifacts:
    - path: "personal-assistant/src/app/api/agent/invoices/route.ts"
      provides: "Invoice list and create API endpoints"
    - path: "personal-assistant/src/app/(dashboard)/invoices/page.tsx"
      provides: "Dashboard invoices page"
---

<objective>
Wire Invoice Flow into dashboard with API endpoints and invoices list UI.

Purpose: Andy needs to see and manage invoices from the dashboard. This plan connects the Invoice Flow agent to the user-facing interface.

Output: API endpoints for invoice CRUD and a dashboard invoices page showing all invoices with status filters.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-invoice-flow-agent/12-01-PLAN.md
@.planning/phases/12-invoice-flow-agent/12-02-PLAN.md
@personal-assistant/src/lib/agent/shared-tools.ts
@personal-assistant/src/app/api/agent/leads/route.ts
@personal-assistant/src/app/api/agent/sentry/watches/route.ts
@personal-assistant/src/components/leads/LeadsKanban.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create invoice API endpoints and dashboard page</name>
  <files>
    personal-assistant/src/app/api/agent/invoices/route.ts
    personal-assistant/src/app/api/agent/invoices/[invoiceId]/route.ts
    personal-assistant/src/components/invoices/InvoiceList.tsx
    personal-assistant/src/app/(dashboard)/invoices/page.tsx
  </files>
  <action>
    Create REST API endpoints following existing patterns (leads, sentry watches):

    In `invoices/route.ts`:
    - GET: List invoices for org, support ?status= filter, ?client_contact_id= filter. Use searchInvoices from shared-tools.
    - POST: Create invoice from NL command or structured data. Route through Invoice Flow agent (parseInvoiceIntent → createInvoiceFromIntent). Always queues approval.

    In `invoices/[invoiceId]/route.ts`:
    - GET: Get single invoice by ID
    - PATCH: Update invoice status (mark as paid, cancel). Only allow valid transitions.

    In `InvoiceList.tsx`:
    - Table/list component showing: invoice number, client name, total, status (with color badge), due date, actions
    - Status badges: draft (gray), sent (blue), viewed (purple), overdue (red), paid (green), cancelled (gray strikethrough)
    - Action buttons: Send (for draft), Mark Paid (for sent/viewed/overdue), Cancel
    - Status filter tabs: All, Draft, Sent, Overdue, Paid
    - Use existing Radix UI components and patterns from LeadsKanban/ApprovalQueue

    In `invoices/page.tsx`:
    - Dashboard page that renders InvoiceList
    - Add to SPA tab registry and sidebar navigation (follow approvals/leads pattern)

    Follow existing API patterns:
    - Auth via createRouteHandlerClient
    - Org scoping via user profile lookup
    - JSON responses with proper error handling
  </action>
  <verify>Files compile without TypeScript errors. API routes follow existing patterns.</verify>
  <done>Dashboard shows invoices with full CRUD, status tracking, and NL creation via chat. All INVC requirements verified end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Update navigation and verify Phase 12 completion</name>
  <files>
    personal-assistant/src/components/layout/Sidebar.tsx
    personal-assistant/src/lib/navigation.ts
  </files>
  <action>
    Add Invoices to dashboard navigation:
    - Add invoices tab/link to sidebar navigation (follow existing pattern for leads, approvals, sentry)
    - Icon: receipt or file-text from lucide-react
    - Position: after Leads in navigation order

    Verify all INVC requirements are met:
    - INVC-01: NL invoice creation via chat → Invoice Flow agent
    - INVC-02: Branded HTML/PDF generation
    - INVC-03: Email sending with approval queue
    - INVC-04: Status lifecycle (draft→sent→viewed→overdue→paid)
    - INVC-05: Duplicate detection prevents re-invoicing
  </action>
  <verify>Navigation includes Invoices link. All 5 INVC requirements have code paths.</verify>
  <done>Invoice Flow agent is fully wired into BitBit — dashboard, API, scheduler, approval queue.</done>
</task>

</tasks>

<verification>
- API endpoints return correct JSON responses
- Dashboard page renders invoice list
- Navigation includes Invoices
- All INVC-01 through INVC-05 requirements have implementation code paths
</verification>

<success_criteria>
- All 5 INVC requirements satisfied
- Dashboard invoices page functional
- Invoice Flow fully integrated into BitBit platform
- Phase 12 complete → v1.1 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-invoice-flow-agent/12-03-SUMMARY.md`
</output>
