---
phase: 11-lead-swarm-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - personal-assistant/supabase/migrations/022_lead_swarm_intake.sql
  - personal-assistant/src/lib/agent/lead-swarm.ts
  - personal-assistant/src/lib/agent/lead-swarm.test.ts
  - personal-assistant/src/lib/agent/scheduler.ts
  - personal-assistant/src/lib/agent/scheduler.test.ts
autonomous: true
requirements: [LEAD-01, LEAD-02]

must_haves:
  truths:
    - "Inbound business messages are classified as lead/client/spam/personal before lead actions run"
    - "Qualified leads receive a deterministic hot/warm/cold score from budget, service fit, and timeline"
    - "Lead records are persisted with classification and scoring context for downstream automation"
  artifacts:
    - path: "personal-assistant/src/lib/agent/lead-swarm.ts"
      provides: "Lead intake runtime for classification, qualification, and scoring"
      exports: ["classifyInboundLead", "qualifyLead", "runLeadSwarmTick"]
    - path: "personal-assistant/supabase/migrations/022_lead_swarm_intake.sql"
      provides: "Lead intake persistence fields and indexes for scoring metadata"
      contains: "ALTER TABLE leads"
    - path: "personal-assistant/src/lib/agent/scheduler.ts"
      provides: "Lead Swarm scheduled execution path"
      contains: "runLeadSwarmTick"
  key_links:
    - from: "personal-assistant/src/lib/agent/scheduler.ts"
      to: "personal-assistant/src/lib/agent/lead-swarm.ts"
      via: "due lead-swarm config executes runLeadSwarmTick"
      pattern: "runLeadSwarmTick"
    - from: "personal-assistant/src/lib/agent/lead-swarm.ts"
      to: "channel_messages"
      via: "classification + qualification reads inbound message data"
      pattern: "from\('channel_messages'\)"
---

<objective>
Implement Lead Swarm intake runtime so inbound messages are classified and qualified into persisted lead pipeline records.

Purpose: Phase 11 cannot auto-acknowledge or visualize pipeline flow unless lead intake is deterministic and scheduler-driven, using the existing runtime pattern from Phase 8 and escalation-safe scheduler behavior from Phase 10.

Output: Lead Swarm runtime module, schema migration for lead-intake metadata, and scheduler integration that executes lead processing on due ticks.
</objective>

<execution_context>
@/home/claude/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/claude/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/comprehensive-roadmap.md
@.planning/phases/08-agent-runtime/08-02-SUMMARY.md
@.planning/phases/08-agent-runtime/08-03-SUMMARY.md
@.planning/phases/10-sentry-agent/10-03-SUMMARY.md
@personal-assistant/src/lib/agent/classifier.ts
@personal-assistant/src/lib/agent/action-router.ts
@personal-assistant/src/lib/agent/scheduler.ts
@personal-assistant/supabase/migrations/010_leads.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build lead intake module with classification and qualification scoring</name>
  <files>
    personal-assistant/supabase/migrations/022_lead_swarm_intake.sql
    personal-assistant/src/lib/agent/lead-swarm.ts
    personal-assistant/src/lib/agent/lead-swarm.test.ts
  </files>
  <action>
    Create a Lead Swarm runtime module that processes unhandled inbound channel messages and writes leads.

    In `022_lead_swarm_intake.sql`, extend `leads` with fields required for qualification and downstream automation (for example: source_message_id, classification_label, classification_confidence, estimated_value, timeline_days, ack_status, ack_draft_created_at) and add indexes for org/status/ack-status lookups. Keep migration idempotent with `IF NOT EXISTS` checks.

    In `lead-swarm.ts`, implement:
    - `classifyInboundLead(...)` that maps inbound messages into the phase-required labels (`lead`, `client`, `spam`, `personal`) while preserving existing classifier behavior as fallback.
    - `qualifyLead(...)` that computes hot/warm/cold from budget/service/timeline rules (deterministic rule table; no random or model-only scoring).
    - `runLeadSwarmTick(supabase, orgId, agentConfigId)` that fetches recent unprocessed inbound candidates, classifies them, upserts/creates lead rows for `lead` messages, and stores qualification metadata for later ack/escalation steps.
    - Never-throw batch behavior mirroring sentry/relay patterns: continue on per-message failures and return counts `{ processed, created, qualified, hot, failed }`.

    Add vitest coverage for label mapping, scoring thresholds, and tick-level batch result behavior.
  </action>
  <verify>Run `cd personal-assistant && npx vitest run src/lib/agent/lead-swarm.test.ts`.</verify>
  <done>Inbound messages are consistently classified and qualified, and lead rows persist enough data for ack/escalation and pipeline UI.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Lead Swarm into scheduler ticks and run logging</name>
  <files>
    personal-assistant/src/lib/agent/scheduler.ts
    personal-assistant/src/lib/agent/scheduler.test.ts
  </files>
  <action>
    Extend scheduler execution so due `lead-swarm` agent configs run automatically using existing tick conventions.

    Update scheduler routing logic to:
    - Detect `agent_type === 'lead-swarm'` and invoke `runLeadSwarmTick`.
    - Include deterministic lead metrics in `output_summary` (processed/created/qualified/hot/failed) similar to sentry summary formatting.
    - Preserve per-org dedupe and never-throw semantics already used for sentry execution.

    Add scheduler tests proving:
    - Due lead-swarm configs trigger runtime execution.
    - Non-due configs skip execution.
    - Failure in one lead message does not abort the entire scheduler tick.
  </action>
  <verify>Run `cd personal-assistant && npx vitest run src/lib/agent/scheduler.test.ts src/lib/agent/lead-swarm.test.ts`.</verify>
  <done>Lead Swarm executes automatically on schedule and logs stable run summaries for observability.</done>
</task>

</tasks>

<verification>
- Lead intake migration applies cleanly and preserves existing leads schema compatibility.
- `runLeadSwarmTick` returns deterministic counters and persists qualification metadata.
- Scheduler invokes lead-swarm runtime for due agent configs.
- New runtime and scheduler tests pass.
</verification>

<success_criteria>
- LEAD-01 satisfied: inbound messages are classified as lead/client/spam/personal.
- LEAD-02 satisfied: qualified leads receive hot/warm/cold scores based on budget/service/timeline.
- Lead ingestion runs automatically via scheduler without manual triggering.
</success_criteria>

<output>
After completion, create `.planning/phases/11-lead-swarm-agent/11-01-SUMMARY.md`
</output>
