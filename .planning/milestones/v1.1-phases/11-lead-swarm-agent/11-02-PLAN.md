---
phase: 11-lead-swarm-agent
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - personal-assistant/src/lib/agent/lead-swarm.ts
  - personal-assistant/src/lib/agent/lead-acknowledgment.ts
  - personal-assistant/src/lib/agent/lead-acknowledgment.test.ts
  - personal-assistant/src/app/api/agent/leads/ack/route.ts
autonomous: true
requirements: [LEAD-03, LEAD-04]

must_haves:
  truths:
    - "Qualified leads receive an acknowledgment draft within 2 minutes via approval queue, not direct send"
    - "Acknowledgment messages are sent only after approval resolution"
    - "High-value leads above $5k escalate to Andy immediately through the established notification path"
  artifacts:
    - path: "personal-assistant/src/lib/agent/lead-acknowledgment.ts"
      provides: "Lead ack draft and escalation orchestration"
      exports: ["queueLeadAcknowledgment", "escalateHighValueLead", "processPendingLeadAcks"]
    - path: "personal-assistant/src/app/api/agent/leads/ack/route.ts"
      provides: "Scheduler-authenticated endpoint for SLA-driven lead ack processing"
      exports: ["POST"]
    - path: "personal-assistant/src/lib/agent/lead-swarm.ts"
      provides: "High-value lead detection hook feeding escalation workflow"
      contains: "estimated_value"
  key_links:
    - from: "personal-assistant/src/lib/agent/lead-acknowledgment.ts"
      to: "personal-assistant/src/lib/agent/approval-queue.ts"
      via: "ack draft created as approval record before send"
      pattern: "createApproval|queueAgentAction"
    - from: "personal-assistant/src/lib/agent/lead-acknowledgment.ts"
      to: "personal-assistant/src/lib/agent/approval-notifier.ts"
      via: "urgent high-value leads notify Andy immediately"
      pattern: "notifyApproval"
---

<objective>
Implement approval-gated lead acknowledgments and high-value escalation so Lead Swarm responds quickly without bypassing Andy control.

Purpose: Phase 11 requires both speed and governance; this plan reuses Phase 9 approval/WhatsApp patterns to deliver sub-2-minute draft generation and immediate escalation for leads above $5k.

Output: Lead acknowledgment orchestration module, scheduler-authenticated ack endpoint, and Lead Swarm runtime hooks for high-value escalation.
</objective>

<execution_context>
@/home/claude/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/claude/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/comprehensive-roadmap.md
@.planning/phases/09-approval-flow/09-01-SUMMARY.md
@.planning/phases/09-approval-flow/09-03-SUMMARY.md
@.planning/phases/11-lead-swarm-agent/11-01-SUMMARY.md
@personal-assistant/src/lib/agent/approval-queue.ts
@personal-assistant/src/lib/agent/approval-notifier.ts
@personal-assistant/src/app/api/agent/approvals/digest/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement lead acknowledgment draft + high-value escalation orchestrator</name>
  <files>
    personal-assistant/src/lib/agent/lead-swarm.ts
    personal-assistant/src/lib/agent/lead-acknowledgment.ts
    personal-assistant/src/lib/agent/lead-acknowledgment.test.ts
  </files>
  <action>
    Add a dedicated orchestration module for LEAD-03 and LEAD-04 that reuses approval queue semantics.

    In `lead-acknowledgment.ts`, implement:
    - `queueLeadAcknowledgment(...)` to create an approval draft for qualified leads with `action_type='lead_ack_send'` and payload containing lead id, message channel, and draft body.
    - SLA handling: only queue drafts for qualified leads where `created_at <= now - 2 minutes` is false; mark overdue candidates and include them in result counters.
    - `escalateHighValueLead(...)` when `estimated_value > 5000` (strict threshold), generating urgent approval/escalation records and sending immediate notification via existing notifier flow.
    - `processPendingLeadAcks(...)` to move approved drafts to sent state and record sent timestamp on lead rows.

    Update `lead-swarm.ts` to call the new orchestration hooks after qualification so high-value and ack workflows are driven from the same lead facts.

    Add tests for:
    - Draft creation under 2-minute SLA window.
    - No direct send before approval.
    - `>$5k` leads trigger urgent escalation path.
  </action>
  <verify>Run `cd personal-assistant && npx vitest run src/lib/agent/lead-acknowledgment.test.ts`.</verify>
  <done>Qualified leads generate approval-gated ack drafts within SLA, and high-value leads escalate to Andy immediately.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated lead-ack processing endpoint for scheduled execution</name>
  <files>
    personal-assistant/src/app/api/agent/leads/ack/route.ts
  </files>
  <action>
    Create `POST /api/agent/leads/ack` as a scheduler-authenticated route consistent with existing cron endpoints.

    Route behavior:
    - Require `Authorization: Bearer ${SCHEDULER_SECRET}`.
    - Use service-role Supabase client at HTTP boundary (same pattern as relay/scheduler/digest routes).
    - Execute `processPendingLeadAcks` for the default org runtime pattern and return deterministic counts (queued/sent/escalated/skipped_overdue/failed).
    - Return non-200 on missing env or invalid auth with explicit JSON errors.

    Keep implementation automation-only (no manual dashboard dependency) so cron can run every minute for the 2-minute SLA target.
  </action>
  <verify>Run `cd personal-assistant && npm run lint -- src/app/api/agent/leads/ack/route.ts`.</verify>
  <done>Lead acknowledgment processing can run on a minute-level scheduler with strict auth and deterministic result output.</done>
</task>

</tasks>

<verification>
- Ack drafts are created through approval queue, not sent directly.
- High-value leads (`estimated_value > 5000`) create urgent escalation notifications for Andy.
- `POST /api/agent/leads/ack` enforces scheduler bearer auth.
- Lead acknowledgment tests pass.
</verification>

<success_criteria>
- LEAD-03 satisfied: qualified leads get draft acknowledgments within 2 minutes and send only after approval.
- LEAD-04 satisfied: high-value leads above $5k escalate directly to Andy.
</success_criteria>

<output>
After completion, create `.planning/phases/11-lead-swarm-agent/11-02-SUMMARY.md`
</output>
