---
phase: 11-lead-swarm-agent
plan: 04
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - personal-assistant/src/lib/agent/lead-acknowledgment.ts
  - personal-assistant/src/lib/agent/lead-acknowledgment.test.ts
  - personal-assistant/src/lib/channels/whatsapp.ts
autonomous: true
requirements: [LEAD-03]
gap_closure: true

must_haves:
  truths:
    - "Approved lead acknowledgment actions trigger a real outbound send attempt before a lead can be marked sent"
    - "Lead ack status changes to sent only when outbound provider returns a message id"
    - "Failed outbound sends retain non-sent lead state and persist delivery failure metadata for retry/inspection"
  artifacts:
    - path: "personal-assistant/src/lib/agent/lead-acknowledgment.ts"
      provides: "Approved lead ack execution with delivery attempt, success gating, and delivery result persistence"
      exports: ["processPendingLeadAcks"]
    - path: "personal-assistant/src/lib/agent/lead-acknowledgment.test.ts"
      provides: "Regression coverage for approved send success/failure paths"
      contains: "processPendingLeadAcks"
    - path: "personal-assistant/src/lib/channels/whatsapp.ts"
      provides: "Reusable outbound message sender returning provider message id"
      exports: ["sendMessage"]
  key_links:
    - from: "personal-assistant/src/lib/agent/lead-acknowledgment.ts"
      to: "personal-assistant/src/lib/channels/whatsapp.ts"
      via: "channel-specific outbound send adapter invoked for approved lead_ack_send actions"
      pattern: "sendMessage"
    - from: "personal-assistant/src/lib/agent/lead-acknowledgment.ts"
      to: "leads.metadata"
      via: "ackDelivery fields (provider id, channel, sent/failed timestamps, failure reason) persisted with ack status transitions"
      pattern: "ackDelivery"
---

<objective>
Close the LEAD-03 delivery gap by executing approved acknowledgment actions through an outbound channel send and persisting delivery outcomes.

Purpose: Verification shows the current approved path only flips `ack_status` to `sent` with no actual provider send, which breaks the phase truth for real lead acknowledgment delivery.

Output: Delivery-attempt execution in `processPendingLeadAcks`, success-gated sent transitions, persisted provider/failure metadata, and tests proving both success and failure behavior.
</objective>

<execution_context>
@/home/claude/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/claude/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-lead-swarm-agent/11-VERIFICATION.md
@.planning/phases/11-lead-swarm-agent/11-02-SUMMARY.md
@personal-assistant/src/lib/agent/lead-acknowledgment.ts
@personal-assistant/src/lib/agent/lead-acknowledgment.test.ts
@personal-assistant/src/lib/channels/whatsapp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute approved lead_ack_send actions through outbound channel delivery before marking sent</name>
  <files>
    personal-assistant/src/lib/agent/lead-acknowledgment.ts
    personal-assistant/src/lib/channels/whatsapp.ts
  </files>
  <action>
    Replace the state-only approved branch in `processPendingLeadAcks` with a real delivery execution step.

    Implementation requirements:
    - Add a channel delivery helper that consumes approval payload fields (`message_channel`, `recipient`, `draft_body`) and attempts outbound send through existing provider adapters.
    - Use `sendMessage` from `whatsapp.ts` for WhatsApp sends; return explicit unsupported-channel failure for channels without outbound support instead of silently succeeding.
    - Require provider response message id to treat delivery as success.
    - Only set `ack_status='sent'` when delivery succeeds.
    - Persist delivery metadata on lead row for both outcomes in `metadata.ackDelivery`:
      - success: `status`, `providerMessageId`, `channel`, `sentAt`, `approvalId`
      - failure: `status`, `channel`, `failedAt`, `reason`, `approvalId`
    - Keep failure path non-sent (`draft_queued` or `pending`) so retries are possible and verifier can observe unresolved sends.
    - Increment `result.sent` only on success; increment `result.failed` when payload is invalid, channel unsupported, recipient missing, or provider send fails.
  </action>
  <verify>Run `cd personal-assistant && npm run lint -- src/lib/agent/lead-acknowledgment.ts src/lib/channels/whatsapp.ts`.</verify>
  <done>Approved lead ack actions now perform a real outbound delivery attempt and only successful deliveries transition leads to sent.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for approved-send success/failure delivery outcomes</name>
  <files>
    personal-assistant/src/lib/agent/lead-acknowledgment.test.ts
    personal-assistant/src/lib/agent/lead-acknowledgment.ts
  </files>
  <action>
    Extend `lead-acknowledgment.test.ts` with approved-action scenarios that fail under the old implementation and pass after delivery wiring.

    Required cases:
    - Approved ack with valid delivery payload + mocked provider message id marks lead sent and stores success metadata.
    - Approved ack with provider failure (null/exception) does not mark sent, increments failed counter, and stores failure reason metadata.
    - Approved ack with unsupported channel or missing recipient does not mark sent and records structured failure metadata.

    Update mock Supabase state helpers as needed to capture metadata updates and assertions on `ackDelivery` contents.
  </action>
  <verify>Run `cd personal-assistant && npx vitest run src/lib/agent/lead-acknowledgment.test.ts`.</verify>
  <done>Tests prove LEAD-03 send behavior: successful provider delivery is required for sent state, and failures are persisted for retry visibility.</done>
</task>

</tasks>

<verification>
- `processPendingLeadAcks` invokes outbound send logic for approved `lead_ack_send` records before any sent-state transition.
- Lead rows are marked `ack_status='sent'` only when provider message id is returned.
- Delivery failures keep leads non-sent and persist failure metadata (`reason`, timestamp, approval id, channel).
- `lead-acknowledgment` test suite passes with new approved-send success/failure coverage.
</verification>

<success_criteria>
- LEAD-03 gap closed: approved lead acknowledgments are actually delivered through an outbound channel, not just state-transitioned.
- Verification evidence can show provider delivery id on success and explicit failure reason on unsuccessful delivery attempts.
</success_criteria>

<output>
After completion, create `.planning/phases/11-lead-swarm-agent/11-04-SUMMARY.md`
</output>
