---
phase: 10-sentry-agent
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - personal-assistant/src/components/sentry/watch-manager.tsx
  - personal-assistant/src/app/dashboard/sentry/page.tsx
autonomous: true
requirements: [SNTR-04]
gap_closure: true

must_haves:
  truths:
    - "Andy can create a watch from the dashboard"
    - "Andy can pause/resume and delete existing watches from the dashboard"
    - "Dashboard view exposes active alerts and allows acknowledgment"
  artifacts:
    - path: "personal-assistant/src/components/sentry/watch-manager.tsx"
      provides: "Sentry watch CRUD and alert ack interface"
      min_lines: 120
    - path: "personal-assistant/src/app/dashboard/sentry/page.tsx"
      provides: "Dashboard entry route for Sentry operations"
      contains: "WatchManager"
  key_links:
    - from: "personal-assistant/src/components/sentry/watch-manager.tsx"
      to: "/api/agent/sentry/watches"
      via: "fetch list/create/pause/delete watch actions"
      pattern: "fetch\('/api/agent/sentry/watches'"
    - from: "personal-assistant/src/components/sentry/watch-manager.tsx"
      to: "/api/agent/sentry/alerts"
      via: "fetch list and acknowledge alert actions"
      pattern: "fetch\('/api/agent/sentry/alerts'"
---

<objective>
Close SNTR-04 gaps by shipping the missing Sentry dashboard page and watch management UI referenced as absent in verification.

Purpose: SNTR-04 remains blocked because no Sentry dashboard files exist. This plan delivers a complete operator UI for watch lifecycle and alert acknowledgment using the APIs from 10-03.

Output: Dashboard Sentry page and watch manager component with create/pause/delete watch actions and alert ack controls.
</objective>

<execution_context>
@/home/claude/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/claude/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-sentry-agent/10-VERIFICATION.md
@.planning/phases/10-sentry-agent/10-03-SUMMARY.md
@docs/comprehensive-roadmap.md
@personal-assistant/src/components/channels/channel-grid.tsx
@personal-assistant/src/app/dashboard/approvals/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build watch manager UI with watch lifecycle and alert acknowledgment</name>
  <files>
    personal-assistant/src/components/sentry/watch-manager.tsx
  </files>
  <action>
    Create `watch-manager.tsx` as a client component that implements the missing Sentry controls from verification.

    Required behavior:
    - On mount, fetch watches and alerts from `/api/agent/sentry/watches` and `/api/agent/sentry/alerts`.
    - Render watch cards showing type, description, status, interval, escalation minutes, and last checked timestamp.
    - Provide watch creation form with fields for watch type, description, conditions, interval seconds, and escalation minutes.
    - Support pause/resume toggle (`PATCH`), delete action (`DELETE`), and optimistic refresh after each mutation.
    - Render pending/escalated alerts list with acknowledge button that sends `PATCH` to `/api/agent/sentry/alerts`.
    - Reuse existing dashboard UI primitives and feedback patterns already used in channel/approval components; do not add a new UI dependency.

    Handle loading, empty states, and API errors with visible inline feedback so failures are operator-visible.
  </action>
  <verify>Run `cd personal-assistant && npx tsc --noEmit src/components/sentry/watch-manager.tsx`.</verify>
  <done>Watch manager supports create, pause/resume, delete, and alert acknowledgment via real API calls.</done>
</task>

<task type="auto">
  <name>Task 2: Add dashboard Sentry route and page composition</name>
  <files>
    personal-assistant/src/app/dashboard/sentry/page.tsx
  </files>
  <action>
    Create `/dashboard/sentry` page that mounts `WatchManager` and follows current dashboard page conventions.

    Include:
    - Page heading and short operational description.
    - Consistent spacing and card layout with existing dashboard sections.
    - Stable SSR-safe composition with client component import.

    Ensure route is directly accessible under dashboard and ready for human UAT of SNTR-04.
  </action>
  <verify>Run `cd personal-assistant && npx tsc --noEmit src/app/dashboard/sentry/page.tsx src/components/sentry/watch-manager.tsx`.</verify>
  <done>Dashboard route `/dashboard/sentry` renders the watch manager and exposes watch lifecycle controls to Andy.</done>
</task>

</tasks>

<verification>
- `/dashboard/sentry` exists and renders watch manager.
- Watch create/pause/delete flows hit `/api/agent/sentry/watches`.
- Alert ack flow hits `/api/agent/sentry/alerts` and updates UI state.
- TypeScript checks pass for new Sentry dashboard files.
</verification>

<success_criteria>
- SNTR-04 is met: dashboard supports create, pause, and delete watch lifecycle.
- Andy can acknowledge live alerts from the dashboard.
- Missing dashboard artifacts called out in 10-VERIFICATION are now implemented in this plan scope.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sentry-agent/10-04-SUMMARY.md`
</output>
