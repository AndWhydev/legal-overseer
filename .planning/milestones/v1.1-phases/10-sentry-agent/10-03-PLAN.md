---
phase: 10-sentry-agent
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - personal-assistant/src/lib/agent/sentry-escalation.ts
  - personal-assistant/src/lib/agent/sentry-escalation.test.ts
  - personal-assistant/src/app/api/agent/sentry/watches/route.ts
  - personal-assistant/src/app/api/agent/sentry/alerts/route.ts
  - personal-assistant/src/lib/agent/scheduler.ts
  - personal-assistant/src/lib/agent/scheduler.test.ts
autonomous: true
requirements: [SNTR-03]
gap_closure: true

must_haves:
  truths:
    - "Unacknowledged alerts escalate automatically inside each watch escalation window"
    - "Acknowledging an alert prevents additional escalations for that alert"
    - "Escalation runs from scheduler ticks without requiring manual API calls"
  artifacts:
    - path: "personal-assistant/src/lib/agent/sentry-escalation.ts"
      provides: "Escalation processor and acknowledgment helpers"
      exports: ["processSentryEscalations", "acknowledgeSentryAlert"]
    - path: "personal-assistant/src/app/api/agent/sentry/alerts/route.ts"
      provides: "Alerts API for list, acknowledge, and escalation tick"
      exports: ["GET", "PATCH", "POST"]
    - path: "personal-assistant/src/lib/agent/scheduler.ts"
      provides: "Automatic escalation invocation on due sentry configs"
      contains: "processSentryEscalations"
  key_links:
    - from: "personal-assistant/src/lib/agent/scheduler.ts"
      to: "personal-assistant/src/lib/agent/sentry-escalation.ts"
      via: "scheduler tick invokes escalation processor per sentry org"
      pattern: "processSentryEscalations"
    - from: "personal-assistant/src/app/api/agent/sentry/alerts/route.ts"
      to: "personal-assistant/src/lib/agent/sentry-escalation.ts"
      via: "PATCH acknowledge action calls acknowledgeSentryAlert"
      pattern: "acknowledgeSentryAlert"
---

<objective>
Close SNTR-03 gaps by implementing escalation runtime and API wiring that verification reported missing from phase 10.

Purpose: Phase 10 currently detects issues but does not escalate unattended alerts. This plan restores phase-goal reliability by adding scheduler-driven escalation and explicit acknowledgment handling.

Output: Escalation module, alerts/watch API routes, and scheduler integration with tests proving unattended escalation behavior.
</objective>

<execution_context>
@/home/claude/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/claude/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-sentry-agent/10-VERIFICATION.md
@.planning/phases/10-sentry-agent/10-01-SUMMARY.md
@docs/comprehensive-roadmap.md
@personal-assistant/src/lib/agent/sentry.ts
@personal-assistant/src/lib/agent/approval-queue.ts
@personal-assistant/src/app/api/agent/approvals/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement escalation processor and acknowledgment behavior</name>
  <files>
    personal-assistant/src/lib/agent/sentry-escalation.ts
    personal-assistant/src/lib/agent/sentry-escalation.test.ts
  </files>
  <action>
    Create `sentry-escalation.ts` to process due alerts and stop escalation after acknowledgment.

    Implement `processSentryEscalations(supabase, orgId)` to:
    - Select alerts with `status in ('pending','escalated')`, `acknowledged_at is null`, and `next_escalation_at <= now()`.
    - Join watch escalation policy so the next escalation window uses watch-level `escalation_minutes`.
    - Create an urgent approval-queue notification for each due alert containing issue summary, evidence, and remediation suggestion.
    - Update each escalated alert with `status='escalated'`, incremented `escalation_count`, `escalated_at=now`, and recalculated `next_escalation_at`.
    - Return deterministic counts `{ processed, escalated, failed }` and never throw from batch processing.

    Implement `acknowledgeSentryAlert(supabase, alertId, userId)` to:
    - Set `status='acknowledged'`, `acknowledged_by`, `acknowledged_at`, and clear `next_escalation_at`.
    - Return explicit not-found / already-acknowledged errors for API handlers.

    Add vitest coverage for due escalation, next-window scheduling, and exclusion of acknowledged alerts.
  </action>
  <verify>Run `cd personal-assistant && npx vitest run src/lib/agent/sentry-escalation.test.ts`.</verify>
  <done>Due unacknowledged alerts escalate and acknowledged alerts are excluded from future escalation runs.</done>
</task>

<task type="auto">
  <name>Task 2: Wire escalation through APIs and scheduler automation</name>
  <files>
    personal-assistant/src/app/api/agent/sentry/watches/route.ts
    personal-assistant/src/app/api/agent/sentry/alerts/route.ts
    personal-assistant/src/lib/agent/scheduler.ts
    personal-assistant/src/lib/agent/scheduler.test.ts
  </files>
  <action>
    Build missing Sentry API routes and scheduler calls called out by verification gaps.

    In `/api/agent/sentry/watches`:
    - Implement authenticated org-scoped `GET`, `POST`, `PATCH`, `DELETE` handlers for watch lifecycle.
    - Validate watch type, interval bounds, and escalation minutes; return 400 for invalid payloads.

    In `/api/agent/sentry/alerts`:
    - Implement `GET` for recent alerts (default pending + escalated).
    - Implement `PATCH` for `{ action: 'acknowledge', alertId }` that calls `acknowledgeSentryAlert`.
    - Implement `POST` for explicit escalation processing hooks by calling `processSentryEscalations`.

    In scheduler:
    - For due sentry configs, run `processSentryEscalations` in addition to `runSentryTick`.
    - Keep never-throw scheduler behavior and process each org once per tick to avoid duplicate escalations.
    - Add tests proving escalation runs automatically for due sentry orgs and skipped when no alerts are due.
  </action>
  <verify>Run `cd personal-assistant && npx vitest run src/lib/agent/scheduler.test.ts src/lib/agent/sentry-escalation.test.ts`.</verify>
  <done>Scheduler and APIs both invoke the escalation path, and unattended alerts escalate automatically without manual intervention.</done>
</task>

</tasks>

<verification>
- `processSentryEscalations` exists and escalates only due, unacknowledged alerts.
- `/api/agent/sentry/alerts` supports acknowledge and escalation endpoints.
- Scheduler executes escalation processing for due sentry configs.
- New escalation and scheduler tests pass.
</verification>

<success_criteria>
- SNTR-03 is met: unattended alerts escalate within configured windows.
- Acknowledged alerts stop escalating.
- Escalation behavior is automatic and test-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sentry-agent/10-03-SUMMARY.md`
</output>
