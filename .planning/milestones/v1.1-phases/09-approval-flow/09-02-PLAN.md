---
phase: 09-approval-flow
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - personal-assistant/src/app/dashboard/approvals/page.tsx
  - personal-assistant/src/components/dashboard/approval-card.tsx
  - personal-assistant/src/components/dashboard/approval-queue.tsx
autonomous: true
requirements: [APPR-02]

must_haves:
  truths:
    - "Dashboard shows a list of pending agent actions with context, confidence score, and agent name"
    - "Andy can approve an action by clicking an approve button"
    - "Andy can reject an action by clicking a reject button"
    - "Approved/rejected actions disappear from the pending queue"
  artifacts:
    - path: "personal-assistant/src/app/dashboard/approvals/page.tsx"
      provides: "Approvals dashboard page"
      min_lines: 20
    - path: "personal-assistant/src/components/dashboard/approval-card.tsx"
      provides: "Individual approval card with action summary, confidence, approve/reject buttons"
      min_lines: 30
    - path: "personal-assistant/src/components/dashboard/approval-queue.tsx"
      provides: "Queue list component fetching from /api/agent/approvals"
      min_lines: 40
  key_links:
    - from: "personal-assistant/src/components/dashboard/approval-queue.tsx"
      to: "/api/agent/approvals"
      via: "fetch GET for pending list, PATCH for resolve"
      pattern: "fetch.*api/agent/approvals"
    - from: "personal-assistant/src/app/dashboard/approvals/page.tsx"
      to: "personal-assistant/src/components/dashboard/approval-queue.tsx"
      via: "renders ApprovalQueue component"
      pattern: "ApprovalQueue"
---

<objective>
Build the dashboard approval queue UI where Andy can see pending agent actions and approve or reject them.

Purpose: Andy needs a visual queue with enough context to make quick approve/reject decisions. Each card shows what the agent wants to do, why (confidence score), and relevant entity context.

Output: Dashboard page at /dashboard/approvals with approval cards showing pending actions.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-approval-flow/09-01-SUMMARY.md
@personal-assistant/src/app/dashboard/layout.tsx
@personal-assistant/src/app/dashboard/activity/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create approval card and queue components</name>
  <files>
    personal-assistant/src/components/dashboard/approval-card.tsx
    personal-assistant/src/components/dashboard/approval-queue.tsx
  </files>
  <action>
    Create approval-card.tsx as a client component ('use client'):
    - Props: approval object (id, action_summary, action_type, confidence_score, routing_decision, priority, created_at, context_snapshot, agent_name from join)
    - Display: agent name badge, action_summary as main text, confidence score as percentage with color coding (red <0.55, yellow 0.55-0.85), priority badge (urgent=red, normal=yellow, low=gray), time since created (relative)
    - Context section: if context_snapshot has entity info, show key fields (contact name, project, amounts)
    - Two buttons: "Approve" (green) and "Reject" (red outline)
    - onApprove and onReject callback props
    - Loading state while resolving (disable buttons, show spinner)
    - Use existing Tailwind patterns from the project (check activity/page.tsx or contacts/page.tsx for styling patterns)

    Create approval-queue.tsx as a client component ('use client'):
    - Fetch pending approvals from GET /api/agent/approvals on mount
    - Display count badge showing number of pending items
    - Render ApprovalCard for each pending approval
    - Handle approve/reject by calling PATCH /api/agent/approvals with approvalId and decision
    - Optimistically remove resolved card from list
    - Empty state: "No pending approvals" message
    - Error state: show error with retry button
    - Auto-refresh every 30 seconds (setInterval with cleanup)
    - Filter tabs: "All", "Urgent", "Normal" to filter by priority
  </action>
  <verify>TypeScript compiles: cd personal-assistant && npx tsc --noEmit src/components/dashboard/approval-card.tsx src/components/dashboard/approval-queue.tsx</verify>
  <done>ApprovalCard renders action details with approve/reject buttons, ApprovalQueue fetches and displays pending approvals with auto-refresh and filtering</done>
</task>

<task type="auto">
  <name>Task 2: Create approvals dashboard page and add navigation</name>
  <files>
    personal-assistant/src/app/dashboard/approvals/page.tsx
  </files>
  <action>
    Create page.tsx at /dashboard/approvals:
    - Page title "Approval Queue"
    - Render ApprovalQueue component
    - Follow same page structure as other dashboard pages (activity, contacts, etc.)

    Update the dashboard navigation to include "Approvals" link. Find the navigation component (likely in SPAShell or a sidebar component referenced from layout.tsx). Add navigation item for /dashboard/approvals with a shield/check icon. If the nav uses an items array, add to it. Include a pending count badge if feasible (can be a simple client-side fetch).

    Note: Read SPAShell component first to understand navigation pattern before modifying.
  </action>
  <verify>TypeScript compiles: cd personal-assistant && npx tsc --noEmit src/app/dashboard/approvals/page.tsx</verify>
  <done>Approvals page accessible at /dashboard/approvals, navigation includes Approvals link</done>
</task>

</tasks>

<verification>
- /dashboard/approvals page renders without errors
- ApprovalQueue fetches from /api/agent/approvals
- Approve button calls PATCH with decision: 'approved'
- Reject button calls PATCH with decision: 'rejected'
- Resolved items disappear from the queue
- Navigation includes Approvals link
</verification>

<success_criteria>
- Dashboard shows pending agent actions in a queue with agent name, action summary, confidence score, and priority
- Andy can approve or reject each action with a single click
- Queue auto-refreshes to show new items
</success_criteria>

<output>
After completion, create `.planning/phases/09-approval-flow/09-02-SUMMARY.md`
</output>
