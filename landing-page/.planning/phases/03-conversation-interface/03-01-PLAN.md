---
phase: 03-conversation-interface
plan: 01
type: execute
---

<objective>
Build the conversation interface for interacting with BitBit agent.

Purpose: Create a demo UI that shows BitBit's intelligence — send messages, see responses, view actions taken.
Output: Working chat page at `/chat` with message input, channel selector, and BitBit response display.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase (agent core):
@.planning/phases/02-agent-core/02-03-SUMMARY.md

# Key files from prior work:
@lib/agent/types.ts
@app/api/agent/route.ts
@lib/agent/response.ts

**Tech stack available:** Next.js 15, React 19, Tailwind CSS, better-sqlite3, Claude SDK
**Established patterns:** API routes return JSON, existing InboxLayout component pattern
**Constraining decisions:**
- AgentRequest: { message, channel, sender: { type, email?, phone?, name? }, context? }
- AgentResponse: { message, actions_taken, reasoning, confidence, session_id, should_escalate, routing }
- Channels: whatsapp, email, voice, sms
- Sender types: customer, xixi, allen
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chat page with message input and controls</name>
  <files>app/chat/page.tsx, app/chat/ChatInterface.tsx</files>
  <action>
Create /chat route with client-side chat interface:

**ChatInterface.tsx** (client component):
- State: messages array, input value, loading, selected channel, selected sender
- Channel selector: dropdown/button group for whatsapp/email/voice/sms
- Sender selector: customer/xixi/allen (with optional name/email fields for customer)
- Text input (textarea) for message with send button
- Message history display: alternating user/assistant messages
- Loading spinner while waiting for agent response

**page.tsx** (server component):
- Simple page wrapper that renders ChatInterface
- Page title "BitBit Conversation"

UI layout:
- Full width, centered max-w-4xl container
- Channel/sender controls at top in a row
- Message history in scrollable area (flex-grow)
- Input area pinned at bottom
- Use existing Tailwind patterns from InboxLayout

Call POST /api/agent on send, display response in message history.
  </action>
  <verify>npm run build passes, navigate to /chat shows interface with inputs</verify>
  <done>Chat page loads at /chat, can type message, select channel/sender, see controls</done>
</task>

<task type="auto">
  <name>Task 2: Add BitBit response display with actions panel</name>
  <files>app/chat/ChatInterface.tsx, app/chat/ResponseCard.tsx</files>
  <action>
Enhance response display to show BitBit's full output:

**ResponseCard.tsx**:
- Props: AgentResponse object
- Main response message (large, clear)
- Confidence indicator: pill showing HIGH/MEDIUM/LOW with color (green/yellow/red)
- Actions taken section:
  - Each action as a collapsed/expandable item
  - Show action type (lookup_order, send_reply, etc.) as heading
  - Show params and result on expand
- Routing badge: shows queue (xixi/allen/auto) and auto_resolve status
- Reasoning section: collapsible, shows agent's reasoning
- Session ID: small gray text for reference

Update ChatInterface:
- Render ResponseCard for each assistant message
- Handle should_escalate flag: show yellow "Needs Human Review" banner
- Show "Thinking..." with spinner while loading

Style: cards with subtle borders, good spacing, readable typography.
  </action>
  <verify>Send a test message, response displays with actions panel, confidence, reasoning visible</verify>
  <done>BitBit responses show message, actions taken (expandable), confidence, routing, and reasoning</done>
</task>

<task type="auto">
  <name>Task 3: Add quick scenario buttons for demo</name>
  <files>app/chat/ChatInterface.tsx</files>
  <action>
Add pre-built scenario buttons for quick demos:

**Scenario buttons** (horizontal row above input):
1. "WISMO" - "Where's my order CG-10001?" (customer, whatsapp)
2. "Return Request" - "I want to return my exfoliating gloves" (customer, email)
3. "Product Question" - "How do I use the sensitive skin gloves?" (customer, whatsapp)
4. "Complaint" - "The gloves didn't work at all and my skin is irritated!" (customer, email)
5. "Shipping Exception" - "We got a delay notice from USPS for 50 orders" (allen, whatsapp)

Each button:
- Sets channel and sender appropriately
- Populates message input (or sends immediately - user preference)
- Visual: small pills/buttons with icon+label

Add to top of chat area, below channel/sender selectors.
  </action>
  <verify>Click scenario button, see input populated with message and correct channel/sender selected</verify>
  <done>5 scenario buttons work, each pre-fills appropriate message and context</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `/chat` page loads and displays interface
- [ ] Can type message and send to /api/agent
- [ ] Response displays with message, actions, confidence, reasoning
- [ ] Scenario buttons populate correct messages and context
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Chat interface works end-to-end with /api/agent
- Demo-ready: can show WISMO flow, return request, complaint handling
</success_criteria>

<output>
After completion, create `.planning/phases/03-conversation-interface/03-01-SUMMARY.md`
</output>
