---
phase: 04-audit-dashboard
plan: 01
type: execute
---

<objective>
Build the activity timeline for the audit dashboard.

Purpose: Allow review of BitBit's recent actions with filtering and expandable details.
Output: Working audit page at `/audit` with timeline, filters, and session detail views.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase (conversation interface):
@.planning/phases/03-conversation-interface/03-01-SUMMARY.md

# Key files from prior work:
@lib/agent/audit.ts
@app/api/agent/audit/route.ts
@app/api/agent/session/[sessionId]/route.ts
@app/chat/ResponseCard.tsx

**Tech stack available:** Next.js 15, React 19, Tailwind CSS, better-sqlite3, Claude SDK
**Established patterns:** ResponseCard component for displaying agent responses
**Available APIs:**
- `GET /api/agent/audit` - Recent entries with filters (limit, offset, action_type, success_only)
- `GET /api/agent/audit?summary=true` - Action counts by type
- `GET /api/agent/audit?sessions_only=true` - List of recent session IDs
- `GET /api/agent/session/[sessionId]` - Full session summary with trail

**Types from lib/agent/audit.ts:**
- AuditActionType: 'request' | 'tool_call' | 'response' | 'escalation' | 'error'
- AuditLogEntry: { session_id, action_type, input, output, reasoning, confidence, success, created_at }
- SessionSummary: { session_id, started_at, completed_at, request, trail, outcome }
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit page with sessions list</name>
  <files>app/audit/page.tsx, app/audit/AuditDashboard.tsx</files>
  <action>
Create /audit route with audit dashboard:

**AuditDashboard.tsx** (client component):
- State: sessions array, selectedSession, filters, isLoading
- On mount: fetch sessions from /api/agent/audit?sessions_only=true
- Display sessions as a list/timeline with:
  - Session ID (abbreviated)
  - Timestamp
  - Request preview (first ~50 chars of message)
  - Channel badge
  - Confidence badge (colored: green/yellow/red)
  - Escalation indicator (if escalated)
- Clicking a session loads full details

**page.tsx** (server component):
- Simple wrapper with "Audit Dashboard" header
- Renders AuditDashboard client component

Layout:
- Two-column design: session list (left, ~350px), session details (right, flex-1)
- Session list is scrollable
- Empty state: "No sessions yet. Use the chat interface to create some."
- Use Tailwind styling consistent with /chat page

For session list items, fetch session summary from /api/agent/session/[id] to get the request message and outcome info. Consider fetching on demand or using SWR/React Query pattern (but keep simple for MVP - just fetch when session list loads, limit to 20 sessions).
  </action>
  <verify>npm run build passes, navigate to /audit shows session list with entries</verify>
  <done>Audit page loads at /audit, displays list of recent sessions with key info</done>
</task>

<task type="auto">
  <name>Task 2: Add session detail view with full trail</name>
  <files>app/audit/AuditDashboard.tsx, app/audit/SessionDetail.tsx</files>
  <action>
Add session detail panel when a session is selected:

**SessionDetail.tsx**:
- Props: sessionId (string)
- Fetch full session from /api/agent/session/[sessionId]
- Display:
  - Header: "Session [abbreviated-id]" with timestamp
  - Request section: message, channel, sender type (card style)
  - Outcome section: final response, confidence, escalation status
  - Trail timeline: vertical timeline of all audit entries
    - Each entry shows: action_type icon, timestamp, summary of input/output
    - Entries expandable to show full JSON (like ResponseCard actions)
    - Color-code by action_type: request (blue), tool_call (purple), response (green), escalation (yellow), error (red)
- Loading state while fetching
- Error state if session not found

**Update AuditDashboard.tsx**:
- When session selected, show SessionDetail in right panel
- Highlight selected session in list
- Clear selection button or escape key

Reuse expand/collapse pattern from ResponseCard for showing entry details.
  </action>
  <verify>Click a session in list, detail panel shows full trail with expandable entries</verify>
  <done>Session detail view displays request, outcome, and full audit trail with expandable entries</done>
</task>

<task type="auto">
  <name>Task 3: Add filters and summary stats</name>
  <files>app/audit/AuditDashboard.tsx</files>
  <action>
Add filter controls and summary stats to the audit dashboard:

**Summary stats bar** (top of page):
- Fetch from /api/agent/audit?summary=true on mount
- Display counts: Total Sessions, Tool Calls, Escalations, Errors
- Each as a small stat card with icon and number
- Color-code: tool_calls (purple), escalations (yellow), errors (red)

**Filter controls** (above session list):
- Channel filter: All, WhatsApp, Email, Voice, SMS (dropdown or button group)
- Confidence filter: All, High (>70), Medium (50-70), Low (<50)
- Show escalations only: checkbox/toggle
- Show errors only: checkbox/toggle
- Clear filters button

**Filtering logic**:
- Filters apply to session list (client-side for MVP, sessions are already loaded)
- Each session summary has enough info to filter by channel, confidence, escalation status
- Confidence from outcome.confidence, escalated from outcome.escalated
- Channel from request.channel

Update session list to respect active filters. Show "No matching sessions" if filters result in empty list.
  </action>
  <verify>Summary stats display, filter controls work, session list updates based on filters</verify>
  <done>Dashboard shows summary stats, filter controls work to narrow session list</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `/audit` page loads and displays session list
- [ ] Clicking session shows full detail with audit trail
- [ ] Summary stats display counts from API
- [ ] Filters narrow the session list appropriately
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Audit dashboard shows BitBit's activity history
- Can drill into any session to see full trail
- Demo-ready: can show what BitBit did and why
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-dashboard/04-01-SUMMARY.md`
</output>
