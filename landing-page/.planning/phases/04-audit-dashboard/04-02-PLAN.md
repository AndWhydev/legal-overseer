---
phase: 04-audit-dashboard
plan: 02
type: execute
---

<objective>
Add decision review and metrics to the audit dashboard.

Purpose: Enable quality review of BitBit's decisions — flag errors, see patterns, review pending items.
Output: Enhanced audit dashboard with flagging, pending items section, and basic metrics.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan in this phase:
@.planning/phases/04-audit-dashboard/04-01-SUMMARY.md

# Key files from prior work:
@app/audit/AuditDashboard.tsx
@app/audit/SessionDetail.tsx
@lib/agent/audit.ts

**Tech stack available:** Next.js 15, React 19, Tailwind CSS, better-sqlite3
**Established patterns:** SessionDetail component, expand/collapse trail entries

**Database tables available:**
- agent_actions (audit log): id, session_id, action_type, input, output, reasoning, confidence, success, error_message, created_at
- tasks: id, title, description, status, priority, owner, category, context, created_at, updated_at

**Note:** For MVP, "flagging" can be lightweight — we don't need a new database table. Options:
1. Create a task when flagging (reuse existing tasks table with category="audit_flag")
2. Local state only (lost on refresh, but simpler)
3. Add a "flagged" column to agent_actions

Recommend option 1: Create task when flagging. This integrates with existing task system and makes flags visible in task list. The task context can store the session_id.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flag/review controls to session detail</name>
  <files>app/audit/SessionDetail.tsx, lib/services/tasks.ts</files>
  <action>
Add ability to flag sessions for review:

**Update SessionDetail.tsx**:
- Add "Flag for Review" button in session header
- On click: open a simple modal/form to capture:
  - Issue type: dropdown (wrong_action, missed_action, incorrect_response, needs_policy_update, other)
  - Notes: textarea for description
- On submit: call createTask from lib/services/tasks.ts with:
  - title: "Audit Flag: [issue_type]"
  - description: notes + link to session
  - category: "audit_review"
  - owner: "xixi" (default reviewer)
  - context: JSON with session_id, flagged_at, issue_type
  - priority: "high"
- Show success/error feedback
- Update UI to show "Flagged" badge after successful flag

**No new API needed** - use existing tasks service directly (this is server-side component, but we may need a simple API route to create the task from client):

If SessionDetail is client component, create:
**app/api/audit/flag/route.ts**:
- POST with { session_id, issue_type, notes }
- Creates task using tasks service
- Returns { success: true, task_id }

Show confirmation: "Flagged for review. Task #[id] created."
  </action>
  <verify>Flag a session, task appears in tasks table, session shows "Flagged" badge</verify>
  <done>Sessions can be flagged for review, creates task in system, visual indicator shows flagged state</done>
</task>

<task type="auto">
  <name>Task 2: Add pending items section</name>
  <files>app/audit/AuditDashboard.tsx, app/audit/PendingItems.tsx</files>
  <action>
Add a section for pending/escalated items that need human attention:

**PendingItems.tsx**:
- Fetch escalated sessions: filter sessions where outcome.escalated = true
- Fetch flagged tasks: GET /api/tasks?category=audit_review (or filter locally)
- Display two subsections:

1. **Escalated Sessions** (need human input):
   - List of sessions where BitBit asked for help or low confidence
   - Each shows: session preview, confidence, escalation reason
   - Click to view full session detail
   - "Mark Resolved" button (for MVP, just hides from list locally or could delete the escalation log entry)

2. **Flagged for Review** (quality issues):
   - List of audit_review tasks
   - Each shows: issue type, notes preview, session link, created_at
   - "View Session" link to jump to session detail
   - "Dismiss" to mark task as complete

**Update AuditDashboard.tsx**:
- Add tab or toggle: "Activity Timeline" | "Pending Items"
- Or show pending items as collapsible section at top when there are items
- Badge showing count of pending items

Keep it simple for MVP — this is a review queue, not a full task management system.
  </action>
  <verify>Pending items section shows escalated sessions and flagged tasks, can dismiss items</verify>
  <done>Pending items section displays escalations and flags, items can be dismissed/resolved</done>
</task>

<task type="auto">
  <name>Task 3: Add basic metrics display</name>
  <files>app/audit/AuditDashboard.tsx, app/audit/MetricsPanel.tsx</files>
  <action>
Add a metrics panel showing BitBit performance overview:

**MetricsPanel.tsx**:
- Fetch data from existing APIs + compute metrics client-side
- Display metrics:

1. **Today's Activity**:
   - Sessions handled today (filter by created_at)
   - Auto-resolved vs escalated ratio
   - Average confidence score

2. **Confidence Distribution** (visual):
   - Simple bar chart or progress bars showing:
     - High confidence (>70%): X sessions
     - Medium (50-70%): Y sessions
     - Low (<50%): Z sessions
   - Use colored bars (green/yellow/red)

3. **Action Breakdown**:
   - Tool calls per session (average)
   - Most common actions (top 3)
   - Error rate

For MVP, use simple div-based visualization (colored bars with widths set by percentage). No charting library needed.

**Update AuditDashboard.tsx**:
- Add MetricsPanel above or beside session list
- Could be collapsible "Metrics" section
- Refresh metrics when filters change or new data loads

This gives a quick health check: "Is BitBit doing a good job? What's the confidence level? How many escalations?"
  </action>
  <verify>Metrics panel displays with activity counts, confidence distribution, action breakdown</verify>
  <done>Basic metrics show BitBit performance overview with visual confidence distribution</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Can flag a session for review
- [ ] Pending items section shows escalations and flags
- [ ] Metrics panel displays performance overview
- [ ] Confidence distribution visualization works
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Audit dashboard enables quality review workflow
- Can flag issues, see pending items, view metrics
- Demo-ready: can show "here's how we monitor and improve BitBit"
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-dashboard/04-02-SUMMARY.md`

**Phase 4 Complete**: Document that the audit dashboard is complete and the MVP is demo-ready.
</output>
