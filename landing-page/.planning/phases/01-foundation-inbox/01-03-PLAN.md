---
phase: 01-foundation-inbox
plan: 03
type: execute
---

<objective>
Create item detail page with original content display and empty recommendation panel.

Purpose: Complete the inbox shell so users can view individual items before AI integration.
Output: Working detail view with content thread and placeholder for BitBit recommendations.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/CLIENT-PACK.md
@.planning/phases/01-foundation-inbox/01-01-SUMMARY.md
@.planning/phases/01-foundation-inbox/01-02-SUMMARY.md
@lib/db.ts
@lib/types.ts
@app/components/ApprovalCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create item detail page with original content</name>
  <files>app/item/[id]/page.tsx, app/components/ItemContent.tsx, lib/queries.ts, app/api/items/[id]/route.ts</files>
  <action>
1. Update lib/queries.ts:
```typescript
export function getItemById(id: number): ApprovalItem | null
export function getAuditLogForItem(id: number): AuditEntry[]
```

2. app/api/items/[id]/route.ts - Single item API:
   - GET /api/items/[id] returns item with full details
   - 404 if not found

3. app/components/ItemContent.tsx - Content display:
   - Full subject as heading
   - Sender info with email
   - Metadata bar: type, status, priority, risk level, due date
   - Full body content (preserve whitespace, handle line breaks)
   - Attachments section (show links if present, placeholder for images)
   - For customer support items: show order #, tracking #, delivery status
   - For content items: show asset link, platform, publish date
   - Timestamp: "Received [relative time]"

4. app/item/[id]/page.tsx - Detail page:
   - Two-column layout (content left, recommendation right)
   - Back button to inbox (preserve lane/filter state)
   - Left column: ItemContent component
   - Right column: Empty panel with "BitBit is analyzing..." placeholder
   - Responsive: stack columns on mobile
  </action>
  <verify>Navigate to /item/1, page renders without error (404 expected until seed data), layout shows two columns on desktop</verify>
  <done>Item detail page with content display and two-column layout</done>
</task>

<task type="auto">
  <name>Task 2: Add recommendation panel shell</name>
  <files>app/components/RecommendationPanel.tsx, app/item/[id]/page.tsx</files>
  <action>
1. app/components/RecommendationPanel.tsx - Placeholder panel:
   - Card container with "BitBit Recommendation" header
   - Sections (all placeholder for Phase 2):
     - Summary: "Analyzing content..." with loading shimmer
     - Recommendation: Empty card
     - Risk Flags: Empty card
     - Draft Response: Empty card with "Will appear after analysis"
     - Task Plan: Empty card
   - Decision buttons at bottom (disabled for now):
     - Approve (green)
     - Needs Changes (yellow)
     - Reject (red)
     - Escalate (purple)
   - Style to match BitBit brand

2. Update app/item/[id]/page.tsx:
   - Pass item to RecommendationPanel
   - Show "No item selected" if item is null
   - Add "Generated in X.Xs" placeholder (will be real in Phase 2)

The panel should look complete but with placeholder content. This creates the visual structure that Phase 2 will populate with real AI output.
  </action>
  <verify>Item detail page shows recommendation panel with all placeholder sections, buttons are visible but disabled</verify>
  <done>Recommendation panel shell with all sections and disabled action buttons</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 Foundation: Next.js app with database, two-lane inbox UI with filters, and item detail view with recommendation panel shell</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000
    3. Verify inbox tabs switch between Xixi and Allen
    4. Verify filter dropdowns appear and update URL
    5. Click on any card (will show empty state or 404 - that's ok)
    6. Visit: http://localhost:3000/item/1 to see detail layout
    7. Confirm two-column layout with content left, recommendation panel right
    8. Confirm mobile responsive (stack columns)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Item detail page renders at /item/[id]
- [ ] Two-column layout works (content + recommendation)
- [ ] Recommendation panel shows all placeholder sections
- [ ] Action buttons visible but disabled
- [ ] Responsive layout works on mobile
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- Item detail page with full content display
- Recommendation panel shell with all sections
- Decision action buttons (disabled)
- Responsive two-column layout
- Phase 1 complete: foundation ready for Phase 2 AI integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-inbox/01-03-SUMMARY.md` with:
- Summary of Phase 1 completion
- All files created
- Ready for Phase 2: AI + Policy Engine
</output>
