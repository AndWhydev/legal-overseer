/**
 * Matters module — public surface for matter-scoped features (budget,
 * supervision, bulk import). The core matters repository lives at
 * src/db/repositories/matters.ts.
 */

export * from './budgeting.js';
